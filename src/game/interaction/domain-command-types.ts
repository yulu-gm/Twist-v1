/**
 * 再导出：契约类型定义见 `game/contracts/domain-command-types`。
 */
export type {
  DomainCommand,
  DomainVerb,
  InteractionSource,
  LineAReadPort,
  WorldSubmitResult
} from "../contracts/domain-command-types";
