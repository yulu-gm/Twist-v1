---
name: feedback-ui-stability
description: UI 元素优先常驻显示，避免条件隐藏导致布局闪烁
type: feedback
---
UI 元素在条件切换时宁可常驻显示，也不要自动隐藏/显示导致布局抖动和闪烁。

**Why:** 用户发现同格对象标签栏在对象数量 1↔多 快速切换时，自动隐藏会造成明显闪烁。

**How to apply:** 设计条件渲染的 UI 组件时，优先考虑始终渲染（内容可为空），而非根据数据条数隐藏整个容器。特别是 Inspector 面板内的子组件，应保持布局稳定。
