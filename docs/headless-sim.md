# Headless 模拟

规范版完整说明见 [docs/human/headless-sim.md](./human/headless-sim.md)（按 `.agent/doc-rules.md` 存放）。下列标题用于与任务计划路径对齐。

## 人工验收

浏览器侧载入与核对流程见 **[人工验收](./human/headless-sim.md#人工验收)**：`loadScenarioIntoGame`、`listAvailableScenarios` 与 `ScenarioLoadResult`（`world` + `pawnStates`）。

## Agent 工作流标准步骤

见 **[Agent 工作流标准步骤](./human/headless-sim.md#agent-工作流标准步骤)**；日常命令：`npm run test:headless`。
