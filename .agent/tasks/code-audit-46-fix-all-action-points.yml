# code-audit-4.6：按行动点逐项修复（子任务集）
# 生成器见 .agent/tasks/_gen_audit_fix_tree.py

task_name: "code-audit-4.6 审计报告：修复全部行动点（逐项子任务）"
created_at: "2026-04-06"
status: "completed"

orchestration_note: |
  主 Agent 向子 Agent 派发时，将对应条目的 `subagent_context_injection` **原样注入**为系统/用户前言。
  本文件内任务按 `id`（AP-0001 … AP-0366）升序排列，与全局行动点编号一致；字段 `audit_report` / `target_src` 由生成器按审计报告路径规则映射。

tasks:
  - id: "AP-0001"
    title: "行动点 #0001"
    status: "completed"
    dependencies: []
    action_point_id: "0001"
    audit_report: "working-plan/skill-audit-code-check/src/data/command-menu.md"
    target_src: "src/data/command-menu.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/data/command-menu.md"
        - "src/data/command-menu.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0001**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/data/command-menu.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/data/command-menu.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/data/command-menu.md`，找到 **行动点 #0001** 的完整表述与依据引用的 YAML。
      3. Read `src/data/command-menu.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/data/command-menu.md` 中 **行动点 #0001** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0002"
    title: "行动点 #0002"
    status: "completed"
    dependencies: []
    action_point_id: "0002"
    audit_report: "working-plan/skill-audit-code-check/src/data/command-menu.md"
    target_src: "src/data/command-menu.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/data/command-menu.md"
        - "src/data/command-menu.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0002**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/data/command-menu.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/data/command-menu.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/data/command-menu.md`，找到 **行动点 #0002** 的完整表述与依据引用的 YAML。
      3. Read `src/data/command-menu.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/data/command-menu.md` 中 **行动点 #0002** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0003"
    title: "行动点 #0003"
    status: "completed"
    dependencies: []
    action_point_id: "0003"
    audit_report: "working-plan/skill-audit-code-check/src/data/command-menu.md"
    target_src: "src/data/command-menu.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/data/command-menu.md"
        - "src/data/command-menu.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0003**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/data/command-menu.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/data/command-menu.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/data/command-menu.md`，找到 **行动点 #0003** 的完整表述与依据引用的 YAML。
      3. Read `src/data/command-menu.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/data/command-menu.md` 中 **行动点 #0003** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0004"
    title: "行动点 #0004"
    status: "completed"
    dependencies: []
    action_point_id: "0004"
    audit_report: "working-plan/skill-audit-code-check/src/data/command-menu.md"
    target_src: "src/data/command-menu.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/data/command-menu.md"
        - "src/data/command-menu.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0004**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/data/command-menu.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/data/command-menu.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/data/command-menu.md`，找到 **行动点 #0004** 的完整表述与依据引用的 YAML。
      3. Read `src/data/command-menu.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/data/command-menu.md` 中 **行动点 #0004** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0005"
    title: "行动点 #0005"
    status: "completed"
    dependencies: []
    action_point_id: "0005"
    audit_report: "working-plan/skill-audit-code-check/src/data/grid-cell-info.md"
    target_src: "src/data/grid-cell-info.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/data/grid-cell-info.md"
        - "src/data/grid-cell-info.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0005**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/data/grid-cell-info.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/data/grid-cell-info.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/data/grid-cell-info.md`，找到 **行动点 #0005** 的完整表述与依据引用的 YAML。
      3. Read `src/data/grid-cell-info.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/data/grid-cell-info.md` 中 **行动点 #0005** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0006"
    title: "行动点 #0006"
    status: "completed"
    dependencies: []
    action_point_id: "0006"
    audit_report: "working-plan/skill-audit-code-check/src/data/grid-cell-info.md"
    target_src: "src/data/grid-cell-info.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/data/grid-cell-info.md"
        - "src/data/grid-cell-info.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0006**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/data/grid-cell-info.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/data/grid-cell-info.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/data/grid-cell-info.md`，找到 **行动点 #0006** 的完整表述与依据引用的 YAML。
      3. Read `src/data/grid-cell-info.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/data/grid-cell-info.md` 中 **行动点 #0006** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0007"
    title: "行动点 #0007"
    status: "completed"
    dependencies: []
    action_point_id: "0007"
    audit_report: "working-plan/skill-audit-code-check/src/data/grid-cell-info.md"
    target_src: "src/data/grid-cell-info.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/data/grid-cell-info.md"
        - "src/data/grid-cell-info.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0007**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/data/grid-cell-info.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/data/grid-cell-info.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/data/grid-cell-info.md`，找到 **行动点 #0007** 的完整表述与依据引用的 YAML。
      3. Read `src/data/grid-cell-info.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/data/grid-cell-info.md` 中 **行动点 #0007** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0008"
    title: "行动点 #0008"
    status: "completed"
    dependencies: []
    action_point_id: "0008"
    audit_report: "working-plan/skill-audit-code-check/src/data/ground-items.md"
    target_src: "src/data/ground-items.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/data/ground-items.md"
        - "src/data/ground-items.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0008**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/data/ground-items.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/data/ground-items.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/data/ground-items.md`，找到 **行动点 #0008** 的完整表述与依据引用的 YAML。
      3. Read `src/data/ground-items.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/data/ground-items.md` 中 **行动点 #0008** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0009"
    title: "行动点 #0009"
    status: "completed"
    dependencies: []
    action_point_id: "0009"
    audit_report: "working-plan/skill-audit-code-check/src/data/ground-items.md"
    target_src: "src/data/ground-items.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/data/ground-items.md"
        - "src/data/ground-items.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0009**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/data/ground-items.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/data/ground-items.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/data/ground-items.md`，找到 **行动点 #0009** 的完整表述与依据引用的 YAML。
      3. Read `src/data/ground-items.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/data/ground-items.md` 中 **行动点 #0009** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0010"
    title: "行动点 #0010"
    status: "completed"
    dependencies: []
    action_point_id: "0010"
    audit_report: "working-plan/skill-audit-code-check/src/data/ground-items.md"
    target_src: "src/data/ground-items.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/data/ground-items.md"
        - "src/data/ground-items.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0010**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/data/ground-items.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/data/ground-items.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/data/ground-items.md`，找到 **行动点 #0010** 的完整表述与依据引用的 YAML。
      3. Read `src/data/ground-items.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/data/ground-items.md` 中 **行动点 #0010** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0011"
    title: "行动点 #0011"
    status: "completed"
    dependencies: []
    action_point_id: "0011"
    audit_report: "working-plan/skill-audit-code-check/src/data/task-markers.md"
    target_src: "src/data/task-markers.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/data/task-markers.md"
        - "src/data/task-markers.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0011**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/data/task-markers.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/data/task-markers.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/data/task-markers.md`，找到 **行动点 #0011** 的完整表述与依据引用的 YAML。
      3. Read `src/data/task-markers.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/data/task-markers.md` 中 **行动点 #0011** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0012"
    title: "行动点 #0012"
    status: "completed"
    dependencies: []
    action_point_id: "0012"
    audit_report: "working-plan/skill-audit-code-check/src/data/task-markers.md"
    target_src: "src/data/task-markers.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/data/task-markers.md"
        - "src/data/task-markers.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0012**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/data/task-markers.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/data/task-markers.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/data/task-markers.md`，找到 **行动点 #0012** 的完整表述与依据引用的 YAML。
      3. Read `src/data/task-markers.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/data/task-markers.md` 中 **行动点 #0012** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0013"
    title: "行动点 #0013"
    status: "completed"
    dependencies: []
    action_point_id: "0013"
    audit_report: "working-plan/skill-audit-code-check/src/game/README.md"
    target_src: "src/game/README.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/README.md"
        - "src/game/README.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0013**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/README.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/README.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/README.md`，找到 **行动点 #0013** 的完整表述与依据引用的 YAML。
      3. Read `src/game/README.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/README.md` 中 **行动点 #0013** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0014"
    title: "行动点 #0014"
    status: "completed"
    dependencies: []
    action_point_id: "0014"
    audit_report: "working-plan/skill-audit-code-check/src/game/README.md"
    target_src: "src/game/README.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/README.md"
        - "src/game/README.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0014**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/README.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/README.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/README.md`，找到 **行动点 #0014** 的完整表述与依据引用的 YAML。
      3. Read `src/game/README.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/README.md` 中 **行动点 #0014** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0015"
    title: "行动点 #0015"
    status: "completed"
    dependencies: []
    action_point_id: "0015"
    audit_report: "working-plan/skill-audit-code-check/src/game/README.md"
    target_src: "src/game/README.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/README.md"
        - "src/game/README.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0015**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/README.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/README.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/README.md`，找到 **行动点 #0015** 的完整表述与依据引用的 YAML。
      3. Read `src/game/README.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/README.md` 中 **行动点 #0015** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0016"
    title: "行动点 #0016"
    status: "completed"
    dependencies: []
    action_point_id: "0016"
    audit_report: "working-plan/skill-audit-code-check/src/game/README.md"
    target_src: "src/game/README.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/README.md"
        - "src/game/README.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0016**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/README.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/README.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/README.md`，找到 **行动点 #0016** 的完整表述与依据引用的 YAML。
      3. Read `src/game/README.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/README.md` 中 **行动点 #0016** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0017"
    title: "行动点 #0017"
    status: "completed"
    dependencies: []
    action_point_id: "0017"
    audit_report: "working-plan/skill-audit-code-check/src/game/README.md"
    target_src: "src/game/README.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/README.md"
        - "src/game/README.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0017**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/README.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/README.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/README.md`，找到 **行动点 #0017** 的完整表述与依据引用的 YAML。
      3. Read `src/game/README.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/README.md` 中 **行动点 #0017** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0018"
    title: "行动点 #0018"
    status: "completed"
    dependencies: []
    action_point_id: "0018"
    audit_report: "working-plan/skill-audit-code-check/src/game/bed-auto-assign.md"
    target_src: "src/game/bed-auto-assign.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/bed-auto-assign.md"
        - "src/game/bed-auto-assign.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0018**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/bed-auto-assign.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/bed-auto-assign.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/bed-auto-assign.md`，找到 **行动点 #0018** 的完整表述与依据引用的 YAML。
      3. Read `src/game/bed-auto-assign.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/bed-auto-assign.md` 中 **行动点 #0018** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0019"
    title: "行动点 #0019"
    status: "completed"
    dependencies: []
    action_point_id: "0019"
    audit_report: "working-plan/skill-audit-code-check/src/game/bed-auto-assign.md"
    target_src: "src/game/bed-auto-assign.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/bed-auto-assign.md"
        - "src/game/bed-auto-assign.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0019**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/bed-auto-assign.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/bed-auto-assign.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/bed-auto-assign.md`，找到 **行动点 #0019** 的完整表述与依据引用的 YAML。
      3. Read `src/game/bed-auto-assign.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/bed-auto-assign.md` 中 **行动点 #0019** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0020"
    title: "行动点 #0020"
    status: "completed"
    dependencies: []
    action_point_id: "0020"
    audit_report: "working-plan/skill-audit-code-check/src/game/bed-auto-assign.md"
    target_src: "src/game/bed-auto-assign.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/bed-auto-assign.md"
        - "src/game/bed-auto-assign.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0020**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/bed-auto-assign.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/bed-auto-assign.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/bed-auto-assign.md`，找到 **行动点 #0020** 的完整表述与依据引用的 YAML。
      3. Read `src/game/bed-auto-assign.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/bed-auto-assign.md` 中 **行动点 #0020** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0021"
    title: "行动点 #0021"
    status: "completed"
    dependencies: []
    action_point_id: "0021"
    audit_report: "working-plan/skill-audit-code-check/src/game/bed-auto-assign.md"
    target_src: "src/game/bed-auto-assign.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/bed-auto-assign.md"
        - "src/game/bed-auto-assign.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0021**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/bed-auto-assign.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/bed-auto-assign.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/bed-auto-assign.md`，找到 **行动点 #0021** 的完整表述与依据引用的 YAML。
      3. Read `src/game/bed-auto-assign.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/bed-auto-assign.md` 中 **行动点 #0021** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0022"
    title: "行动点 #0022"
    status: "completed"
    dependencies: []
    action_point_id: "0022"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/a-star-pathfinding.md"
    target_src: "src/game/behavior/a-star-pathfinding.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/a-star-pathfinding.md"
        - "src/game/behavior/a-star-pathfinding.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0022**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/a-star-pathfinding.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/a-star-pathfinding.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/a-star-pathfinding.md`，找到 **行动点 #0022** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/a-star-pathfinding.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/a-star-pathfinding.md` 中 **行动点 #0022** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0023"
    title: "行动点 #0023"
    status: "completed"
    dependencies: []
    action_point_id: "0023"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/a-star-pathfinding.md"
    target_src: "src/game/behavior/a-star-pathfinding.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/a-star-pathfinding.md"
        - "src/game/behavior/a-star-pathfinding.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0023**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/a-star-pathfinding.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/a-star-pathfinding.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/a-star-pathfinding.md`，找到 **行动点 #0023** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/a-star-pathfinding.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/a-star-pathfinding.md` 中 **行动点 #0023** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0024"
    title: "行动点 #0024"
    status: "completed"
    dependencies: []
    action_point_id: "0024"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/a-star-pathfinding.md"
    target_src: "src/game/behavior/a-star-pathfinding.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/a-star-pathfinding.md"
        - "src/game/behavior/a-star-pathfinding.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0024**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/a-star-pathfinding.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/a-star-pathfinding.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/a-star-pathfinding.md`，找到 **行动点 #0024** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/a-star-pathfinding.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/a-star-pathfinding.md` 中 **行动点 #0024** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0025"
    title: "行动点 #0025"
    status: "completed"
    dependencies: []
    action_point_id: "0025"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/a-star-pathfinding.md"
    target_src: "src/game/behavior/a-star-pathfinding.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/a-star-pathfinding.md"
        - "src/game/behavior/a-star-pathfinding.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0025**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/a-star-pathfinding.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/a-star-pathfinding.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/a-star-pathfinding.md`，找到 **行动点 #0025** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/a-star-pathfinding.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/a-star-pathfinding.md` 中 **行动点 #0025** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0026"
    title: "行动点 #0026"
    status: "completed"
    dependencies: []
    action_point_id: "0026"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/action-scorer.md"
    target_src: "src/game/behavior/action-scorer.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/action-scorer.md"
        - "src/game/behavior/action-scorer.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0026**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/action-scorer.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/action-scorer.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/action-scorer.md`，找到 **行动点 #0026** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/action-scorer.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/action-scorer.md` 中 **行动点 #0026** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0027"
    title: "行动点 #0027"
    status: "completed"
    dependencies: []
    action_point_id: "0027"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/action-scorer.md"
    target_src: "src/game/behavior/action-scorer.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/action-scorer.md"
        - "src/game/behavior/action-scorer.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0027**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/action-scorer.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/action-scorer.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/action-scorer.md`，找到 **行动点 #0027** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/action-scorer.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/action-scorer.md` 中 **行动点 #0027** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0028"
    title: "行动点 #0028"
    status: "completed"
    dependencies: []
    action_point_id: "0028"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/action-scorer.md"
    target_src: "src/game/behavior/action-scorer.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/action-scorer.md"
        - "src/game/behavior/action-scorer.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0028**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/action-scorer.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/action-scorer.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/action-scorer.md`，找到 **行动点 #0028** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/action-scorer.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/action-scorer.md` 中 **行动点 #0028** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0029"
    title: "行动点 #0029"
    status: "completed"
    dependencies: []
    action_point_id: "0029"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/action-scorer.md"
    target_src: "src/game/behavior/action-scorer.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/action-scorer.md"
        - "src/game/behavior/action-scorer.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0029**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/action-scorer.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/action-scorer.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/action-scorer.md`，找到 **行动点 #0029** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/action-scorer.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/action-scorer.md` 中 **行动点 #0029** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0030"
    title: "行动点 #0030"
    status: "completed"
    dependencies: []
    action_point_id: "0030"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/action-scorer.md"
    target_src: "src/game/behavior/action-scorer.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/action-scorer.md"
        - "src/game/behavior/action-scorer.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0030**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/action-scorer.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/action-scorer.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/action-scorer.md`，找到 **行动点 #0030** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/action-scorer.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/action-scorer.md` 中 **行动点 #0030** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0031"
    title: "行动点 #0031"
    status: "completed"
    dependencies: []
    action_point_id: "0031"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/behavior-context.md"
    target_src: "src/game/behavior/behavior-context.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/behavior-context.md"
        - "src/game/behavior/behavior-context.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0031**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/behavior-context.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/behavior-context.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/behavior-context.md`，找到 **行动点 #0031** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/behavior-context.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/behavior-context.md` 中 **行动点 #0031** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0032"
    title: "行动点 #0032"
    status: "completed"
    dependencies: []
    action_point_id: "0032"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/behavior-context.md"
    target_src: "src/game/behavior/behavior-context.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/behavior-context.md"
        - "src/game/behavior/behavior-context.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0032**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/behavior-context.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/behavior-context.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/behavior-context.md`，找到 **行动点 #0032** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/behavior-context.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/behavior-context.md` 中 **行动点 #0032** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0033"
    title: "行动点 #0033"
    status: "completed"
    dependencies: []
    action_point_id: "0033"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/behavior-context.md"
    target_src: "src/game/behavior/behavior-context.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/behavior-context.md"
        - "src/game/behavior/behavior-context.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0033**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/behavior-context.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/behavior-context.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/behavior-context.md`，找到 **行动点 #0033** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/behavior-context.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/behavior-context.md` 中 **行动点 #0033** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0034"
    title: "行动点 #0034"
    status: "completed"
    dependencies: []
    action_point_id: "0034"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/behavior-context.md"
    target_src: "src/game/behavior/behavior-context.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/behavior-context.md"
        - "src/game/behavior/behavior-context.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0034**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/behavior-context.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/behavior-context.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/behavior-context.md`，找到 **行动点 #0034** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/behavior-context.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/behavior-context.md` 中 **行动点 #0034** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0035"
    title: "行动点 #0035"
    status: "completed"
    dependencies: []
    action_point_id: "0035"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/behavior-state-machine.md"
    target_src: "src/game/behavior/behavior-state-machine.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/behavior-state-machine.md"
        - "src/game/behavior/behavior-state-machine.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0035**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/behavior-state-machine.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/behavior-state-machine.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/behavior-state-machine.md`，找到 **行动点 #0035** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/behavior-state-machine.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/behavior-state-machine.md` 中 **行动点 #0035** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0036"
    title: "行动点 #0036"
    status: "completed"
    dependencies: []
    action_point_id: "0036"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/behavior-state-machine.md"
    target_src: "src/game/behavior/behavior-state-machine.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/behavior-state-machine.md"
        - "src/game/behavior/behavior-state-machine.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0036**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/behavior-state-machine.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/behavior-state-machine.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/behavior-state-machine.md`，找到 **行动点 #0036** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/behavior-state-machine.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/behavior-state-machine.md` 中 **行动点 #0036** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0037"
    title: "行动点 #0037"
    status: "completed"
    dependencies: []
    action_point_id: "0037"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/behavior-state-machine.md"
    target_src: "src/game/behavior/behavior-state-machine.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/behavior-state-machine.md"
        - "src/game/behavior/behavior-state-machine.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0037**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/behavior-state-machine.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/behavior-state-machine.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/behavior-state-machine.md`，找到 **行动点 #0037** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/behavior-state-machine.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/behavior-state-machine.md` 中 **行动点 #0037** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0038"
    title: "行动点 #0038"
    status: "completed"
    dependencies: []
    action_point_id: "0038"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/behavior-state-machine.md"
    target_src: "src/game/behavior/behavior-state-machine.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/behavior-state-machine.md"
        - "src/game/behavior/behavior-state-machine.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0038**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/behavior-state-machine.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/behavior-state-machine.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/behavior-state-machine.md`，找到 **行动点 #0038** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/behavior-state-machine.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/behavior-state-machine.md` 中 **行动点 #0038** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0039"
    title: "行动点 #0039"
    status: "completed"
    dependencies: []
    action_point_id: "0039"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/goal-driven-planning.md"
    target_src: "src/game/behavior/goal-driven-planning.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/goal-driven-planning.md"
        - "src/game/behavior/goal-driven-planning.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0039**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/goal-driven-planning.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/goal-driven-planning.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/goal-driven-planning.md`，找到 **行动点 #0039** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/goal-driven-planning.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/goal-driven-planning.md` 中 **行动点 #0039** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0040"
    title: "行动点 #0040"
    status: "completed"
    dependencies: []
    action_point_id: "0040"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/goal-driven-planning.md"
    target_src: "src/game/behavior/goal-driven-planning.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/goal-driven-planning.md"
        - "src/game/behavior/goal-driven-planning.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0040**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/goal-driven-planning.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/goal-driven-planning.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/goal-driven-planning.md`，找到 **行动点 #0040** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/goal-driven-planning.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/goal-driven-planning.md` 中 **行动点 #0040** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0041"
    title: "行动点 #0041"
    status: "completed"
    dependencies: []
    action_point_id: "0041"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/goal-driven-planning.md"
    target_src: "src/game/behavior/goal-driven-planning.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/goal-driven-planning.md"
        - "src/game/behavior/goal-driven-planning.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0041**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/goal-driven-planning.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/goal-driven-planning.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/goal-driven-planning.md`，找到 **行动点 #0041** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/goal-driven-planning.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/goal-driven-planning.md` 中 **行动点 #0041** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0042"
    title: "行动点 #0042"
    status: "completed"
    dependencies: []
    action_point_id: "0042"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/goal-driven-planning.md"
    target_src: "src/game/behavior/goal-driven-planning.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/goal-driven-planning.md"
        - "src/game/behavior/goal-driven-planning.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0042**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/goal-driven-planning.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/goal-driven-planning.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/goal-driven-planning.md`，找到 **行动点 #0042** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/goal-driven-planning.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/goal-driven-planning.md` 中 **行动点 #0042** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0043"
    title: "行动点 #0043"
    status: "completed"
    dependencies: []
    action_point_id: "0043"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/goal-driven-planning.md"
    target_src: "src/game/behavior/goal-driven-planning.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/goal-driven-planning.md"
        - "src/game/behavior/goal-driven-planning.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0043**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/goal-driven-planning.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/goal-driven-planning.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/goal-driven-planning.md`，找到 **行动点 #0043** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/goal-driven-planning.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/goal-driven-planning.md` 中 **行动点 #0043** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0044"
    title: "行动点 #0044"
    status: "completed"
    dependencies: []
    action_point_id: "0044"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/sim-config.md"
    target_src: "src/game/behavior/sim-config.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/sim-config.md"
        - "src/game/behavior/sim-config.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0044**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/sim-config.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/sim-config.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/sim-config.md`，找到 **行动点 #0044** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/sim-config.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/sim-config.md` 中 **行动点 #0044** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0045"
    title: "行动点 #0045"
    status: "completed"
    dependencies: []
    action_point_id: "0045"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/sim-config.md"
    target_src: "src/game/behavior/sim-config.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/sim-config.md"
        - "src/game/behavior/sim-config.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0045**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/sim-config.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/sim-config.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/sim-config.md`，找到 **行动点 #0045** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/sim-config.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/sim-config.md` 中 **行动点 #0045** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0046"
    title: "行动点 #0046"
    status: "completed"
    dependencies: []
    action_point_id: "0046"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/sim-config.md"
    target_src: "src/game/behavior/sim-config.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/sim-config.md"
        - "src/game/behavior/sim-config.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0046**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/sim-config.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/sim-config.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/sim-config.md`，找到 **行动点 #0046** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/sim-config.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/sim-config.md` 中 **行动点 #0046** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0047"
    title: "行动点 #0047"
    status: "completed"
    dependencies: []
    action_point_id: "0047"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/sim-loop.md"
    target_src: "src/game/behavior/sim-loop.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/sim-loop.md"
        - "src/game/behavior/sim-loop.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0047**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/sim-loop.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/sim-loop.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/sim-loop.md`，找到 **行动点 #0047** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/sim-loop.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/sim-loop.md` 中 **行动点 #0047** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0048"
    title: "行动点 #0048"
    status: "completed"
    dependencies: []
    action_point_id: "0048"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/sim-loop.md"
    target_src: "src/game/behavior/sim-loop.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/sim-loop.md"
        - "src/game/behavior/sim-loop.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0048**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/sim-loop.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/sim-loop.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/sim-loop.md`，找到 **行动点 #0048** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/sim-loop.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/sim-loop.md` 中 **行动点 #0048** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0049"
    title: "行动点 #0049"
    status: "completed"
    dependencies: []
    action_point_id: "0049"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/sim-loop.md"
    target_src: "src/game/behavior/sim-loop.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/sim-loop.md"
        - "src/game/behavior/sim-loop.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0049**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/sim-loop.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/sim-loop.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/sim-loop.md`，找到 **行动点 #0049** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/sim-loop.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/sim-loop.md` 中 **行动点 #0049** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0050"
    title: "行动点 #0050"
    status: "completed"
    dependencies: []
    action_point_id: "0050"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/sim-loop.md"
    target_src: "src/game/behavior/sim-loop.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/sim-loop.md"
        - "src/game/behavior/sim-loop.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0050**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/sim-loop.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/sim-loop.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/sim-loop.md`，找到 **行动点 #0050** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/sim-loop.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/sim-loop.md` 中 **行动点 #0050** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0051"
    title: "行动点 #0051"
    status: "completed"
    dependencies: []
    action_point_id: "0051"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/wander-planning.md"
    target_src: "src/game/behavior/wander-planning.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/wander-planning.md"
        - "src/game/behavior/wander-planning.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0051**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/wander-planning.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/wander-planning.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/wander-planning.md`，找到 **行动点 #0051** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/wander-planning.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/wander-planning.md` 中 **行动点 #0051** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0052"
    title: "行动点 #0052"
    status: "completed"
    dependencies: []
    action_point_id: "0052"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/wander-planning.md"
    target_src: "src/game/behavior/wander-planning.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/wander-planning.md"
        - "src/game/behavior/wander-planning.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0052**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/wander-planning.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/wander-planning.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/wander-planning.md`，找到 **行动点 #0052** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/wander-planning.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/wander-planning.md` 中 **行动点 #0052** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0053"
    title: "行动点 #0053"
    status: "completed"
    dependencies: []
    action_point_id: "0053"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/wander-planning.md"
    target_src: "src/game/behavior/wander-planning.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/wander-planning.md"
        - "src/game/behavior/wander-planning.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0053**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/wander-planning.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/wander-planning.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/wander-planning.md`，找到 **行动点 #0053** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/wander-planning.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/wander-planning.md` 中 **行动点 #0053** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0054"
    title: "行动点 #0054"
    status: "completed"
    dependencies: []
    action_point_id: "0054"
    audit_report: "working-plan/skill-audit-code-check/src/game/behavior/wander-planning.md"
    target_src: "src/game/behavior/wander-planning.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/behavior/wander-planning.md"
        - "src/game/behavior/wander-planning.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0054**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/behavior/wander-planning.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/behavior/wander-planning.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/behavior/wander-planning.md`，找到 **行动点 #0054** 的完整表述与依据引用的 YAML。
      3. Read `src/game/behavior/wander-planning.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/behavior/wander-planning.md` 中 **行动点 #0054** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0055"
    title: "行动点 #0055"
    status: "completed"
    dependencies: []
    action_point_id: "0055"
    audit_report: "working-plan/skill-audit-code-check/src/game/building/blueprint-manager.md"
    target_src: "src/game/building/blueprint-manager.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/building/blueprint-manager.md"
        - "src/game/building/blueprint-manager.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0055**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/building/blueprint-manager.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/building/blueprint-manager.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/building/blueprint-manager.md`，找到 **行动点 #0055** 的完整表述与依据引用的 YAML。
      3. Read `src/game/building/blueprint-manager.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/building/blueprint-manager.md` 中 **行动点 #0055** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0056"
    title: "行动点 #0056"
    status: "completed"
    dependencies: []
    action_point_id: "0056"
    audit_report: "working-plan/skill-audit-code-check/src/game/building/blueprint-manager.md"
    target_src: "src/game/building/blueprint-manager.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/building/blueprint-manager.md"
        - "src/game/building/blueprint-manager.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0056**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/building/blueprint-manager.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/building/blueprint-manager.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/building/blueprint-manager.md`，找到 **行动点 #0056** 的完整表述与依据引用的 YAML。
      3. Read `src/game/building/blueprint-manager.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/building/blueprint-manager.md` 中 **行动点 #0056** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0057"
    title: "行动点 #0057"
    status: "completed"
    dependencies: []
    action_point_id: "0057"
    audit_report: "working-plan/skill-audit-code-check/src/game/building/blueprint-manager.md"
    target_src: "src/game/building/blueprint-manager.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/building/blueprint-manager.md"
        - "src/game/building/blueprint-manager.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0057**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/building/blueprint-manager.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/building/blueprint-manager.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/building/blueprint-manager.md`，找到 **行动点 #0057** 的完整表述与依据引用的 YAML。
      3. Read `src/game/building/blueprint-manager.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/building/blueprint-manager.md` 中 **行动点 #0057** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0058"
    title: "行动点 #0058"
    status: "completed"
    dependencies: []
    action_point_id: "0058"
    audit_report: "working-plan/skill-audit-code-check/src/game/building/blueprint-placement.md"
    target_src: "src/game/building/blueprint-placement.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/building/blueprint-placement.md"
        - "src/game/building/blueprint-placement.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0058**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/building/blueprint-placement.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/building/blueprint-placement.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/building/blueprint-placement.md`，找到 **行动点 #0058** 的完整表述与依据引用的 YAML。
      3. Read `src/game/building/blueprint-placement.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/building/blueprint-placement.md` 中 **行动点 #0058** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0059"
    title: "行动点 #0059"
    status: "completed"
    dependencies: []
    action_point_id: "0059"
    audit_report: "working-plan/skill-audit-code-check/src/game/building/blueprint-placement.md"
    target_src: "src/game/building/blueprint-placement.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/building/blueprint-placement.md"
        - "src/game/building/blueprint-placement.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0059**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/building/blueprint-placement.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/building/blueprint-placement.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/building/blueprint-placement.md`，找到 **行动点 #0059** 的完整表述与依据引用的 YAML。
      3. Read `src/game/building/blueprint-placement.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/building/blueprint-placement.md` 中 **行动点 #0059** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0060"
    title: "行动点 #0060"
    status: "completed"
    dependencies: []
    action_point_id: "0060"
    audit_report: "working-plan/skill-audit-code-check/src/game/building/blueprint-placement.md"
    target_src: "src/game/building/blueprint-placement.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/building/blueprint-placement.md"
        - "src/game/building/blueprint-placement.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0060**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/building/blueprint-placement.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/building/blueprint-placement.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/building/blueprint-placement.md`，找到 **行动点 #0060** 的完整表述与依据引用的 YAML。
      3. Read `src/game/building/blueprint-placement.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/building/blueprint-placement.md` 中 **行动点 #0060** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0061"
    title: "行动点 #0061"
    status: "completed"
    dependencies: []
    action_point_id: "0061"
    audit_report: "working-plan/skill-audit-code-check/src/game/building/building-spec-catalog.md"
    target_src: "src/game/building/building-spec-catalog.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/building/building-spec-catalog.md"
        - "src/game/building/building-spec-catalog.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0061**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/building/building-spec-catalog.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/building/building-spec-catalog.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/building/building-spec-catalog.md`，找到 **行动点 #0061** 的完整表述与依据引用的 YAML。
      3. Read `src/game/building/building-spec-catalog.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/building/building-spec-catalog.md` 中 **行动点 #0061** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0062"
    title: "行动点 #0062"
    status: "completed"
    dependencies: []
    action_point_id: "0062"
    audit_report: "working-plan/skill-audit-code-check/src/game/building/building-spec-catalog.md"
    target_src: "src/game/building/building-spec-catalog.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/building/building-spec-catalog.md"
        - "src/game/building/building-spec-catalog.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0062**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/building/building-spec-catalog.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/building/building-spec-catalog.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/building/building-spec-catalog.md`，找到 **行动点 #0062** 的完整表述与依据引用的 YAML。
      3. Read `src/game/building/building-spec-catalog.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/building/building-spec-catalog.md` 中 **行动点 #0062** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0063"
    title: "行动点 #0063"
    status: "completed"
    dependencies: []
    action_point_id: "0063"
    audit_report: "working-plan/skill-audit-code-check/src/game/building/building-spec-catalog.md"
    target_src: "src/game/building/building-spec-catalog.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/building/building-spec-catalog.md"
        - "src/game/building/building-spec-catalog.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0063**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/building/building-spec-catalog.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/building/building-spec-catalog.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/building/building-spec-catalog.md`，找到 **行动点 #0063** 的完整表述与依据引用的 YAML。
      3. Read `src/game/building/building-spec-catalog.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/building/building-spec-catalog.md` 中 **行动点 #0063** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0064"
    title: "行动点 #0064"
    status: "completed"
    dependencies: []
    action_point_id: "0064"
    audit_report: "working-plan/skill-audit-code-check/src/game/building/building-spec-catalog.md"
    target_src: "src/game/building/building-spec-catalog.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/building/building-spec-catalog.md"
        - "src/game/building/building-spec-catalog.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0064**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/building/building-spec-catalog.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/building/building-spec-catalog.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/building/building-spec-catalog.md`，找到 **行动点 #0064** 的完整表述与依据引用的 YAML。
      3. Read `src/game/building/building-spec-catalog.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/building/building-spec-catalog.md` 中 **行动点 #0064** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0065"
    title: "行动点 #0065"
    status: "completed"
    dependencies: []
    action_point_id: "0065"
    audit_report: "working-plan/skill-audit-code-check/src/game/building/building-spec-catalog.md"
    target_src: "src/game/building/building-spec-catalog.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/building/building-spec-catalog.md"
        - "src/game/building/building-spec-catalog.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0065**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/building/building-spec-catalog.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/building/building-spec-catalog.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/building/building-spec-catalog.md`，找到 **行动点 #0065** 的完整表述与依据引用的 YAML。
      3. Read `src/game/building/building-spec-catalog.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/building/building-spec-catalog.md` 中 **行动点 #0065** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0066"
    title: "行动点 #0066"
    status: "completed"
    dependencies: []
    action_point_id: "0066"
    audit_report: "working-plan/skill-audit-code-check/src/game/entity/entity-projection.md"
    target_src: "src/game/entity/entity-projection.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/entity/entity-projection.md"
        - "src/game/entity/entity-projection.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0066**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/entity/entity-projection.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/entity/entity-projection.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/entity/entity-projection.md`，找到 **行动点 #0066** 的完整表述与依据引用的 YAML。
      3. Read `src/game/entity/entity-projection.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/entity/entity-projection.md` 中 **行动点 #0066** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0067"
    title: "行动点 #0067"
    status: "completed"
    dependencies: []
    action_point_id: "0067"
    audit_report: "working-plan/skill-audit-code-check/src/game/entity/entity-projection.md"
    target_src: "src/game/entity/entity-projection.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/entity/entity-projection.md"
        - "src/game/entity/entity-projection.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0067**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/entity/entity-projection.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/entity/entity-projection.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/entity/entity-projection.md`，找到 **行动点 #0067** 的完整表述与依据引用的 YAML。
      3. Read `src/game/entity/entity-projection.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/entity/entity-projection.md` 中 **行动点 #0067** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0068"
    title: "行动点 #0068"
    status: "completed"
    dependencies: []
    action_point_id: "0068"
    audit_report: "working-plan/skill-audit-code-check/src/game/entity/entity-projection.md"
    target_src: "src/game/entity/entity-projection.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/entity/entity-projection.md"
        - "src/game/entity/entity-projection.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0068**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/entity/entity-projection.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/entity/entity-projection.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/entity/entity-projection.md`，找到 **行动点 #0068** 的完整表述与依据引用的 YAML。
      3. Read `src/game/entity/entity-projection.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/entity/entity-projection.md` 中 **行动点 #0068** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0069"
    title: "行动点 #0069"
    status: "completed"
    dependencies: []
    action_point_id: "0069"
    audit_report: "working-plan/skill-audit-code-check/src/game/entity/entity-projection.md"
    target_src: "src/game/entity/entity-projection.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/entity/entity-projection.md"
        - "src/game/entity/entity-projection.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0069**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/entity/entity-projection.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/entity/entity-projection.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/entity/entity-projection.md`，找到 **行动点 #0069** 的完整表述与依据引用的 YAML。
      3. Read `src/game/entity/entity-projection.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/entity/entity-projection.md` 中 **行动点 #0069** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0070"
    title: "行动点 #0070"
    status: "completed"
    dependencies: []
    action_point_id: "0070"
    audit_report: "working-plan/skill-audit-code-check/src/game/entity/entity-registry.md"
    target_src: "src/game/entity/entity-registry.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/entity/entity-registry.md"
        - "src/game/entity/entity-registry.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0070**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/entity/entity-registry.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/entity/entity-registry.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/entity/entity-registry.md`，找到 **行动点 #0070** 的完整表述与依据引用的 YAML。
      3. Read `src/game/entity/entity-registry.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/entity/entity-registry.md` 中 **行动点 #0070** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0071"
    title: "行动点 #0071"
    status: "completed"
    dependencies: []
    action_point_id: "0071"
    audit_report: "working-plan/skill-audit-code-check/src/game/entity/entity-registry.md"
    target_src: "src/game/entity/entity-registry.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/entity/entity-registry.md"
        - "src/game/entity/entity-registry.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0071**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/entity/entity-registry.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/entity/entity-registry.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/entity/entity-registry.md`，找到 **行动点 #0071** 的完整表述与依据引用的 YAML。
      3. Read `src/game/entity/entity-registry.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/entity/entity-registry.md` 中 **行动点 #0071** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0072"
    title: "行动点 #0072"
    status: "completed"
    dependencies: []
    action_point_id: "0072"
    audit_report: "working-plan/skill-audit-code-check/src/game/entity/entity-registry.md"
    target_src: "src/game/entity/entity-registry.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/entity/entity-registry.md"
        - "src/game/entity/entity-registry.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0072**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/entity/entity-registry.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/entity/entity-registry.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/entity/entity-registry.md`，找到 **行动点 #0072** 的完整表述与依据引用的 YAML。
      3. Read `src/game/entity/entity-registry.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/entity/entity-registry.md` 中 **行动点 #0072** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0073"
    title: "行动点 #0073"
    status: "completed"
    dependencies: []
    action_point_id: "0073"
    audit_report: "working-plan/skill-audit-code-check/src/game/entity/entity-registry.md"
    target_src: "src/game/entity/entity-registry.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/entity/entity-registry.md"
        - "src/game/entity/entity-registry.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0073**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/entity/entity-registry.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/entity/entity-registry.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/entity/entity-registry.md`，找到 **行动点 #0073** 的完整表述与依据引用的 YAML。
      3. Read `src/game/entity/entity-registry.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/entity/entity-registry.md` 中 **行动点 #0073** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0074"
    title: "行动点 #0074"
    status: "completed"
    dependencies: []
    action_point_id: "0074"
    audit_report: "working-plan/skill-audit-code-check/src/game/entity/entity-types.md"
    target_src: "src/game/entity/entity-types.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/entity/entity-types.md"
        - "src/game/entity/entity-types.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0074**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/entity/entity-types.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/entity/entity-types.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/entity/entity-types.md`，找到 **行动点 #0074** 的完整表述与依据引用的 YAML。
      3. Read `src/game/entity/entity-types.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/entity/entity-types.md` 中 **行动点 #0074** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0075"
    title: "行动点 #0075"
    status: "completed"
    dependencies: []
    action_point_id: "0075"
    audit_report: "working-plan/skill-audit-code-check/src/game/entity/entity-types.md"
    target_src: "src/game/entity/entity-types.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/entity/entity-types.md"
        - "src/game/entity/entity-types.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0075**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/entity/entity-types.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/entity/entity-types.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/entity/entity-types.md`，找到 **行动点 #0075** 的完整表述与依据引用的 YAML。
      3. Read `src/game/entity/entity-types.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/entity/entity-types.md` 中 **行动点 #0075** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0076"
    title: "行动点 #0076"
    status: "completed"
    dependencies: []
    action_point_id: "0076"
    audit_report: "working-plan/skill-audit-code-check/src/game/entity/entity-types.md"
    target_src: "src/game/entity/entity-types.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/entity/entity-types.md"
        - "src/game/entity/entity-types.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0076**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/entity/entity-types.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/entity/entity-types.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/entity/entity-types.md`，找到 **行动点 #0076** 的完整表述与依据引用的 YAML。
      3. Read `src/game/entity/entity-types.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/entity/entity-types.md` 中 **行动点 #0076** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0077"
    title: "行动点 #0077"
    status: "completed"
    dependencies: []
    action_point_id: "0077"
    audit_report: "working-plan/skill-audit-code-check/src/game/entity/entity-types.md"
    target_src: "src/game/entity/entity-types.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/entity/entity-types.md"
        - "src/game/entity/entity-types.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0077**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/entity/entity-types.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/entity/entity-types.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/entity/entity-types.md`，找到 **行动点 #0077** 的完整表述与依据引用的 YAML。
      3. Read `src/game/entity/entity-types.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/entity/entity-types.md` 中 **行动点 #0077** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0078"
    title: "行动点 #0078"
    status: "completed"
    dependencies: []
    action_point_id: "0078"
    audit_report: "working-plan/skill-audit-code-check/src/game/entity/lifecycle-rules.md"
    target_src: "src/game/entity/lifecycle-rules.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/entity/lifecycle-rules.md"
        - "src/game/entity/lifecycle-rules.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0078**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/entity/lifecycle-rules.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/entity/lifecycle-rules.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/entity/lifecycle-rules.md`，找到 **行动点 #0078** 的完整表述与依据引用的 YAML。
      3. Read `src/game/entity/lifecycle-rules.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/entity/lifecycle-rules.md` 中 **行动点 #0078** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0079"
    title: "行动点 #0079"
    status: "completed"
    dependencies: []
    action_point_id: "0079"
    audit_report: "working-plan/skill-audit-code-check/src/game/entity/lifecycle-rules.md"
    target_src: "src/game/entity/lifecycle-rules.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/entity/lifecycle-rules.md"
        - "src/game/entity/lifecycle-rules.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0079**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/entity/lifecycle-rules.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/entity/lifecycle-rules.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/entity/lifecycle-rules.md`，找到 **行动点 #0079** 的完整表述与依据引用的 YAML。
      3. Read `src/game/entity/lifecycle-rules.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/entity/lifecycle-rules.md` 中 **行动点 #0079** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0080"
    title: "行动点 #0080"
    status: "completed"
    dependencies: []
    action_point_id: "0080"
    audit_report: "working-plan/skill-audit-code-check/src/game/entity/lifecycle-rules.md"
    target_src: "src/game/entity/lifecycle-rules.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/entity/lifecycle-rules.md"
        - "src/game/entity/lifecycle-rules.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0080**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/entity/lifecycle-rules.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/entity/lifecycle-rules.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/entity/lifecycle-rules.md`，找到 **行动点 #0080** 的完整表述与依据引用的 YAML。
      3. Read `src/game/entity/lifecycle-rules.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/entity/lifecycle-rules.md` 中 **行动点 #0080** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0081"
    title: "行动点 #0081"
    status: "completed"
    dependencies: []
    action_point_id: "0081"
    audit_report: "working-plan/skill-audit-code-check/src/game/entity/lifecycle-rules.md"
    target_src: "src/game/entity/lifecycle-rules.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/entity/lifecycle-rules.md"
        - "src/game/entity/lifecycle-rules.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0081**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/entity/lifecycle-rules.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/entity/lifecycle-rules.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/entity/lifecycle-rules.md`，找到 **行动点 #0081** 的完整表述与依据引用的 YAML。
      3. Read `src/game/entity/lifecycle-rules.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/entity/lifecycle-rules.md` 中 **行动点 #0081** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0082"
    title: "行动点 #0082"
    status: "completed"
    dependencies: []
    action_point_id: "0082"
    audit_report: "working-plan/skill-audit-code-check/src/game/entity/lifecycle-rules.md"
    target_src: "src/game/entity/lifecycle-rules.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/entity/lifecycle-rules.md"
        - "src/game/entity/lifecycle-rules.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0082**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/entity/lifecycle-rules.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/entity/lifecycle-rules.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/entity/lifecycle-rules.md`，找到 **行动点 #0082** 的完整表述与依据引用的 YAML。
      3. Read `src/game/entity/lifecycle-rules.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/entity/lifecycle-rules.md` 中 **行动点 #0082** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0083"
    title: "行动点 #0083"
    status: "completed"
    dependencies: []
    action_point_id: "0083"
    audit_report: "working-plan/skill-audit-code-check/src/game/entity/relationship-rules.md"
    target_src: "src/game/entity/relationship-rules.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/entity/relationship-rules.md"
        - "src/game/entity/relationship-rules.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0083**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/entity/relationship-rules.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/entity/relationship-rules.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/entity/relationship-rules.md`，找到 **行动点 #0083** 的完整表述与依据引用的 YAML。
      3. Read `src/game/entity/relationship-rules.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/entity/relationship-rules.md` 中 **行动点 #0083** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0084"
    title: "行动点 #0084"
    status: "completed"
    dependencies: []
    action_point_id: "0084"
    audit_report: "working-plan/skill-audit-code-check/src/game/entity/relationship-rules.md"
    target_src: "src/game/entity/relationship-rules.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/entity/relationship-rules.md"
        - "src/game/entity/relationship-rules.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0084**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/entity/relationship-rules.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/entity/relationship-rules.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/entity/relationship-rules.md`，找到 **行动点 #0084** 的完整表述与依据引用的 YAML。
      3. Read `src/game/entity/relationship-rules.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/entity/relationship-rules.md` 中 **行动点 #0084** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0085"
    title: "行动点 #0085"
    status: "completed"
    dependencies: []
    action_point_id: "0085"
    audit_report: "working-plan/skill-audit-code-check/src/game/entity/relationship-rules.md"
    target_src: "src/game/entity/relationship-rules.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/entity/relationship-rules.md"
        - "src/game/entity/relationship-rules.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0085**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/entity/relationship-rules.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/entity/relationship-rules.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/entity/relationship-rules.md`，找到 **行动点 #0085** 的完整表述与依据引用的 YAML。
      3. Read `src/game/entity/relationship-rules.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/entity/relationship-rules.md` 中 **行动点 #0085** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0086"
    title: "行动点 #0086"
    status: "completed"
    dependencies: []
    action_point_id: "0086"
    audit_report: "working-plan/skill-audit-code-check/src/game/entity/relationship-rules.md"
    target_src: "src/game/entity/relationship-rules.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/entity/relationship-rules.md"
        - "src/game/entity/relationship-rules.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0086**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/entity/relationship-rules.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/entity/relationship-rules.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/entity/relationship-rules.md`，找到 **行动点 #0086** 的完整表述与依据引用的 YAML。
      3. Read `src/game/entity/relationship-rules.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/entity/relationship-rules.md` 中 **行动点 #0086** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0087"
    title: "行动点 #0087"
    status: "completed"
    dependencies: []
    action_point_id: "0087"
    audit_report: "working-plan/skill-audit-code-check/src/game/flows/build-flow.md"
    target_src: "src/game/flows/build-flow.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/flows/build-flow.md"
        - "src/game/flows/build-flow.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0087**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/flows/build-flow.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/flows/build-flow.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/flows/build-flow.md`，找到 **行动点 #0087** 的完整表述与依据引用的 YAML。
      3. Read `src/game/flows/build-flow.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/flows/build-flow.md` 中 **行动点 #0087** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0088"
    title: "行动点 #0088"
    status: "completed"
    dependencies: []
    action_point_id: "0088"
    audit_report: "working-plan/skill-audit-code-check/src/game/flows/build-flow.md"
    target_src: "src/game/flows/build-flow.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/flows/build-flow.md"
        - "src/game/flows/build-flow.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0088**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/flows/build-flow.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/flows/build-flow.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/flows/build-flow.md`，找到 **行动点 #0088** 的完整表述与依据引用的 YAML。
      3. Read `src/game/flows/build-flow.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/flows/build-flow.md` 中 **行动点 #0088** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0089"
    title: "行动点 #0089"
    status: "completed"
    dependencies: []
    action_point_id: "0089"
    audit_report: "working-plan/skill-audit-code-check/src/game/flows/build-flow.md"
    target_src: "src/game/flows/build-flow.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/flows/build-flow.md"
        - "src/game/flows/build-flow.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0089**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/flows/build-flow.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/flows/build-flow.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/flows/build-flow.md`，找到 **行动点 #0089** 的完整表述与依据引用的 YAML。
      3. Read `src/game/flows/build-flow.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/flows/build-flow.md` 中 **行动点 #0089** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0090"
    title: "行动点 #0090"
    status: "completed"
    dependencies: []
    action_point_id: "0090"
    audit_report: "working-plan/skill-audit-code-check/src/game/flows/build-flow.md"
    target_src: "src/game/flows/build-flow.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/flows/build-flow.md"
        - "src/game/flows/build-flow.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0090**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/flows/build-flow.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/flows/build-flow.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/flows/build-flow.md`，找到 **行动点 #0090** 的完整表述与依据引用的 YAML。
      3. Read `src/game/flows/build-flow.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/flows/build-flow.md` 中 **行动点 #0090** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0091"
    title: "行动点 #0091"
    status: "completed"
    dependencies: []
    action_point_id: "0091"
    audit_report: "working-plan/skill-audit-code-check/src/game/flows/chop-flow.md"
    target_src: "src/game/flows/chop-flow.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/flows/chop-flow.md"
        - "src/game/flows/chop-flow.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0091**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/flows/chop-flow.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/flows/chop-flow.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/flows/chop-flow.md`，找到 **行动点 #0091** 的完整表述与依据引用的 YAML。
      3. Read `src/game/flows/chop-flow.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/flows/chop-flow.md` 中 **行动点 #0091** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0092"
    title: "行动点 #0092"
    status: "completed"
    dependencies: []
    action_point_id: "0092"
    audit_report: "working-plan/skill-audit-code-check/src/game/flows/chop-flow.md"
    target_src: "src/game/flows/chop-flow.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/flows/chop-flow.md"
        - "src/game/flows/chop-flow.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0092**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/flows/chop-flow.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/flows/chop-flow.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/flows/chop-flow.md`，找到 **行动点 #0092** 的完整表述与依据引用的 YAML。
      3. Read `src/game/flows/chop-flow.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/flows/chop-flow.md` 中 **行动点 #0092** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0093"
    title: "行动点 #0093"
    status: "completed"
    dependencies: []
    action_point_id: "0093"
    audit_report: "working-plan/skill-audit-code-check/src/game/flows/chop-flow.md"
    target_src: "src/game/flows/chop-flow.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/flows/chop-flow.md"
        - "src/game/flows/chop-flow.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0093**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/flows/chop-flow.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/flows/chop-flow.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/flows/chop-flow.md`，找到 **行动点 #0093** 的完整表述与依据引用的 YAML。
      3. Read `src/game/flows/chop-flow.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/flows/chop-flow.md` 中 **行动点 #0093** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0094"
    title: "行动点 #0094"
    status: "completed"
    dependencies: []
    action_point_id: "0094"
    audit_report: "working-plan/skill-audit-code-check/src/game/flows/chop-flow.md"
    target_src: "src/game/flows/chop-flow.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/flows/chop-flow.md"
        - "src/game/flows/chop-flow.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0094**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/flows/chop-flow.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/flows/chop-flow.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/flows/chop-flow.md`，找到 **行动点 #0094** 的完整表述与依据引用的 YAML。
      3. Read `src/game/flows/chop-flow.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/flows/chop-flow.md` 中 **行动点 #0094** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0095"
    title: "行动点 #0095"
    status: "completed"
    dependencies: []
    action_point_id: "0095"
    audit_report: "working-plan/skill-audit-code-check/src/game/flows/night-rest-flow.md"
    target_src: "src/game/flows/night-rest-flow.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/flows/night-rest-flow.md"
        - "src/game/flows/night-rest-flow.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0095**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/flows/night-rest-flow.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/flows/night-rest-flow.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/flows/night-rest-flow.md`，找到 **行动点 #0095** 的完整表述与依据引用的 YAML。
      3. Read `src/game/flows/night-rest-flow.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/flows/night-rest-flow.md` 中 **行动点 #0095** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0096"
    title: "行动点 #0096"
    status: "completed"
    dependencies: []
    action_point_id: "0096"
    audit_report: "working-plan/skill-audit-code-check/src/game/flows/night-rest-flow.md"
    target_src: "src/game/flows/night-rest-flow.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/flows/night-rest-flow.md"
        - "src/game/flows/night-rest-flow.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0096**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/flows/night-rest-flow.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/flows/night-rest-flow.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/flows/night-rest-flow.md`，找到 **行动点 #0096** 的完整表述与依据引用的 YAML。
      3. Read `src/game/flows/night-rest-flow.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/flows/night-rest-flow.md` 中 **行动点 #0096** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0097"
    title: "行动点 #0097"
    status: "completed"
    dependencies: []
    action_point_id: "0097"
    audit_report: "working-plan/skill-audit-code-check/src/game/flows/night-rest-flow.md"
    target_src: "src/game/flows/night-rest-flow.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/flows/night-rest-flow.md"
        - "src/game/flows/night-rest-flow.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0097**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/flows/night-rest-flow.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/flows/night-rest-flow.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/flows/night-rest-flow.md`，找到 **行动点 #0097** 的完整表述与依据引用的 YAML。
      3. Read `src/game/flows/night-rest-flow.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/flows/night-rest-flow.md` 中 **行动点 #0097** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0098"
    title: "行动点 #0098"
    status: "completed"
    dependencies: []
    action_point_id: "0098"
    audit_report: "working-plan/skill-audit-code-check/src/game/game-orchestrator.md"
    target_src: "src/game/game-orchestrator.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/game-orchestrator.md"
        - "src/game/game-orchestrator.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0098**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/game-orchestrator.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/game-orchestrator.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/game-orchestrator.md`，找到 **行动点 #0098** 的完整表述与依据引用的 YAML。
      3. Read `src/game/game-orchestrator.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/game-orchestrator.md` 中 **行动点 #0098** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0099"
    title: "行动点 #0099"
    status: "completed"
    dependencies: []
    action_point_id: "0099"
    audit_report: "working-plan/skill-audit-code-check/src/game/game-orchestrator.md"
    target_src: "src/game/game-orchestrator.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/game-orchestrator.md"
        - "src/game/game-orchestrator.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0099**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/game-orchestrator.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/game-orchestrator.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/game-orchestrator.md`，找到 **行动点 #0099** 的完整表述与依据引用的 YAML。
      3. Read `src/game/game-orchestrator.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/game-orchestrator.md` 中 **行动点 #0099** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0100"
    title: "行动点 #0100"
    status: "completed"
    dependencies: []
    action_point_id: "0100"
    audit_report: "working-plan/skill-audit-code-check/src/game/game-orchestrator.md"
    target_src: "src/game/game-orchestrator.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/game-orchestrator.md"
        - "src/game/game-orchestrator.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0100**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/game-orchestrator.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/game-orchestrator.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/game-orchestrator.md`，找到 **行动点 #0100** 的完整表述与依据引用的 YAML。
      3. Read `src/game/game-orchestrator.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/game-orchestrator.md` 中 **行动点 #0100** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0101"
    title: "行动点 #0101"
    status: "completed"
    dependencies: []
    action_point_id: "0101"
    audit_report: "working-plan/skill-audit-code-check/src/game/interaction/domain-command-types.md"
    target_src: "src/game/interaction/domain-command-types.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/interaction/domain-command-types.md"
        - "src/game/interaction/domain-command-types.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0101**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/interaction/domain-command-types.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/interaction/domain-command-types.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/interaction/domain-command-types.md`，找到 **行动点 #0101** 的完整表述与依据引用的 YAML。
      3. Read `src/game/interaction/domain-command-types.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/interaction/domain-command-types.md` 中 **行动点 #0101** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0102"
    title: "行动点 #0102"
    status: "completed"
    dependencies: []
    action_point_id: "0102"
    audit_report: "working-plan/skill-audit-code-check/src/game/interaction/domain-command-types.md"
    target_src: "src/game/interaction/domain-command-types.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/interaction/domain-command-types.md"
        - "src/game/interaction/domain-command-types.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0102**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/interaction/domain-command-types.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/interaction/domain-command-types.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/interaction/domain-command-types.md`，找到 **行动点 #0102** 的完整表述与依据引用的 YAML。
      3. Read `src/game/interaction/domain-command-types.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/interaction/domain-command-types.md` 中 **行动点 #0102** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0103"
    title: "行动点 #0103"
    status: "completed"
    dependencies: []
    action_point_id: "0103"
    audit_report: "working-plan/skill-audit-code-check/src/game/interaction/domain-command-types.md"
    target_src: "src/game/interaction/domain-command-types.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/interaction/domain-command-types.md"
        - "src/game/interaction/domain-command-types.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0103**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/interaction/domain-command-types.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/interaction/domain-command-types.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/interaction/domain-command-types.md`，找到 **行动点 #0103** 的完整表述与依据引用的 YAML。
      3. Read `src/game/interaction/domain-command-types.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/interaction/domain-command-types.md` 中 **行动点 #0103** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0104"
    title: "行动点 #0104"
    status: "completed"
    dependencies: []
    action_point_id: "0104"
    audit_report: "working-plan/skill-audit-code-check/src/game/interaction/floor-selection.md"
    target_src: "src/game/interaction/floor-selection.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/interaction/floor-selection.md"
        - "src/game/interaction/floor-selection.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0104**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/interaction/floor-selection.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/interaction/floor-selection.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/interaction/floor-selection.md`，找到 **行动点 #0104** 的完整表述与依据引用的 YAML。
      3. Read `src/game/interaction/floor-selection.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/interaction/floor-selection.md` 中 **行动点 #0104** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0105"
    title: "行动点 #0105"
    status: "completed"
    dependencies: []
    action_point_id: "0105"
    audit_report: "working-plan/skill-audit-code-check/src/game/interaction/floor-selection.md"
    target_src: "src/game/interaction/floor-selection.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/interaction/floor-selection.md"
        - "src/game/interaction/floor-selection.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0105**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/interaction/floor-selection.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/interaction/floor-selection.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/interaction/floor-selection.md`，找到 **行动点 #0105** 的完整表述与依据引用的 YAML。
      3. Read `src/game/interaction/floor-selection.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/interaction/floor-selection.md` 中 **行动点 #0105** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0106"
    title: "行动点 #0106"
    status: "completed"
    dependencies: []
    action_point_id: "0106"
    audit_report: "working-plan/skill-audit-code-check/src/game/interaction/index.md"
    target_src: "src/game/interaction/index.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/interaction/index.md"
        - "src/game/interaction/index.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0106**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/interaction/index.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/interaction/index.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/interaction/index.md`，找到 **行动点 #0106** 的完整表述与依据引用的 YAML。
      3. Read `src/game/interaction/index.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/interaction/index.md` 中 **行动点 #0106** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0107"
    title: "行动点 #0107"
    status: "completed"
    dependencies: []
    action_point_id: "0107"
    audit_report: "working-plan/skill-audit-code-check/src/game/interaction/index.md"
    target_src: "src/game/interaction/index.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/interaction/index.md"
        - "src/game/interaction/index.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0107**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/interaction/index.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/interaction/index.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/interaction/index.md`，找到 **行动点 #0107** 的完整表述与依据引用的 YAML。
      3. Read `src/game/interaction/index.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/interaction/index.md` 中 **行动点 #0107** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0108"
    title: "行动点 #0108"
    status: "completed"
    dependencies: []
    action_point_id: "0108"
    audit_report: "working-plan/skill-audit-code-check/src/game/interaction/mode-registry.md"
    target_src: "src/game/interaction/mode-registry.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/interaction/mode-registry.md"
        - "src/game/interaction/mode-registry.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0108**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/interaction/mode-registry.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/interaction/mode-registry.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/interaction/mode-registry.md`，找到 **行动点 #0108** 的完整表述与依据引用的 YAML。
      3. Read `src/game/interaction/mode-registry.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/interaction/mode-registry.md` 中 **行动点 #0108** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0109"
    title: "行动点 #0109"
    status: "completed"
    dependencies: []
    action_point_id: "0109"
    audit_report: "working-plan/skill-audit-code-check/src/game/interaction/mode-registry.md"
    target_src: "src/game/interaction/mode-registry.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/interaction/mode-registry.md"
        - "src/game/interaction/mode-registry.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0109**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/interaction/mode-registry.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/interaction/mode-registry.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/interaction/mode-registry.md`，找到 **行动点 #0109** 的完整表述与依据引用的 YAML。
      3. Read `src/game/interaction/mode-registry.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/interaction/mode-registry.md` 中 **行动点 #0109** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0110"
    title: "行动点 #0110"
    status: "completed"
    dependencies: []
    action_point_id: "0110"
    audit_report: "working-plan/skill-audit-code-check/src/game/interaction/mode-registry.md"
    target_src: "src/game/interaction/mode-registry.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/interaction/mode-registry.md"
        - "src/game/interaction/mode-registry.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0110**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/interaction/mode-registry.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/interaction/mode-registry.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/interaction/mode-registry.md`，找到 **行动点 #0110** 的完整表述与依据引用的 YAML。
      3. Read `src/game/interaction/mode-registry.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/interaction/mode-registry.md` 中 **行动点 #0110** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0111"
    title: "行动点 #0111"
    status: "completed"
    dependencies: []
    action_point_id: "0111"
    audit_report: "working-plan/skill-audit-code-check/src/game/interaction/session-manager.md"
    target_src: "src/game/interaction/session-manager.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/interaction/session-manager.md"
        - "src/game/interaction/session-manager.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0111**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/interaction/session-manager.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/interaction/session-manager.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/interaction/session-manager.md`，找到 **行动点 #0111** 的完整表述与依据引用的 YAML。
      3. Read `src/game/interaction/session-manager.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/interaction/session-manager.md` 中 **行动点 #0111** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0112"
    title: "行动点 #0112"
    status: "completed"
    dependencies: []
    action_point_id: "0112"
    audit_report: "working-plan/skill-audit-code-check/src/game/interaction/session-manager.md"
    target_src: "src/game/interaction/session-manager.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/interaction/session-manager.md"
        - "src/game/interaction/session-manager.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0112**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/interaction/session-manager.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/interaction/session-manager.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/interaction/session-manager.md`，找到 **行动点 #0112** 的完整表述与依据引用的 YAML。
      3. Read `src/game/interaction/session-manager.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/interaction/session-manager.md` 中 **行动点 #0112** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0113"
    title: "行动点 #0113"
    status: "completed"
    dependencies: []
    action_point_id: "0113"
    audit_report: "working-plan/skill-audit-code-check/src/game/interaction/session-manager.md"
    target_src: "src/game/interaction/session-manager.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/interaction/session-manager.md"
        - "src/game/interaction/session-manager.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0113**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/interaction/session-manager.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/interaction/session-manager.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/interaction/session-manager.md`，找到 **行动点 #0113** 的完整表述与依据引用的 YAML。
      3. Read `src/game/interaction/session-manager.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/interaction/session-manager.md` 中 **行动点 #0113** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0114"
    title: "行动点 #0114"
    status: "completed"
    dependencies: []
    action_point_id: "0114"
    audit_report: "working-plan/skill-audit-code-check/src/game/interaction/session-manager.md"
    target_src: "src/game/interaction/session-manager.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/interaction/session-manager.md"
        - "src/game/interaction/session-manager.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0114**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/interaction/session-manager.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/interaction/session-manager.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/interaction/session-manager.md`，找到 **行动点 #0114** 的完整表述与依据引用的 YAML。
      3. Read `src/game/interaction/session-manager.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/interaction/session-manager.md` 中 **行动点 #0114** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0115"
    title: "行动点 #0115"
    status: "completed"
    dependencies: []
    action_point_id: "0115"
    audit_report: "working-plan/skill-audit-code-check/src/game/map/index.md"
    target_src: "src/game/map/index.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/map/index.md"
        - "src/game/map/index.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0115**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/map/index.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/map/index.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/map/index.md`，找到 **行动点 #0115** 的完整表述与依据引用的 YAML。
      3. Read `src/game/map/index.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/map/index.md` 中 **行动点 #0115** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0116"
    title: "行动点 #0116"
    status: "completed"
    dependencies: []
    action_point_id: "0116"
    audit_report: "working-plan/skill-audit-code-check/src/game/map/index.md"
    target_src: "src/game/map/index.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/map/index.md"
        - "src/game/map/index.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0116**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/map/index.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/map/index.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/map/index.md`，找到 **行动点 #0116** 的完整表述与依据引用的 YAML。
      3. Read `src/game/map/index.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/map/index.md` 中 **行动点 #0116** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0117"
    title: "行动点 #0117"
    status: "completed"
    dependencies: []
    action_point_id: "0117"
    audit_report: "working-plan/skill-audit-code-check/src/game/map/occupancy-manager.md"
    target_src: "src/game/map/occupancy-manager.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/map/occupancy-manager.md"
        - "src/game/map/occupancy-manager.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0117**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/map/occupancy-manager.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/map/occupancy-manager.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/map/occupancy-manager.md`，找到 **行动点 #0117** 的完整表述与依据引用的 YAML。
      3. Read `src/game/map/occupancy-manager.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/map/occupancy-manager.md` 中 **行动点 #0117** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0118"
    title: "行动点 #0118"
    status: "completed"
    dependencies: []
    action_point_id: "0118"
    audit_report: "working-plan/skill-audit-code-check/src/game/map/occupancy-manager.md"
    target_src: "src/game/map/occupancy-manager.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/map/occupancy-manager.md"
        - "src/game/map/occupancy-manager.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0118**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/map/occupancy-manager.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/map/occupancy-manager.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/map/occupancy-manager.md`，找到 **行动点 #0118** 的完整表述与依据引用的 YAML。
      3. Read `src/game/map/occupancy-manager.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/map/occupancy-manager.md` 中 **行动点 #0118** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0119"
    title: "行动点 #0119"
    status: "completed"
    dependencies: []
    action_point_id: "0119"
    audit_report: "working-plan/skill-audit-code-check/src/game/map/occupancy-manager.md"
    target_src: "src/game/map/occupancy-manager.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/map/occupancy-manager.md"
        - "src/game/map/occupancy-manager.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0119**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/map/occupancy-manager.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/map/occupancy-manager.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/map/occupancy-manager.md`，找到 **行动点 #0119** 的完整表述与依据引用的 YAML。
      3. Read `src/game/map/occupancy-manager.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/map/occupancy-manager.md` 中 **行动点 #0119** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0120"
    title: "行动点 #0120"
    status: "completed"
    dependencies: []
    action_point_id: "0120"
    audit_report: "working-plan/skill-audit-code-check/src/game/map/storage-zones.md"
    target_src: "src/game/map/storage-zones.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/map/storage-zones.md"
        - "src/game/map/storage-zones.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0120**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/map/storage-zones.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/map/storage-zones.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/map/storage-zones.md`，找到 **行动点 #0120** 的完整表述与依据引用的 YAML。
      3. Read `src/game/map/storage-zones.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/map/storage-zones.md` 中 **行动点 #0120** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0121"
    title: "行动点 #0121"
    status: "completed"
    dependencies: []
    action_point_id: "0121"
    audit_report: "working-plan/skill-audit-code-check/src/game/map/storage-zones.md"
    target_src: "src/game/map/storage-zones.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/map/storage-zones.md"
        - "src/game/map/storage-zones.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0121**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/map/storage-zones.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/map/storage-zones.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/map/storage-zones.md`，找到 **行动点 #0121** 的完整表述与依据引用的 YAML。
      3. Read `src/game/map/storage-zones.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/map/storage-zones.md` 中 **行动点 #0121** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0122"
    title: "行动点 #0122"
    status: "completed"
    dependencies: []
    action_point_id: "0122"
    audit_report: "working-plan/skill-audit-code-check/src/game/map/storage-zones.md"
    target_src: "src/game/map/storage-zones.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/map/storage-zones.md"
        - "src/game/map/storage-zones.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0122**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/map/storage-zones.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/map/storage-zones.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/map/storage-zones.md`，找到 **行动点 #0122** 的完整表述与依据引用的 YAML。
      3. Read `src/game/map/storage-zones.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/map/storage-zones.md` 中 **行动点 #0122** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0123"
    title: "行动点 #0123"
    status: "completed"
    dependencies: []
    action_point_id: "0123"
    audit_report: "working-plan/skill-audit-code-check/src/game/map/storage-zones.md"
    target_src: "src/game/map/storage-zones.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/map/storage-zones.md"
        - "src/game/map/storage-zones.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0123**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/map/storage-zones.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/map/storage-zones.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/map/storage-zones.md`，找到 **行动点 #0123** 的完整表述与依据引用的 YAML。
      3. Read `src/game/map/storage-zones.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/map/storage-zones.md` 中 **行动点 #0123** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0124"
    title: "行动点 #0124"
    status: "completed"
    dependencies: []
    action_point_id: "0124"
    audit_report: "working-plan/skill-audit-code-check/src/game/map/world-grid.md"
    target_src: "src/game/map/world-grid.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/map/world-grid.md"
        - "src/game/map/world-grid.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0124**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/map/world-grid.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/map/world-grid.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/map/world-grid.md`，找到 **行动点 #0124** 的完整表述与依据引用的 YAML。
      3. Read `src/game/map/world-grid.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/map/world-grid.md` 中 **行动点 #0124** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0125"
    title: "行动点 #0125"
    status: "completed"
    dependencies: []
    action_point_id: "0125"
    audit_report: "working-plan/skill-audit-code-check/src/game/map/world-grid.md"
    target_src: "src/game/map/world-grid.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/map/world-grid.md"
        - "src/game/map/world-grid.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0125**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/map/world-grid.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/map/world-grid.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/map/world-grid.md`，找到 **行动点 #0125** 的完整表述与依据引用的 YAML。
      3. Read `src/game/map/world-grid.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/map/world-grid.md` 中 **行动点 #0125** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0126"
    title: "行动点 #0126"
    status: "completed"
    dependencies: []
    action_point_id: "0126"
    audit_report: "working-plan/skill-audit-code-check/src/game/map/world-grid.md"
    target_src: "src/game/map/world-grid.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/map/world-grid.md"
        - "src/game/map/world-grid.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0126**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/map/world-grid.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/map/world-grid.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/map/world-grid.md`，找到 **行动点 #0126** 的完整表述与依据引用的 YAML。
      3. Read `src/game/map/world-grid.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/map/world-grid.md` 中 **行动点 #0126** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0127"
    title: "行动点 #0127"
    status: "completed"
    dependencies: []
    action_point_id: "0127"
    audit_report: "working-plan/skill-audit-code-check/src/game/map/world-grid.md"
    target_src: "src/game/map/world-grid.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/map/world-grid.md"
        - "src/game/map/world-grid.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0127**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/map/world-grid.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/map/world-grid.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/map/world-grid.md`，找到 **行动点 #0127** 的完整表述与依据引用的 YAML。
      3. Read `src/game/map/world-grid.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/map/world-grid.md` 中 **行动点 #0127** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0128"
    title: "行动点 #0128"
    status: "completed"
    dependencies: []
    action_point_id: "0128"
    audit_report: "working-plan/skill-audit-code-check/src/game/map/world-grid.md"
    target_src: "src/game/map/world-grid.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/map/world-grid.md"
        - "src/game/map/world-grid.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0128**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/map/world-grid.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/map/world-grid.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/map/world-grid.md`，找到 **行动点 #0128** 的完整表述与依据引用的 YAML。
      3. Read `src/game/map/world-grid.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/map/world-grid.md` 中 **行动点 #0128** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0129"
    title: "行动点 #0129"
    status: "completed"
    dependencies: []
    action_point_id: "0129"
    audit_report: "working-plan/skill-audit-code-check/src/game/map/world-seed-entities.md"
    target_src: "src/game/map/world-seed-entities.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/map/world-seed-entities.md"
        - "src/game/map/world-seed-entities.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0129**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/map/world-seed-entities.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/map/world-seed-entities.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/map/world-seed-entities.md`，找到 **行动点 #0129** 的完整表述与依据引用的 YAML。
      3. Read `src/game/map/world-seed-entities.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/map/world-seed-entities.md` 中 **行动点 #0129** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0130"
    title: "行动点 #0130"
    status: "completed"
    dependencies: []
    action_point_id: "0130"
    audit_report: "working-plan/skill-audit-code-check/src/game/map/world-seed-entities.md"
    target_src: "src/game/map/world-seed-entities.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/map/world-seed-entities.md"
        - "src/game/map/world-seed-entities.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0130**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/map/world-seed-entities.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/map/world-seed-entities.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/map/world-seed-entities.md`，找到 **行动点 #0130** 的完整表述与依据引用的 YAML。
      3. Read `src/game/map/world-seed-entities.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/map/world-seed-entities.md` 中 **行动点 #0130** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0131"
    title: "行动点 #0131"
    status: "completed"
    dependencies: []
    action_point_id: "0131"
    audit_report: "working-plan/skill-audit-code-check/src/game/map/world-seed-entities.md"
    target_src: "src/game/map/world-seed-entities.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/map/world-seed-entities.md"
        - "src/game/map/world-seed-entities.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0131**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/map/world-seed-entities.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/map/world-seed-entities.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/map/world-seed-entities.md`，找到 **行动点 #0131** 的完整表述与依据引用的 YAML。
      3. Read `src/game/map/world-seed-entities.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/map/world-seed-entities.md` 中 **行动点 #0131** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0132"
    title: "行动点 #0132"
    status: "completed"
    dependencies: []
    action_point_id: "0132"
    audit_report: "working-plan/skill-audit-code-check/src/game/map/world-seed.md"
    target_src: "src/game/map/world-seed.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/map/world-seed.md"
        - "src/game/map/world-seed.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0132**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/map/world-seed.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/map/world-seed.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/map/world-seed.md`，找到 **行动点 #0132** 的完整表述与依据引用的 YAML。
      3. Read `src/game/map/world-seed.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/map/world-seed.md` 中 **行动点 #0132** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0133"
    title: "行动点 #0133"
    status: "completed"
    dependencies: []
    action_point_id: "0133"
    audit_report: "working-plan/skill-audit-code-check/src/game/map/world-seed.md"
    target_src: "src/game/map/world-seed.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/map/world-seed.md"
        - "src/game/map/world-seed.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0133**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/map/world-seed.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/map/world-seed.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/map/world-seed.md`，找到 **行动点 #0133** 的完整表述与依据引用的 YAML。
      3. Read `src/game/map/world-seed.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/map/world-seed.md` 中 **行动点 #0133** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0134"
    title: "行动点 #0134"
    status: "completed"
    dependencies: []
    action_point_id: "0134"
    audit_report: "working-plan/skill-audit-code-check/src/game/map/world-seed.md"
    target_src: "src/game/map/world-seed.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/map/world-seed.md"
        - "src/game/map/world-seed.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0134**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/map/world-seed.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/map/world-seed.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/map/world-seed.md`，找到 **行动点 #0134** 的完整表述与依据引用的 YAML。
      3. Read `src/game/map/world-seed.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/map/world-seed.md` 中 **行动点 #0134** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0135"
    title: "行动点 #0135"
    status: "completed"
    dependencies: []
    action_point_id: "0135"
    audit_report: "working-plan/skill-audit-code-check/src/game/map/zone-manager.md"
    target_src: "src/game/map/zone-manager.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/map/zone-manager.md"
        - "src/game/map/zone-manager.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0135**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/map/zone-manager.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/map/zone-manager.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/map/zone-manager.md`，找到 **行动点 #0135** 的完整表述与依据引用的 YAML。
      3. Read `src/game/map/zone-manager.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/map/zone-manager.md` 中 **行动点 #0135** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0136"
    title: "行动点 #0136"
    status: "completed"
    dependencies: []
    action_point_id: "0136"
    audit_report: "working-plan/skill-audit-code-check/src/game/map/zone-manager.md"
    target_src: "src/game/map/zone-manager.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/map/zone-manager.md"
        - "src/game/map/zone-manager.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0136**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/map/zone-manager.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/map/zone-manager.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/map/zone-manager.md`，找到 **行动点 #0136** 的完整表述与依据引用的 YAML。
      3. Read `src/game/map/zone-manager.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/map/zone-manager.md` 中 **行动点 #0136** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0137"
    title: "行动点 #0137"
    status: "completed"
    dependencies: []
    action_point_id: "0137"
    audit_report: "working-plan/skill-audit-code-check/src/game/map/zone-manager.md"
    target_src: "src/game/map/zone-manager.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/map/zone-manager.md"
        - "src/game/map/zone-manager.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0137**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/map/zone-manager.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/map/zone-manager.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/map/zone-manager.md`，找到 **行动点 #0137** 的完整表述与依据引用的 YAML。
      3. Read `src/game/map/zone-manager.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/map/zone-manager.md` 中 **行动点 #0137** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0138"
    title: "行动点 #0138"
    status: "completed"
    dependencies: []
    action_point_id: "0138"
    audit_report: "working-plan/skill-audit-code-check/src/game/need/need-evolution-engine.md"
    target_src: "src/game/need/need-evolution-engine.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/need/need-evolution-engine.md"
        - "src/game/need/need-evolution-engine.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0138**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/need/need-evolution-engine.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/need/need-evolution-engine.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/need/need-evolution-engine.md`，找到 **行动点 #0138** 的完整表述与依据引用的 YAML。
      3. Read `src/game/need/need-evolution-engine.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/need/need-evolution-engine.md` 中 **行动点 #0138** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0139"
    title: "行动点 #0139"
    status: "completed"
    dependencies: []
    action_point_id: "0139"
    audit_report: "working-plan/skill-audit-code-check/src/game/need/need-evolution-engine.md"
    target_src: "src/game/need/need-evolution-engine.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/need/need-evolution-engine.md"
        - "src/game/need/need-evolution-engine.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0139**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/need/need-evolution-engine.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/need/need-evolution-engine.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/need/need-evolution-engine.md`，找到 **行动点 #0139** 的完整表述与依据引用的 YAML。
      3. Read `src/game/need/need-evolution-engine.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/need/need-evolution-engine.md` 中 **行动点 #0139** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0140"
    title: "行动点 #0140"
    status: "completed"
    dependencies: []
    action_point_id: "0140"
    audit_report: "working-plan/skill-audit-code-check/src/game/need/need-evolution-engine.md"
    target_src: "src/game/need/need-evolution-engine.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/need/need-evolution-engine.md"
        - "src/game/need/need-evolution-engine.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0140**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/need/need-evolution-engine.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/need/need-evolution-engine.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/need/need-evolution-engine.md`，找到 **行动点 #0140** 的完整表述与依据引用的 YAML。
      3. Read `src/game/need/need-evolution-engine.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/need/need-evolution-engine.md` 中 **行动点 #0140** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0141"
    title: "行动点 #0141"
    status: "completed"
    dependencies: []
    action_point_id: "0141"
    audit_report: "working-plan/skill-audit-code-check/src/game/need/need-signals.md"
    target_src: "src/game/need/need-signals.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/need/need-signals.md"
        - "src/game/need/need-signals.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0141**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/need/need-signals.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/need/need-signals.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/need/need-signals.md`，找到 **行动点 #0141** 的完整表述与依据引用的 YAML。
      3. Read `src/game/need/need-signals.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/need/need-signals.md` 中 **行动点 #0141** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0142"
    title: "行动点 #0142"
    status: "completed"
    dependencies: []
    action_point_id: "0142"
    audit_report: "working-plan/skill-audit-code-check/src/game/need/need-signals.md"
    target_src: "src/game/need/need-signals.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/need/need-signals.md"
        - "src/game/need/need-signals.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0142**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/need/need-signals.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/need/need-signals.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/need/need-signals.md`，找到 **行动点 #0142** 的完整表述与依据引用的 YAML。
      3. Read `src/game/need/need-signals.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/need/need-signals.md` 中 **行动点 #0142** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0143"
    title: "行动点 #0143"
    status: "completed"
    dependencies: []
    action_point_id: "0143"
    audit_report: "working-plan/skill-audit-code-check/src/game/need/need-signals.md"
    target_src: "src/game/need/need-signals.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/need/need-signals.md"
        - "src/game/need/need-signals.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0143**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/need/need-signals.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/need/need-signals.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/need/need-signals.md`，找到 **行动点 #0143** 的完整表述与依据引用的 YAML。
      3. Read `src/game/need/need-signals.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/need/need-signals.md` 中 **行动点 #0143** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0144"
    title: "行动点 #0144"
    status: "completed"
    dependencies: []
    action_point_id: "0144"
    audit_report: "working-plan/skill-audit-code-check/src/game/need/need-utils.md"
    target_src: "src/game/need/need-utils.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/need/need-utils.md"
        - "src/game/need/need-utils.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0144**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/need/need-utils.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/need/need-utils.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/need/need-utils.md`，找到 **行动点 #0144** 的完整表述与依据引用的 YAML。
      3. Read `src/game/need/need-utils.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/need/need-utils.md` 中 **行动点 #0144** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0145"
    title: "行动点 #0145"
    status: "completed"
    dependencies: []
    action_point_id: "0145"
    audit_report: "working-plan/skill-audit-code-check/src/game/need/need-utils.md"
    target_src: "src/game/need/need-utils.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/need/need-utils.md"
        - "src/game/need/need-utils.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0145**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/need/need-utils.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/need/need-utils.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/need/need-utils.md`，找到 **行动点 #0145** 的完整表述与依据引用的 YAML。
      3. Read `src/game/need/need-utils.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/need/need-utils.md` 中 **行动点 #0145** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0146"
    title: "行动点 #0146"
    status: "completed"
    dependencies: []
    action_point_id: "0146"
    audit_report: "working-plan/skill-audit-code-check/src/game/need/need-utils.md"
    target_src: "src/game/need/need-utils.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/need/need-utils.md"
        - "src/game/need/need-utils.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0146**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/need/need-utils.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/need/need-utils.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/need/need-utils.md`，找到 **行动点 #0146** 的完整表述与依据引用的 YAML。
      3. Read `src/game/need/need-utils.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/need/need-utils.md` 中 **行动点 #0146** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0147"
    title: "行动点 #0147"
    status: "completed"
    dependencies: []
    action_point_id: "0147"
    audit_report: "working-plan/skill-audit-code-check/src/game/need/threshold-rules.md"
    target_src: "src/game/need/threshold-rules.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/need/threshold-rules.md"
        - "src/game/need/threshold-rules.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0147**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/need/threshold-rules.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/need/threshold-rules.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/need/threshold-rules.md`，找到 **行动点 #0147** 的完整表述与依据引用的 YAML。
      3. Read `src/game/need/threshold-rules.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/need/threshold-rules.md` 中 **行动点 #0147** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0148"
    title: "行动点 #0148"
    status: "completed"
    dependencies: []
    action_point_id: "0148"
    audit_report: "working-plan/skill-audit-code-check/src/game/need/threshold-rules.md"
    target_src: "src/game/need/threshold-rules.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/need/threshold-rules.md"
        - "src/game/need/threshold-rules.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0148**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/need/threshold-rules.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/need/threshold-rules.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/need/threshold-rules.md`，找到 **行动点 #0148** 的完整表述与依据引用的 YAML。
      3. Read `src/game/need/threshold-rules.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/need/threshold-rules.md` 中 **行动点 #0148** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0149"
    title: "行动点 #0149"
    status: "completed"
    dependencies: []
    action_point_id: "0149"
    audit_report: "working-plan/skill-audit-code-check/src/game/orchestrator-world-bridge.md"
    target_src: "src/game/orchestrator-world-bridge.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/orchestrator-world-bridge.md"
        - "src/game/orchestrator-world-bridge.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0149**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/orchestrator-world-bridge.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/orchestrator-world-bridge.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/orchestrator-world-bridge.md`，找到 **行动点 #0149** 的完整表述与依据引用的 YAML。
      3. Read `src/game/orchestrator-world-bridge.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/orchestrator-world-bridge.md` 中 **行动点 #0149** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0150"
    title: "行动点 #0150"
    status: "completed"
    dependencies: []
    action_point_id: "0150"
    audit_report: "working-plan/skill-audit-code-check/src/game/orchestrator-world-bridge.md"
    target_src: "src/game/orchestrator-world-bridge.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/orchestrator-world-bridge.md"
        - "src/game/orchestrator-world-bridge.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0150**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/orchestrator-world-bridge.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/orchestrator-world-bridge.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/orchestrator-world-bridge.md`，找到 **行动点 #0150** 的完整表述与依据引用的 YAML。
      3. Read `src/game/orchestrator-world-bridge.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/orchestrator-world-bridge.md` 中 **行动点 #0150** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0151"
    title: "行动点 #0151"
    status: "completed"
    dependencies: []
    action_point_id: "0151"
    audit_report: "working-plan/skill-audit-code-check/src/game/pawn-state.md"
    target_src: "src/game/pawn-state.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/pawn-state.md"
        - "src/game/pawn-state.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0151**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/pawn-state.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/pawn-state.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/pawn-state.md`，找到 **行动点 #0151** 的完整表述与依据引用的 YAML。
      3. Read `src/game/pawn-state.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/pawn-state.md` 中 **行动点 #0151** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0152"
    title: "行动点 #0152"
    status: "completed"
    dependencies: []
    action_point_id: "0152"
    audit_report: "working-plan/skill-audit-code-check/src/game/pawn-state.md"
    target_src: "src/game/pawn-state.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/pawn-state.md"
        - "src/game/pawn-state.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0152**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/pawn-state.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/pawn-state.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/pawn-state.md`，找到 **行动点 #0152** 的完整表述与依据引用的 YAML。
      3. Read `src/game/pawn-state.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/pawn-state.md` 中 **行动点 #0152** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0153"
    title: "行动点 #0153"
    status: "completed"
    dependencies: []
    action_point_id: "0153"
    audit_report: "working-plan/skill-audit-code-check/src/game/pawn-state.md"
    target_src: "src/game/pawn-state.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/pawn-state.md"
        - "src/game/pawn-state.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0153**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/pawn-state.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/pawn-state.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/pawn-state.md`，找到 **行动点 #0153** 的完整表述与依据引用的 YAML。
      3. Read `src/game/pawn-state.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/pawn-state.md` 中 **行动点 #0153** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0154"
    title: "行动点 #0154"
    status: "completed"
    dependencies: []
    action_point_id: "0154"
    audit_report: "working-plan/skill-audit-code-check/src/game/time/index.md"
    target_src: "src/game/time/index.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/time/index.md"
        - "src/game/time/index.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0154**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/time/index.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/time/index.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/time/index.md`，找到 **行动点 #0154** 的完整表述与依据引用的 YAML。
      3. Read `src/game/time/index.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/time/index.md` 中 **行动点 #0154** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0155"
    title: "行动点 #0155"
    status: "completed"
    dependencies: []
    action_point_id: "0155"
    audit_report: "working-plan/skill-audit-code-check/src/game/time/time-event-bus.md"
    target_src: "src/game/time/time-event-bus.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/time/time-event-bus.md"
        - "src/game/time/time-event-bus.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0155**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/time/time-event-bus.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/time/time-event-bus.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/time/time-event-bus.md`，找到 **行动点 #0155** 的完整表述与依据引用的 YAML。
      3. Read `src/game/time/time-event-bus.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/time/time-event-bus.md` 中 **行动点 #0155** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0156"
    title: "行动点 #0156"
    status: "completed"
    dependencies: []
    action_point_id: "0156"
    audit_report: "working-plan/skill-audit-code-check/src/game/time/time-event-bus.md"
    target_src: "src/game/time/time-event-bus.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/time/time-event-bus.md"
        - "src/game/time/time-event-bus.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0156**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/time/time-event-bus.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/time/time-event-bus.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/time/time-event-bus.md`，找到 **行动点 #0156** 的完整表述与依据引用的 YAML。
      3. Read `src/game/time/time-event-bus.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/time/time-event-bus.md` 中 **行动点 #0156** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0157"
    title: "行动点 #0157"
    status: "completed"
    dependencies: []
    action_point_id: "0157"
    audit_report: "working-plan/skill-audit-code-check/src/game/time/time-event-bus.md"
    target_src: "src/game/time/time-event-bus.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/time/time-event-bus.md"
        - "src/game/time/time-event-bus.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0157**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/time/time-event-bus.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/time/time-event-bus.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/time/time-event-bus.md`，找到 **行动点 #0157** 的完整表述与依据引用的 YAML。
      3. Read `src/game/time/time-event-bus.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/time/time-event-bus.md` 中 **行动点 #0157** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0158"
    title: "行动点 #0158"
    status: "completed"
    dependencies: []
    action_point_id: "0158"
    audit_report: "working-plan/skill-audit-code-check/src/game/time/time-of-day.md"
    target_src: "src/game/time/time-of-day.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/time/time-of-day.md"
        - "src/game/time/time-of-day.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0158**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/time/time-of-day.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/time/time-of-day.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/time/time-of-day.md`，找到 **行动点 #0158** 的完整表述与依据引用的 YAML。
      3. Read `src/game/time/time-of-day.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/time/time-of-day.md` 中 **行动点 #0158** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0159"
    title: "行动点 #0159"
    status: "completed"
    dependencies: []
    action_point_id: "0159"
    audit_report: "working-plan/skill-audit-code-check/src/game/time/time-of-day.md"
    target_src: "src/game/time/time-of-day.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/time/time-of-day.md"
        - "src/game/time/time-of-day.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0159**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/time/time-of-day.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/time/time-of-day.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/time/time-of-day.md`，找到 **行动点 #0159** 的完整表述与依据引用的 YAML。
      3. Read `src/game/time/time-of-day.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/time/time-of-day.md` 中 **行动点 #0159** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0160"
    title: "行动点 #0160"
    status: "completed"
    dependencies: []
    action_point_id: "0160"
    audit_report: "working-plan/skill-audit-code-check/src/game/time/time-of-day.md"
    target_src: "src/game/time/time-of-day.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/time/time-of-day.md"
        - "src/game/time/time-of-day.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0160**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/time/time-of-day.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/time/time-of-day.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/time/time-of-day.md`，找到 **行动点 #0160** 的完整表述与依据引用的 YAML。
      3. Read `src/game/time/time-of-day.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/time/time-of-day.md` 中 **行动点 #0160** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0161"
    title: "行动点 #0161"
    status: "completed"
    dependencies: []
    action_point_id: "0161"
    audit_report: "working-plan/skill-audit-code-check/src/game/time/world-time.md"
    target_src: "src/game/time/world-time.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/time/world-time.md"
        - "src/game/time/world-time.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0161**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/time/world-time.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/time/world-time.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/time/world-time.md`，找到 **行动点 #0161** 的完整表述与依据引用的 YAML。
      3. Read `src/game/time/world-time.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/time/world-time.md` 中 **行动点 #0161** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0162"
    title: "行动点 #0162"
    status: "completed"
    dependencies: []
    action_point_id: "0162"
    audit_report: "working-plan/skill-audit-code-check/src/game/time/world-time.md"
    target_src: "src/game/time/world-time.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/time/world-time.md"
        - "src/game/time/world-time.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0162**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/time/world-time.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/time/world-time.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/time/world-time.md`，找到 **行动点 #0162** 的完整表述与依据引用的 YAML。
      3. Read `src/game/time/world-time.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/time/world-time.md` 中 **行动点 #0162** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0163"
    title: "行动点 #0163"
    status: "completed"
    dependencies: []
    action_point_id: "0163"
    audit_report: "working-plan/skill-audit-code-check/src/game/time/world-time.md"
    target_src: "src/game/time/world-time.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/time/world-time.md"
        - "src/game/time/world-time.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0163**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/time/world-time.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/time/world-time.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/time/world-time.md`，找到 **行动点 #0163** 的完整表述与依据引用的 YAML。
      3. Read `src/game/time/world-time.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/time/world-time.md` 中 **行动点 #0163** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0164"
    title: "行动点 #0164"
    status: "completed"
    dependencies: []
    action_point_id: "0164"
    audit_report: "working-plan/skill-audit-code-check/src/game/work/index.md"
    target_src: "src/game/work/index.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/work/index.md"
        - "src/game/work/index.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0164**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/work/index.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/work/index.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/work/index.md`，找到 **行动点 #0164** 的完整表述与依据引用的 YAML。
      3. Read `src/game/work/index.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/work/index.md` 中 **行动点 #0164** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0165"
    title: "行动点 #0165"
    status: "completed"
    dependencies: []
    action_point_id: "0165"
    audit_report: "working-plan/skill-audit-code-check/src/game/work/index.md"
    target_src: "src/game/work/index.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/work/index.md"
        - "src/game/work/index.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0165**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/work/index.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/work/index.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/work/index.md`，找到 **行动点 #0165** 的完整表述与依据引用的 YAML。
      3. Read `src/game/work/index.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/work/index.md` 中 **行动点 #0165** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0166"
    title: "行动点 #0166"
    status: "completed"
    dependencies: []
    action_point_id: "0166"
    audit_report: "working-plan/skill-audit-code-check/src/game/work/work-generator.md"
    target_src: "src/game/work/work-generator.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/work/work-generator.md"
        - "src/game/work/work-generator.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0166**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/work/work-generator.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/work/work-generator.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/work/work-generator.md`，找到 **行动点 #0166** 的完整表述与依据引用的 YAML。
      3. Read `src/game/work/work-generator.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/work/work-generator.md` 中 **行动点 #0166** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0167"
    title: "行动点 #0167"
    status: "completed"
    dependencies: []
    action_point_id: "0167"
    audit_report: "working-plan/skill-audit-code-check/src/game/work/work-generator.md"
    target_src: "src/game/work/work-generator.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/work/work-generator.md"
        - "src/game/work/work-generator.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0167**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/work/work-generator.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/work/work-generator.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/work/work-generator.md`，找到 **行动点 #0167** 的完整表述与依据引用的 YAML。
      3. Read `src/game/work/work-generator.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/work/work-generator.md` 中 **行动点 #0167** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0168"
    title: "行动点 #0168"
    status: "completed"
    dependencies: []
    action_point_id: "0168"
    audit_report: "working-plan/skill-audit-code-check/src/game/work/work-generator.md"
    target_src: "src/game/work/work-generator.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/work/work-generator.md"
        - "src/game/work/work-generator.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0168**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/work/work-generator.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/work/work-generator.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/work/work-generator.md`，找到 **行动点 #0168** 的完整表述与依据引用的 YAML。
      3. Read `src/game/work/work-generator.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/work/work-generator.md` 中 **行动点 #0168** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0169"
    title: "行动点 #0169"
    status: "completed"
    dependencies: []
    action_point_id: "0169"
    audit_report: "working-plan/skill-audit-code-check/src/game/work/work-item-duration.md"
    target_src: "src/game/work/work-item-duration.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/work/work-item-duration.md"
        - "src/game/work/work-item-duration.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0169**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/work/work-item-duration.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/work/work-item-duration.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/work/work-item-duration.md`，找到 **行动点 #0169** 的完整表述与依据引用的 YAML。
      3. Read `src/game/work/work-item-duration.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/work/work-item-duration.md` 中 **行动点 #0169** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0170"
    title: "行动点 #0170"
    status: "completed"
    dependencies: []
    action_point_id: "0170"
    audit_report: "working-plan/skill-audit-code-check/src/game/work/work-item-duration.md"
    target_src: "src/game/work/work-item-duration.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/work/work-item-duration.md"
        - "src/game/work/work-item-duration.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0170**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/work/work-item-duration.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/work/work-item-duration.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/work/work-item-duration.md`，找到 **行动点 #0170** 的完整表述与依据引用的 YAML。
      3. Read `src/game/work/work-item-duration.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/work/work-item-duration.md` 中 **行动点 #0170** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0171"
    title: "行动点 #0171"
    status: "completed"
    dependencies: []
    action_point_id: "0171"
    audit_report: "working-plan/skill-audit-code-check/src/game/work/work-item-duration.md"
    target_src: "src/game/work/work-item-duration.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/work/work-item-duration.md"
        - "src/game/work/work-item-duration.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0171**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/work/work-item-duration.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/work/work-item-duration.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/work/work-item-duration.md`，找到 **行动点 #0171** 的完整表述与依据引用的 YAML。
      3. Read `src/game/work/work-item-duration.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/work/work-item-duration.md` 中 **行动点 #0171** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0172"
    title: "行动点 #0172"
    status: "completed"
    dependencies: []
    action_point_id: "0172"
    audit_report: "working-plan/skill-audit-code-check/src/game/work/work-operations.md"
    target_src: "src/game/work/work-operations.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/work/work-operations.md"
        - "src/game/work/work-operations.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0172**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/work/work-operations.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/work/work-operations.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/work/work-operations.md`，找到 **行动点 #0172** 的完整表述与依据引用的 YAML。
      3. Read `src/game/work/work-operations.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/work/work-operations.md` 中 **行动点 #0172** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0173"
    title: "行动点 #0173"
    status: "completed"
    dependencies: []
    action_point_id: "0173"
    audit_report: "working-plan/skill-audit-code-check/src/game/work/work-operations.md"
    target_src: "src/game/work/work-operations.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/work/work-operations.md"
        - "src/game/work/work-operations.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0173**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/work/work-operations.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/work/work-operations.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/work/work-operations.md`，找到 **行动点 #0173** 的完整表述与依据引用的 YAML。
      3. Read `src/game/work/work-operations.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/work/work-operations.md` 中 **行动点 #0173** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0174"
    title: "行动点 #0174"
    status: "completed"
    dependencies: []
    action_point_id: "0174"
    audit_report: "working-plan/skill-audit-code-check/src/game/work/work-operations.md"
    target_src: "src/game/work/work-operations.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/work/work-operations.md"
        - "src/game/work/work-operations.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0174**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/work/work-operations.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/work/work-operations.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/work/work-operations.md`，找到 **行动点 #0174** 的完整表述与依据引用的 YAML。
      3. Read `src/game/work/work-operations.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/work/work-operations.md` 中 **行动点 #0174** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0175"
    title: "行动点 #0175"
    status: "completed"
    dependencies: []
    action_point_id: "0175"
    audit_report: "working-plan/skill-audit-code-check/src/game/work/work-operations.md"
    target_src: "src/game/work/work-operations.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/work/work-operations.md"
        - "src/game/work/work-operations.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0175**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/work/work-operations.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/work/work-operations.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/work/work-operations.md`，找到 **行动点 #0175** 的完整表述与依据引用的 YAML。
      3. Read `src/game/work/work-operations.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/work/work-operations.md` 中 **行动点 #0175** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0176"
    title: "行动点 #0176"
    status: "completed"
    dependencies: []
    action_point_id: "0176"
    audit_report: "working-plan/skill-audit-code-check/src/game/work/work-operations.md"
    target_src: "src/game/work/work-operations.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/work/work-operations.md"
        - "src/game/work/work-operations.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0176**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/work/work-operations.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/work/work-operations.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/work/work-operations.md`，找到 **行动点 #0176** 的完整表述与依据引用的 YAML。
      3. Read `src/game/work/work-operations.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/work/work-operations.md` 中 **行动点 #0176** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0177"
    title: "行动点 #0177"
    status: "completed"
    dependencies: []
    action_point_id: "0177"
    audit_report: "working-plan/skill-audit-code-check/src/game/work/work-operations.md"
    target_src: "src/game/work/work-operations.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/work/work-operations.md"
        - "src/game/work/work-operations.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0177**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/work/work-operations.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/work/work-operations.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/work/work-operations.md`，找到 **行动点 #0177** 的完整表述与依据引用的 YAML。
      3. Read `src/game/work/work-operations.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/work/work-operations.md` 中 **行动点 #0177** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0178"
    title: "行动点 #0178"
    status: "completed"
    dependencies: []
    action_point_id: "0178"
    audit_report: "working-plan/skill-audit-code-check/src/game/work/work-registry.md"
    target_src: "src/game/work/work-registry.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/work/work-registry.md"
        - "src/game/work/work-registry.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0178**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/work/work-registry.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/work/work-registry.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/work/work-registry.md`，找到 **行动点 #0178** 的完整表述与依据引用的 YAML。
      3. Read `src/game/work/work-registry.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/work/work-registry.md` 中 **行动点 #0178** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0179"
    title: "行动点 #0179"
    status: "completed"
    dependencies: []
    action_point_id: "0179"
    audit_report: "working-plan/skill-audit-code-check/src/game/work/work-registry.md"
    target_src: "src/game/work/work-registry.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/work/work-registry.md"
        - "src/game/work/work-registry.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0179**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/work/work-registry.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/work/work-registry.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/work/work-registry.md`，找到 **行动点 #0179** 的完整表述与依据引用的 YAML。
      3. Read `src/game/work/work-registry.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/work/work-registry.md` 中 **行动点 #0179** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0180"
    title: "行动点 #0180"
    status: "completed"
    dependencies: []
    action_point_id: "0180"
    audit_report: "working-plan/skill-audit-code-check/src/game/work/work-registry.md"
    target_src: "src/game/work/work-registry.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/work/work-registry.md"
        - "src/game/work/work-registry.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0180**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/work/work-registry.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/work/work-registry.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/work/work-registry.md`，找到 **行动点 #0180** 的完整表述与依据引用的 YAML。
      3. Read `src/game/work/work-registry.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/work/work-registry.md` 中 **行动点 #0180** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0181"
    title: "行动点 #0181"
    status: "completed"
    dependencies: []
    action_point_id: "0181"
    audit_report: "working-plan/skill-audit-code-check/src/game/work/work-registry.md"
    target_src: "src/game/work/work-registry.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/work/work-registry.md"
        - "src/game/work/work-registry.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0181**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/work/work-registry.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/work/work-registry.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/work/work-registry.md`，找到 **行动点 #0181** 的完整表述与依据引用的 YAML。
      3. Read `src/game/work/work-registry.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/work/work-registry.md` 中 **行动点 #0181** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0182"
    title: "行动点 #0182"
    status: "completed"
    dependencies: []
    action_point_id: "0182"
    audit_report: "working-plan/skill-audit-code-check/src/game/work/work-scheduler.md"
    target_src: "src/game/work/work-scheduler.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/work/work-scheduler.md"
        - "src/game/work/work-scheduler.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0182**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/work/work-scheduler.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/work/work-scheduler.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/work/work-scheduler.md`，找到 **行动点 #0182** 的完整表述与依据引用的 YAML。
      3. Read `src/game/work/work-scheduler.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/work/work-scheduler.md` 中 **行动点 #0182** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0183"
    title: "行动点 #0183"
    status: "completed"
    dependencies: []
    action_point_id: "0183"
    audit_report: "working-plan/skill-audit-code-check/src/game/work/work-scheduler.md"
    target_src: "src/game/work/work-scheduler.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/work/work-scheduler.md"
        - "src/game/work/work-scheduler.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0183**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/work/work-scheduler.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/work/work-scheduler.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/work/work-scheduler.md`，找到 **行动点 #0183** 的完整表述与依据引用的 YAML。
      3. Read `src/game/work/work-scheduler.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/work/work-scheduler.md` 中 **行动点 #0183** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0184"
    title: "行动点 #0184"
    status: "completed"
    dependencies: []
    action_point_id: "0184"
    audit_report: "working-plan/skill-audit-code-check/src/game/work/work-scheduler.md"
    target_src: "src/game/work/work-scheduler.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/work/work-scheduler.md"
        - "src/game/work/work-scheduler.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0184**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/work/work-scheduler.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/work/work-scheduler.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/work/work-scheduler.md`，找到 **行动点 #0184** 的完整表述与依据引用的 YAML。
      3. Read `src/game/work/work-scheduler.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/work/work-scheduler.md` 中 **行动点 #0184** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0185"
    title: "行动点 #0185"
    status: "completed"
    dependencies: []
    action_point_id: "0185"
    audit_report: "working-plan/skill-audit-code-check/src/game/work/work-settler.md"
    target_src: "src/game/work/work-settler.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/work/work-settler.md"
        - "src/game/work/work-settler.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0185**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/work/work-settler.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/work/work-settler.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/work/work-settler.md`，找到 **行动点 #0185** 的完整表述与依据引用的 YAML。
      3. Read `src/game/work/work-settler.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/work/work-settler.md` 中 **行动点 #0185** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0186"
    title: "行动点 #0186"
    status: "completed"
    dependencies: []
    action_point_id: "0186"
    audit_report: "working-plan/skill-audit-code-check/src/game/work/work-settler.md"
    target_src: "src/game/work/work-settler.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/work/work-settler.md"
        - "src/game/work/work-settler.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0186**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/work/work-settler.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/work/work-settler.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/work/work-settler.md`，找到 **行动点 #0186** 的完整表述与依据引用的 YAML。
      3. Read `src/game/work/work-settler.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/work/work-settler.md` 中 **行动点 #0186** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0187"
    title: "行动点 #0187"
    status: "completed"
    dependencies: []
    action_point_id: "0187"
    audit_report: "working-plan/skill-audit-code-check/src/game/work/work-settler.md"
    target_src: "src/game/work/work-settler.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/work/work-settler.md"
        - "src/game/work/work-settler.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0187**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/work/work-settler.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/work/work-settler.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/work/work-settler.md`，找到 **行动点 #0187** 的完整表述与依据引用的 YAML。
      3. Read `src/game/work/work-settler.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/work/work-settler.md` 中 **行动点 #0187** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0188"
    title: "行动点 #0188"
    status: "completed"
    dependencies: []
    action_point_id: "0188"
    audit_report: "working-plan/skill-audit-code-check/src/game/work/work-types.md"
    target_src: "src/game/work/work-types.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/work/work-types.md"
        - "src/game/work/work-types.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0188**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/work/work-types.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/work/work-types.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/work/work-types.md`，找到 **行动点 #0188** 的完整表述与依据引用的 YAML。
      3. Read `src/game/work/work-types.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/work/work-types.md` 中 **行动点 #0188** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0189"
    title: "行动点 #0189"
    status: "completed"
    dependencies: []
    action_point_id: "0189"
    audit_report: "working-plan/skill-audit-code-check/src/game/work/work-types.md"
    target_src: "src/game/work/work-types.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/work/work-types.md"
        - "src/game/work/work-types.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0189**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/work/work-types.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/work/work-types.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/work/work-types.md`，找到 **行动点 #0189** 的完整表述与依据引用的 YAML。
      3. Read `src/game/work/work-types.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/work/work-types.md` 中 **行动点 #0189** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0190"
    title: "行动点 #0190"
    status: "completed"
    dependencies: []
    action_point_id: "0190"
    audit_report: "working-plan/skill-audit-code-check/src/game/work/work-types.md"
    target_src: "src/game/work/work-types.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/work/work-types.md"
        - "src/game/work/work-types.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0190**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/work/work-types.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/work/work-types.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/work/work-types.md`，找到 **行动点 #0190** 的完整表述与依据引用的 YAML。
      3. Read `src/game/work/work-types.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/work/work-types.md` 中 **行动点 #0190** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0191"
    title: "行动点 #0191"
    status: "completed"
    dependencies: []
    action_point_id: "0191"
    audit_report: "working-plan/skill-audit-code-check/src/game/world-bootstrap.md"
    target_src: "src/game/world-bootstrap.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/world-bootstrap.md"
        - "src/game/world-bootstrap.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0191**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/world-bootstrap.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/world-bootstrap.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/world-bootstrap.md`，找到 **行动点 #0191** 的完整表述与依据引用的 YAML。
      3. Read `src/game/world-bootstrap.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/world-bootstrap.md` 中 **行动点 #0191** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0192"
    title: "行动点 #0192"
    status: "completed"
    dependencies: []
    action_point_id: "0192"
    audit_report: "working-plan/skill-audit-code-check/src/game/world-bootstrap.md"
    target_src: "src/game/world-bootstrap.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/world-bootstrap.md"
        - "src/game/world-bootstrap.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0192**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/world-bootstrap.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/world-bootstrap.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/world-bootstrap.md`，找到 **行动点 #0192** 的完整表述与依据引用的 YAML。
      3. Read `src/game/world-bootstrap.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/world-bootstrap.md` 中 **行动点 #0192** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0193"
    title: "行动点 #0193"
    status: "completed"
    dependencies: []
    action_point_id: "0193"
    audit_report: "working-plan/skill-audit-code-check/src/game/world-bootstrap.md"
    target_src: "src/game/world-bootstrap.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/world-bootstrap.md"
        - "src/game/world-bootstrap.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0193**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/world-bootstrap.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/world-bootstrap.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/world-bootstrap.md`，找到 **行动点 #0193** 的完整表述与依据引用的 YAML。
      3. Read `src/game/world-bootstrap.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/world-bootstrap.md` 中 **行动点 #0193** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0194"
    title: "行动点 #0194"
    status: "completed"
    dependencies: []
    action_point_id: "0194"
    audit_report: "working-plan/skill-audit-code-check/src/game/world-construct-tick.md"
    target_src: "src/game/world-construct-tick.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/world-construct-tick.md"
        - "src/game/world-construct-tick.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0194**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/world-construct-tick.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/world-construct-tick.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/world-construct-tick.md`，找到 **行动点 #0194** 的完整表述与依据引用的 YAML。
      3. Read `src/game/world-construct-tick.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/world-construct-tick.md` 中 **行动点 #0194** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0195"
    title: "行动点 #0195"
    status: "completed"
    dependencies: []
    action_point_id: "0195"
    audit_report: "working-plan/skill-audit-code-check/src/game/world-construct-tick.md"
    target_src: "src/game/world-construct-tick.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/world-construct-tick.md"
        - "src/game/world-construct-tick.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0195**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/world-construct-tick.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/world-construct-tick.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/world-construct-tick.md`，找到 **行动点 #0195** 的完整表述与依据引用的 YAML。
      3. Read `src/game/world-construct-tick.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/world-construct-tick.md` 中 **行动点 #0195** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0196"
    title: "行动点 #0196"
    status: "completed"
    dependencies: []
    action_point_id: "0196"
    audit_report: "working-plan/skill-audit-code-check/src/game/world-construct-tick.md"
    target_src: "src/game/world-construct-tick.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/world-construct-tick.md"
        - "src/game/world-construct-tick.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0196**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/world-construct-tick.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/world-construct-tick.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/world-construct-tick.md`，找到 **行动点 #0196** 的完整表述与依据引用的 YAML。
      3. Read `src/game/world-construct-tick.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/world-construct-tick.md` 中 **行动点 #0196** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0197"
    title: "行动点 #0197"
    status: "completed"
    dependencies: []
    action_point_id: "0197"
    audit_report: "working-plan/skill-audit-code-check/src/game/world-core.md"
    target_src: "src/game/world-core.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/world-core.md"
        - "src/game/world-core.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0197**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/world-core.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/world-core.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/world-core.md`，找到 **行动点 #0197** 的完整表述与依据引用的 YAML。
      3. Read `src/game/world-core.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/world-core.md` 中 **行动点 #0197** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0198"
    title: "行动点 #0198"
    status: "completed"
    dependencies: []
    action_point_id: "0198"
    audit_report: "working-plan/skill-audit-code-check/src/game/world-core.md"
    target_src: "src/game/world-core.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/world-core.md"
        - "src/game/world-core.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0198**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/world-core.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/world-core.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/world-core.md`，找到 **行动点 #0198** 的完整表述与依据引用的 YAML。
      3. Read `src/game/world-core.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/world-core.md` 中 **行动点 #0198** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0199"
    title: "行动点 #0199"
    status: "completed"
    dependencies: []
    action_point_id: "0199"
    audit_report: "working-plan/skill-audit-code-check/src/game/world-core.md"
    target_src: "src/game/world-core.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/world-core.md"
        - "src/game/world-core.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0199**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/world-core.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/world-core.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/world-core.md`，找到 **行动点 #0199** 的完整表述与依据引用的 YAML。
      3. Read `src/game/world-core.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/world-core.md` 中 **行动点 #0199** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0200"
    title: "行动点 #0200"
    status: "completed"
    dependencies: []
    action_point_id: "0200"
    audit_report: "working-plan/skill-audit-code-check/src/game/world-core.md"
    target_src: "src/game/world-core.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/world-core.md"
        - "src/game/world-core.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0200**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/world-core.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/world-core.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/world-core.md`，找到 **行动点 #0200** 的完整表述与依据引用的 YAML。
      3. Read `src/game/world-core.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/world-core.md` 中 **行动点 #0200** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0201"
    title: "行动点 #0201"
    status: "completed"
    dependencies: []
    action_point_id: "0201"
    audit_report: "working-plan/skill-audit-code-check/src/game/world-internal.md"
    target_src: "src/game/world-internal.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/world-internal.md"
        - "src/game/world-internal.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0201**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/world-internal.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/world-internal.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/world-internal.md`，找到 **行动点 #0201** 的完整表述与依据引用的 YAML。
      3. Read `src/game/world-internal.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/world-internal.md` 中 **行动点 #0201** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0202"
    title: "行动点 #0202"
    status: "completed"
    dependencies: []
    action_point_id: "0202"
    audit_report: "working-plan/skill-audit-code-check/src/game/world-internal.md"
    target_src: "src/game/world-internal.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/world-internal.md"
        - "src/game/world-internal.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0202**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/world-internal.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/world-internal.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/world-internal.md`，找到 **行动点 #0202** 的完整表述与依据引用的 YAML。
      3. Read `src/game/world-internal.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/world-internal.md` 中 **行动点 #0202** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0203"
    title: "行动点 #0203"
    status: "completed"
    dependencies: []
    action_point_id: "0203"
    audit_report: "working-plan/skill-audit-code-check/src/game/world-internal.md"
    target_src: "src/game/world-internal.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/world-internal.md"
        - "src/game/world-internal.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0203**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/world-internal.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/world-internal.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/world-internal.md`，找到 **行动点 #0203** 的完整表述与依据引用的 YAML。
      3. Read `src/game/world-internal.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/world-internal.md` 中 **行动点 #0203** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0204"
    title: "行动点 #0204"
    status: "completed"
    dependencies: []
    action_point_id: "0204"
    audit_report: "working-plan/skill-audit-code-check/src/game/world-internal.md"
    target_src: "src/game/world-internal.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/world-internal.md"
        - "src/game/world-internal.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0204**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/world-internal.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/world-internal.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/world-internal.md`，找到 **行动点 #0204** 的完整表述与依据引用的 YAML。
      3. Read `src/game/world-internal.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/world-internal.md` 中 **行动点 #0204** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0205"
    title: "行动点 #0205"
    status: "completed"
    dependencies: []
    action_point_id: "0205"
    audit_report: "working-plan/skill-audit-code-check/src/game/world-internal.md"
    target_src: "src/game/world-internal.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/world-internal.md"
        - "src/game/world-internal.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0205**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/world-internal.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/world-internal.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/world-internal.md`，找到 **行动点 #0205** 的完整表述与依据引用的 YAML。
      3. Read `src/game/world-internal.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/world-internal.md` 中 **行动点 #0205** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0206"
    title: "行动点 #0206"
    status: "completed"
    dependencies: []
    action_point_id: "0206"
    audit_report: "working-plan/skill-audit-code-check/src/game/world-sim-bridge.md"
    target_src: "src/game/world-sim-bridge.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/world-sim-bridge.md"
        - "src/game/world-sim-bridge.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0206**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/world-sim-bridge.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/world-sim-bridge.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/world-sim-bridge.md`，找到 **行动点 #0206** 的完整表述与依据引用的 YAML。
      3. Read `src/game/world-sim-bridge.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/world-sim-bridge.md` 中 **行动点 #0206** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0207"
AP-0207    status: "completed"
    dependencies: []
    action_point_id: "0207"
    audit_report: "working-plan/skill-audit-code-check/src/game/world-sim-bridge.md"
    target_src: "src/game/world-sim-bridge.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/world-sim-bridge.md"
        - "src/game/world-sim-bridge.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0207**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/world-sim-bridge.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/world-sim-bridge.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/world-sim-bridge.md`，找到 **行动点 #0207** 的完整表述与依据引用的 YAML。
      3. Read `src/game/world-sim-bridge.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/world-sim-bridge.md` 中 **行动点 #0207** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0208"
    title: "行动点 #0208"
    status: "completed"
    dependencies: []
    action_point_id: "0208"
    audit_report: "working-plan/skill-audit-code-check/src/game/world-sim-bridge.md"
    target_src: "src/game/world-sim-bridge.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/world-sim-bridge.md"
        - "src/game/world-sim-bridge.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0208**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/world-sim-bridge.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/world-sim-bridge.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/world-sim-bridge.md`，找到 **行动点 #0208** 的完整表述与依据引用的 YAML。
      3. Read `src/game/world-sim-bridge.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/world-sim-bridge.md` 中 **行动点 #0208** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0209"
    title: "行动点 #0209"
    status: "completed"
    dependencies: []
    action_point_id: "0209"
    audit_report: "working-plan/skill-audit-code-check/src/game/world-sim-bridge.md"
    target_src: "src/game/world-sim-bridge.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/world-sim-bridge.md"
        - "src/game/world-sim-bridge.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0209**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/world-sim-bridge.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/world-sim-bridge.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/world-sim-bridge.md`，找到 **行动点 #0209** 的完整表述与依据引用的 YAML。
      3. Read `src/game/world-sim-bridge.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/world-sim-bridge.md` 中 **行动点 #0209** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0210"
    title: "行动点 #0210"
    status: "completed"
    dependencies: []
    action_point_id: "0210"
    audit_report: "working-plan/skill-audit-code-check/src/game/world-work-tick.md"
    target_src: "src/game/world-work-tick.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/world-work-tick.md"
        - "src/game/world-work-tick.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0210**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/world-work-tick.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/world-work-tick.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/world-work-tick.md`，找到 **行动点 #0210** 的完整表述与依据引用的 YAML。
      3. Read `src/game/world-work-tick.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/world-work-tick.md` 中 **行动点 #0210** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0211"
    title: "行动点 #0211"
    status: "completed"
    dependencies: []
    action_point_id: "0211"
    audit_report: "working-plan/skill-audit-code-check/src/game/world-work-tick.md"
    target_src: "src/game/world-work-tick.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/world-work-tick.md"
        - "src/game/world-work-tick.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0211**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/world-work-tick.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/world-work-tick.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/world-work-tick.md`，找到 **行动点 #0211** 的完整表述与依据引用的 YAML。
      3. Read `src/game/world-work-tick.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/world-work-tick.md` 中 **行动点 #0211** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0212"
    title: "行动点 #0212"
    status: "completed"
    dependencies: []
    action_point_id: "0212"
    audit_report: "working-plan/skill-audit-code-check/src/game/world-work-tick.md"
    target_src: "src/game/world-work-tick.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/game/world-work-tick.md"
        - "src/game/world-work-tick.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0212**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/game/world-work-tick.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/game/world-work-tick.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/game/world-work-tick.md`，找到 **行动点 #0212** 的完整表述与依据引用的 YAML。
      3. Read `src/game/world-work-tick.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/game/world-work-tick.md` 中 **行动点 #0212** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0213"
    title: "行动点 #0213"
    status: "completed"
    dependencies: []
    action_point_id: "0213"
    audit_report: "working-plan/skill-audit-code-check/src/headless/headless-sim-access.md"
    target_src: "src/headless/headless-sim-access.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/headless-sim-access.md"
        - "src/headless/headless-sim-access.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0213**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/headless-sim-access.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/headless-sim-access.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/headless-sim-access.md`，找到 **行动点 #0213** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/headless-sim-access.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/headless-sim-access.md` 中 **行动点 #0213** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0214"
    title: "行动点 #0214"
    status: "completed"
    dependencies: []
    action_point_id: "0214"
    audit_report: "working-plan/skill-audit-code-check/src/headless/headless-sim-access.md"
    target_src: "src/headless/headless-sim-access.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/headless-sim-access.md"
        - "src/headless/headless-sim-access.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0214**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/headless-sim-access.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/headless-sim-access.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/headless-sim-access.md`，找到 **行动点 #0214** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/headless-sim-access.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/headless-sim-access.md` 中 **行动点 #0214** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0215"
    title: "行动点 #0215"
    status: "completed"
    dependencies: []
    action_point_id: "0215"
    audit_report: "working-plan/skill-audit-code-check/src/headless/headless-sim-access.md"
    target_src: "src/headless/headless-sim-access.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/headless-sim-access.md"
        - "src/headless/headless-sim-access.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0215**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/headless-sim-access.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/headless-sim-access.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/headless-sim-access.md`，找到 **行动点 #0215** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/headless-sim-access.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/headless-sim-access.md` 中 **行动点 #0215** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0216"
    title: "行动点 #0216"
    status: "completed"
    dependencies: []
    action_point_id: "0216"
    audit_report: "working-plan/skill-audit-code-check/src/headless/headless-sim.md"
    target_src: "src/headless/headless-sim.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/headless-sim.md"
        - "src/headless/headless-sim.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0216**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/headless-sim.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/headless-sim.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/headless-sim.md`，找到 **行动点 #0216** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/headless-sim.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/headless-sim.md` 中 **行动点 #0216** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0217"
    title: "行动点 #0217"
    status: "completed"
    dependencies: []
    action_point_id: "0217"
    audit_report: "working-plan/skill-audit-code-check/src/headless/headless-sim.md"
    target_src: "src/headless/headless-sim.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/headless-sim.md"
        - "src/headless/headless-sim.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0217**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/headless-sim.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/headless-sim.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/headless-sim.md`，找到 **行动点 #0217** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/headless-sim.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/headless-sim.md` 中 **行动点 #0217** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0218"
    title: "行动点 #0218"
    status: "completed"
    dependencies: []
    action_point_id: "0218"
    audit_report: "working-plan/skill-audit-code-check/src/headless/headless-sim.md"
    target_src: "src/headless/headless-sim.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/headless-sim.md"
        - "src/headless/headless-sim.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0218**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/headless-sim.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/headless-sim.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/headless-sim.md`，找到 **行动点 #0218** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/headless-sim.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/headless-sim.md` 中 **行动点 #0218** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0219"
    title: "行动点 #0219"
    status: "completed"
    dependencies: []
    action_point_id: "0219"
    audit_report: "working-plan/skill-audit-code-check/src/headless/headless-sim.md"
    target_src: "src/headless/headless-sim.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/headless-sim.md"
        - "src/headless/headless-sim.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0219**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/headless-sim.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/headless-sim.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/headless-sim.md`，找到 **行动点 #0219** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/headless-sim.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/headless-sim.md` 中 **行动点 #0219** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0220"
    title: "行动点 #0220"
    status: "completed"
    dependencies: []
    action_point_id: "0220"
    audit_report: "working-plan/skill-audit-code-check/src/headless/scenario-helpers.md"
    target_src: "src/headless/scenario-helpers.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/scenario-helpers.md"
        - "src/headless/scenario-helpers.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0220**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/scenario-helpers.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/scenario-helpers.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/scenario-helpers.md`，找到 **行动点 #0220** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/scenario-helpers.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/scenario-helpers.md` 中 **行动点 #0220** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0221"
    title: "行动点 #0221"
    status: "completed"
    dependencies: []
    action_point_id: "0221"
    audit_report: "working-plan/skill-audit-code-check/src/headless/scenario-helpers.md"
    target_src: "src/headless/scenario-helpers.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/scenario-helpers.md"
        - "src/headless/scenario-helpers.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0221**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/scenario-helpers.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/scenario-helpers.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/scenario-helpers.md`，找到 **行动点 #0221** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/scenario-helpers.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/scenario-helpers.md` 中 **行动点 #0221** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0222"
    title: "行动点 #0222"
    status: "completed"
    dependencies: []
    action_point_id: "0222"
    audit_report: "working-plan/skill-audit-code-check/src/headless/scenario-helpers.md"
    target_src: "src/headless/scenario-helpers.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/scenario-helpers.md"
        - "src/headless/scenario-helpers.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0222**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/scenario-helpers.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/scenario-helpers.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/scenario-helpers.md`，找到 **行动点 #0222** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/scenario-helpers.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/scenario-helpers.md` 中 **行动点 #0222** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0223"
    title: "行动点 #0223"
    status: "completed"
    dependencies: []
    action_point_id: "0223"
    audit_report: "working-plan/skill-audit-code-check/src/headless/scenario-helpers.md"
    target_src: "src/headless/scenario-helpers.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/scenario-helpers.md"
        - "src/headless/scenario-helpers.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0223**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/scenario-helpers.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/scenario-helpers.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/scenario-helpers.md`，找到 **行动点 #0223** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/scenario-helpers.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/scenario-helpers.md` 中 **行动点 #0223** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0224"
    title: "行动点 #0224"
    status: "completed"
    dependencies: []
    action_point_id: "0224"
    audit_report: "working-plan/skill-audit-code-check/src/headless/scenario-observers.md"
    target_src: "src/headless/scenario-observers.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/scenario-observers.md"
        - "src/headless/scenario-observers.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0224**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/scenario-observers.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/scenario-observers.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/scenario-observers.md`，找到 **行动点 #0224** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/scenario-observers.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/scenario-observers.md` 中 **行动点 #0224** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0225"
    title: "行动点 #0225"
    status: "completed"
    dependencies: []
    action_point_id: "0225"
    audit_report: "working-plan/skill-audit-code-check/src/headless/scenario-observers.md"
    target_src: "src/headless/scenario-observers.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/scenario-observers.md"
        - "src/headless/scenario-observers.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0225**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/scenario-observers.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/scenario-observers.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/scenario-observers.md`，找到 **行动点 #0225** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/scenario-observers.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/scenario-observers.md` 中 **行动点 #0225** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0226"
AP-0226    status: "completed"
    dependencies: []
    action_point_id: "0226"
    audit_report: "working-plan/skill-audit-code-check/src/headless/scenario-observers.md"
    target_src: "src/headless/scenario-observers.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/scenario-observers.md"
        - "src/headless/scenario-observers.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0226**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/scenario-observers.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/scenario-observers.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/scenario-observers.md`，找到 **行动点 #0226** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/scenario-observers.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/scenario-observers.md` 中 **行动点 #0226** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0227"
    title: "行动点 #0227"
    status: "completed"
    dependencies: []
    action_point_id: "0227"
    audit_report: "working-plan/skill-audit-code-check/src/headless/scenario-runner.md"
    target_src: "src/headless/scenario-runner.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/scenario-runner.md"
        - "src/headless/scenario-runner.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0227**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/scenario-runner.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/scenario-runner.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/scenario-runner.md`，找到 **行动点 #0227** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/scenario-runner.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/scenario-runner.md` 中 **行动点 #0227** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0228"
    title: "行动点 #0228"
    status: "completed"
    dependencies: []
    action_point_id: "0228"
    audit_report: "working-plan/skill-audit-code-check/src/headless/scenario-runner.md"
    target_src: "src/headless/scenario-runner.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/scenario-runner.md"
        - "src/headless/scenario-runner.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0228**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/scenario-runner.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/scenario-runner.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/scenario-runner.md`，找到 **行动点 #0228** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/scenario-runner.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/scenario-runner.md` 中 **行动点 #0228** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0229"
AP-0229    status: "completed"
    dependencies: []
    action_point_id: "0229"
    audit_report: "working-plan/skill-audit-code-check/src/headless/scenario-runner.md"
    target_src: "src/headless/scenario-runner.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/scenario-runner.md"
        - "src/headless/scenario-runner.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0229**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/scenario-runner.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/scenario-runner.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/scenario-runner.md`，找到 **行动点 #0229** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/scenario-runner.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/scenario-runner.md` 中 **行动点 #0229** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0230"
AP-0230    status: "completed"
    dependencies: []
    action_point_id: "0230"
    audit_report: "working-plan/skill-audit-code-check/src/headless/scenario-runner.md"
    target_src: "src/headless/scenario-runner.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/scenario-runner.md"
        - "src/headless/scenario-runner.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0230**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/scenario-runner.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/scenario-runner.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/scenario-runner.md`，找到 **行动点 #0230** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/scenario-runner.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/scenario-runner.md` 中 **行动点 #0230** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0231"
AP-0231    status: "completed"
    dependencies: []
    action_point_id: "0231"
    audit_report: "working-plan/skill-audit-code-check/src/headless/scenario-runner.md"
    target_src: "src/headless/scenario-runner.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/scenario-runner.md"
        - "src/headless/scenario-runner.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0231**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/scenario-runner.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/scenario-runner.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/scenario-runner.md`，找到 **行动点 #0231** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/scenario-runner.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/scenario-runner.md` 中 **行动点 #0231** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0232"
AP-0232    status: "completed"
    dependencies: []
    action_point_id: "0232"
    audit_report: "working-plan/skill-audit-code-check/src/headless/scenario-types.md"
    target_src: "src/headless/scenario-types.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/scenario-types.md"
        - "src/headless/scenario-types.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0232**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/scenario-types.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/scenario-types.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/scenario-types.md`，找到 **行动点 #0232** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/scenario-types.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/scenario-types.md` 中 **行动点 #0232** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0233"
    title: "行动点 #0233"
    status: "completed"
    dependencies: []
    action_point_id: "0233"
    audit_report: "working-plan/skill-audit-code-check/src/headless/scenario-types.md"
    target_src: "src/headless/scenario-types.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/scenario-types.md"
        - "src/headless/scenario-types.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0233**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/scenario-types.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/scenario-types.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/scenario-types.md`，找到 **行动点 #0233** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/scenario-types.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/scenario-types.md` 中 **行动点 #0233** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0234"
AP-0234    status: "completed"
    dependencies: []
    action_point_id: "0234"
    audit_report: "working-plan/skill-audit-code-check/src/headless/scenario-types.md"
    target_src: "src/headless/scenario-types.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/scenario-types.md"
        - "src/headless/scenario-types.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0234**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/scenario-types.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/scenario-types.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/scenario-types.md`，找到 **行动点 #0234** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/scenario-types.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/scenario-types.md` 中 **行动点 #0234** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0235"
AP-0235    status: "completed"
    dependencies: []
    action_point_id: "0235"
    audit_report: "working-plan/skill-audit-code-check/src/headless/scenario-types.md"
    target_src: "src/headless/scenario-types.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/scenario-types.md"
        - "src/headless/scenario-types.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0235**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/scenario-types.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/scenario-types.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/scenario-types.md`，找到 **行动点 #0235** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/scenario-types.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/scenario-types.md` 中 **行动点 #0235** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0236"
AP-0236    status: "completed"
    dependencies: []
    action_point_id: "0236"
    audit_report: "working-plan/skill-audit-code-check/src/headless/scenario-types.md"
    target_src: "src/headless/scenario-types.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/scenario-types.md"
        - "src/headless/scenario-types.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0236**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/scenario-types.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/scenario-types.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/scenario-types.md`，找到 **行动点 #0236** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/scenario-types.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/scenario-types.md` 中 **行动点 #0236** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0237"
AP-0237    status: "completed"
    dependencies: []
    action_point_id: "0237"
    audit_report: "working-plan/skill-audit-code-check/src/headless/scenario-types.md"
    target_src: "src/headless/scenario-types.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/scenario-types.md"
        - "src/headless/scenario-types.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0237**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/scenario-types.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/scenario-types.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/scenario-types.md`，找到 **行动点 #0237** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/scenario-types.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/scenario-types.md` 中 **行动点 #0237** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0238"
AP-0238    status: "completed"
    dependencies: []
    action_point_id: "0238"
    audit_report: "working-plan/skill-audit-code-check/src/headless/scenario-types.md"
    target_src: "src/headless/scenario-types.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/scenario-types.md"
        - "src/headless/scenario-types.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0238**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/scenario-types.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/scenario-types.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/scenario-types.md`，找到 **行动点 #0238** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/scenario-types.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/scenario-types.md` 中 **行动点 #0238** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0239"
AP-0239    status: "completed"
    dependencies: []
    action_point_id: "0239"
    audit_report: "working-plan/skill-audit-code-check/src/headless/sim-debug-trace.md"
    target_src: "src/headless/sim-debug-trace.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/sim-debug-trace.md"
        - "src/headless/sim-debug-trace.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0239**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/sim-debug-trace.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/sim-debug-trace.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/sim-debug-trace.md`，找到 **行动点 #0239** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/sim-debug-trace.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/sim-debug-trace.md` 中 **行动点 #0239** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0240"
    title: "行动点 #0240"
    status: "completed"
    dependencies: []
    action_point_id: "0240"
    audit_report: "working-plan/skill-audit-code-check/src/headless/sim-debug-trace.md"
    target_src: "src/headless/sim-debug-trace.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/sim-debug-trace.md"
        - "src/headless/sim-debug-trace.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0240**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/sim-debug-trace.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/sim-debug-trace.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/sim-debug-trace.md`，找到 **行动点 #0240** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/sim-debug-trace.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/sim-debug-trace.md` 中 **行动点 #0240** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0241"
AP-0241    status: "completed"
    dependencies: []
    action_point_id: "0241"
    audit_report: "working-plan/skill-audit-code-check/src/headless/sim-debug-trace.md"
    target_src: "src/headless/sim-debug-trace.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/sim-debug-trace.md"
        - "src/headless/sim-debug-trace.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0241**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/sim-debug-trace.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/sim-debug-trace.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/sim-debug-trace.md`，找到 **行动点 #0241** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/sim-debug-trace.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/sim-debug-trace.md` 中 **行动点 #0241** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0242"
AP-0242    status: "completed"
    dependencies: []
    action_point_id: "0242"
    audit_report: "working-plan/skill-audit-code-check/src/headless/sim-debug-trace.md"
    target_src: "src/headless/sim-debug-trace.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/sim-debug-trace.md"
        - "src/headless/sim-debug-trace.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0242**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/sim-debug-trace.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/sim-debug-trace.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/sim-debug-trace.md`，找到 **行动点 #0242** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/sim-debug-trace.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/sim-debug-trace.md` 中 **行动点 #0242** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0243"
AP-0243    status: "completed"
    dependencies: []
    action_point_id: "0243"
    audit_report: "working-plan/skill-audit-code-check/src/headless/sim-event-log.md"
    target_src: "src/headless/sim-event-log.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/sim-event-log.md"
        - "src/headless/sim-event-log.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0243**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/sim-event-log.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/sim-event-log.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/sim-event-log.md`，找到 **行动点 #0243** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/sim-event-log.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/sim-event-log.md` 中 **行动点 #0243** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0244"
    title: "行动点 #0244"
    status: "completed"
    dependencies: []
    action_point_id: "0244"
    audit_report: "working-plan/skill-audit-code-check/src/headless/sim-event-log.md"
    target_src: "src/headless/sim-event-log.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/sim-event-log.md"
        - "src/headless/sim-event-log.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0244**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/sim-event-log.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/sim-event-log.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/sim-event-log.md`，找到 **行动点 #0244** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/sim-event-log.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/sim-event-log.md` 中 **行动点 #0244** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0245"
AP-0245    status: "completed"
    dependencies: []
    action_point_id: "0245"
    audit_report: "working-plan/skill-audit-code-check/src/headless/sim-event-log.md"
    target_src: "src/headless/sim-event-log.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/sim-event-log.md"
        - "src/headless/sim-event-log.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0245**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/sim-event-log.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/sim-event-log.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/sim-event-log.md`，找到 **行动点 #0245** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/sim-event-log.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/sim-event-log.md` 中 **行动点 #0245** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0246"
AP-0246    status: "completed"
    dependencies: []
    action_point_id: "0246"
    audit_report: "working-plan/skill-audit-code-check/src/headless/sim-reporter.md"
    target_src: "src/headless/sim-reporter.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/sim-reporter.md"
        - "src/headless/sim-reporter.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0246**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/sim-reporter.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/sim-reporter.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/sim-reporter.md`，找到 **行动点 #0246** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/sim-reporter.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/sim-reporter.md` 中 **行动点 #0246** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0247"
    title: "行动点 #0247"
    status: "completed"
    dependencies: []
    action_point_id: "0247"
    audit_report: "working-plan/skill-audit-code-check/src/headless/sim-reporter.md"
    target_src: "src/headless/sim-reporter.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/sim-reporter.md"
        - "src/headless/sim-reporter.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0247**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/sim-reporter.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/sim-reporter.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/sim-reporter.md`，找到 **行动点 #0247** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/sim-reporter.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/sim-reporter.md` 中 **行动点 #0247** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0248"
AP-0248    status: "completed"
    dependencies: []
    action_point_id: "0248"
    audit_report: "working-plan/skill-audit-code-check/src/headless/sim-reporter.md"
    target_src: "src/headless/sim-reporter.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/sim-reporter.md"
        - "src/headless/sim-reporter.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0248**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/sim-reporter.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/sim-reporter.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/sim-reporter.md`，找到 **行动点 #0248** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/sim-reporter.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/sim-reporter.md` 中 **行动点 #0248** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0249"
AP-0249    status: "completed"
    dependencies: []
    action_point_id: "0249"
    audit_report: "working-plan/skill-audit-code-check/src/headless/sim-reporter.md"
    target_src: "src/headless/sim-reporter.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/headless/sim-reporter.md"
        - "src/headless/sim-reporter.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0249**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/headless/sim-reporter.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/headless/sim-reporter.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/headless/sim-reporter.md`，找到 **行动点 #0249** 的完整表述与依据引用的 YAML。
      3. Read `src/headless/sim-reporter.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/headless/sim-reporter.md` 中 **行动点 #0249** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0250"
    title: "行动点 #0250"
    status: "completed"
    dependencies: []
    action_point_id: "0250"
    audit_report: "working-plan/skill-audit-code-check/src/main.md"
    target_src: "src/main.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/main.md"
        - "src/main.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0250**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/main.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/main.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/main.md`，找到 **行动点 #0250** 的完整表述与依据引用的 YAML。
      3. Read `src/main.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/main.md` 中 **行动点 #0250** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0251"
    title: "行动点 #0251"
    status: "completed"
    dependencies: []
    action_point_id: "0251"
    audit_report: "working-plan/skill-audit-code-check/src/main.md"
    target_src: "src/main.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/main.md"
        - "src/main.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0251**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/main.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/main.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/main.md`，找到 **行动点 #0251** 的完整表述与依据引用的 YAML。
      3. Read `src/main.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/main.md` 中 **行动点 #0251** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0252"
    title: "行动点 #0252"
    status: "completed"
    dependencies: []
    action_point_id: "0252"
    audit_report: "working-plan/skill-audit-code-check/src/player/apply-domain-command.md"
    target_src: "src/player/apply-domain-command.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/apply-domain-command.md"
        - "src/player/apply-domain-command.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0252**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/apply-domain-command.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/apply-domain-command.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/apply-domain-command.md`，找到 **行动点 #0252** 的完整表述与依据引用的 YAML。
      3. Read `src/player/apply-domain-command.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/apply-domain-command.md` 中 **行动点 #0252** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0253"
    title: "行动点 #0253"
    status: "completed"
    dependencies: []
    action_point_id: "0253"
    audit_report: "working-plan/skill-audit-code-check/src/player/apply-domain-command.md"
    target_src: "src/player/apply-domain-command.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/apply-domain-command.md"
        - "src/player/apply-domain-command.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0253**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/apply-domain-command.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/apply-domain-command.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/apply-domain-command.md`，找到 **行动点 #0253** 的完整表述与依据引用的 YAML。
      3. Read `src/player/apply-domain-command.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/apply-domain-command.md` 中 **行动点 #0253** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0254"
AP-0254    status: "completed"
    dependencies: []
    action_point_id: "0254"
    audit_report: "working-plan/skill-audit-code-check/src/player/apply-domain-command.md"
    target_src: "src/player/apply-domain-command.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/apply-domain-command.md"
        - "src/player/apply-domain-command.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0254**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/apply-domain-command.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/apply-domain-command.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/apply-domain-command.md`，找到 **行动点 #0254** 的完整表述与依据引用的 YAML。
      3. Read `src/player/apply-domain-command.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/apply-domain-command.md` 中 **行动点 #0254** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0255"
AP-0255    status: "completed"
    dependencies: []
    action_point_id: "0255"
    audit_report: "working-plan/skill-audit-code-check/src/player/apply-domain-command.md"
    target_src: "src/player/apply-domain-command.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/apply-domain-command.md"
        - "src/player/apply-domain-command.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0255**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/apply-domain-command.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/apply-domain-command.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/apply-domain-command.md`，找到 **行动点 #0255** 的完整表述与依据引用的 YAML。
      3. Read `src/player/apply-domain-command.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/apply-domain-command.md` 中 **行动点 #0255** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0256"
    title: "行动点 #0256"
    status: "completed"
    dependencies: []
    action_point_id: "0256"
    audit_report: "working-plan/skill-audit-code-check/src/player/brush-stroke.md"
    target_src: "src/player/brush-stroke.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/brush-stroke.md"
        - "src/player/brush-stroke.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0256**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/brush-stroke.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/brush-stroke.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/brush-stroke.md`，找到 **行动点 #0256** 的完整表述与依据引用的 YAML。
      3. Read `src/player/brush-stroke.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/brush-stroke.md` 中 **行动点 #0256** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0257"
    title: "行动点 #0257"
    status: "completed"
    dependencies: []
    action_point_id: "0257"
    audit_report: "working-plan/skill-audit-code-check/src/player/brush-stroke.md"
    target_src: "src/player/brush-stroke.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/brush-stroke.md"
        - "src/player/brush-stroke.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0257**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/brush-stroke.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/brush-stroke.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/brush-stroke.md`，找到 **行动点 #0257** 的完整表述与依据引用的 YAML。
      3. Read `src/player/brush-stroke.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/brush-stroke.md` 中 **行动点 #0257** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0258"
AP-0258    status: "completed"
    dependencies: []
    action_point_id: "0258"
    audit_report: "working-plan/skill-audit-code-check/src/player/brush-stroke.md"
    target_src: "src/player/brush-stroke.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/brush-stroke.md"
        - "src/player/brush-stroke.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0258**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/brush-stroke.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/brush-stroke.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/brush-stroke.md`，找到 **行动点 #0258** 的完整表述与依据引用的 YAML。
      3. Read `src/player/brush-stroke.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/brush-stroke.md` 中 **行动点 #0258** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0259"
    title: "行动点 #0259"
    status: "completed"
    dependencies: []
    action_point_id: "0259"
    audit_report: "working-plan/skill-audit-code-check/src/player/build-domain-command.md"
    target_src: "src/player/build-domain-command.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/build-domain-command.md"
        - "src/player/build-domain-command.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0259**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/build-domain-command.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/build-domain-command.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/build-domain-command.md`，找到 **行动点 #0259** 的完整表述与依据引用的 YAML。
      3. Read `src/player/build-domain-command.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/build-domain-command.md` 中 **行动点 #0259** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0260"
    title: "行动点 #0260"
    status: "completed"
    dependencies: []
    action_point_id: "0260"
    audit_report: "working-plan/skill-audit-code-check/src/player/build-domain-command.md"
    target_src: "src/player/build-domain-command.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/build-domain-command.md"
        - "src/player/build-domain-command.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0260**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/build-domain-command.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/build-domain-command.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/build-domain-command.md`，找到 **行动点 #0260** 的完整表述与依据引用的 YAML。
      3. Read `src/player/build-domain-command.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/build-domain-command.md` 中 **行动点 #0260** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0261"
AP-0261    status: "completed"
    dependencies: []
    action_point_id: "0261"
    audit_report: "working-plan/skill-audit-code-check/src/player/build-domain-command.md"
    target_src: "src/player/build-domain-command.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/build-domain-command.md"
        - "src/player/build-domain-command.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0261**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/build-domain-command.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/build-domain-command.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/build-domain-command.md`，找到 **行动点 #0261** 的完整表述与依据引用的 YAML。
      3. Read `src/player/build-domain-command.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/build-domain-command.md` 中 **行动点 #0261** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0262"
    title: "行动点 #0262"
    status: "completed"
    dependencies: []
    action_point_id: "0262"
    audit_report: "working-plan/skill-audit-code-check/src/player/commit-player-intent.md"
    target_src: "src/player/commit-player-intent.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/commit-player-intent.md"
        - "src/player/commit-player-intent.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0262**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/commit-player-intent.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/commit-player-intent.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/commit-player-intent.md`，找到 **行动点 #0262** 的完整表述与依据引用的 YAML。
      3. Read `src/player/commit-player-intent.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/commit-player-intent.md` 中 **行动点 #0262** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0263"
    title: "行动点 #0263"
    status: "completed"
    dependencies: []
    action_point_id: "0263"
    audit_report: "working-plan/skill-audit-code-check/src/player/commit-player-intent.md"
    target_src: "src/player/commit-player-intent.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/commit-player-intent.md"
        - "src/player/commit-player-intent.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0263**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/commit-player-intent.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/commit-player-intent.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/commit-player-intent.md`，找到 **行动点 #0263** 的完整表述与依据引用的 YAML。
      3. Read `src/player/commit-player-intent.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/commit-player-intent.md` 中 **行动点 #0263** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0264"
AP-0264    status: "completed"
    dependencies: []
    action_point_id: "0264"
    audit_report: "working-plan/skill-audit-code-check/src/player/commit-player-intent.md"
    target_src: "src/player/commit-player-intent.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/commit-player-intent.md"
        - "src/player/commit-player-intent.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0264**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/commit-player-intent.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/commit-player-intent.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/commit-player-intent.md`，找到 **行动点 #0264** 的完整表述与依据引用的 YAML。
      3. Read `src/player/commit-player-intent.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/commit-player-intent.md` 中 **行动点 #0264** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0265"
    title: "行动点 #0265"
    status: "completed"
    dependencies: []
    action_point_id: "0265"
    audit_report: "working-plan/skill-audit-code-check/src/player/interaction-mode-presenter.md"
    target_src: "src/player/interaction-mode-presenter.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/interaction-mode-presenter.md"
        - "src/player/interaction-mode-presenter.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0265**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/interaction-mode-presenter.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/interaction-mode-presenter.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/interaction-mode-presenter.md`，找到 **行动点 #0265** 的完整表述与依据引用的 YAML。
      3. Read `src/player/interaction-mode-presenter.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/interaction-mode-presenter.md` 中 **行动点 #0265** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0266"
    title: "行动点 #0266"
    status: "completed"
    dependencies: []
    action_point_id: "0266"
    audit_report: "working-plan/skill-audit-code-check/src/player/interaction-mode-presenter.md"
    target_src: "src/player/interaction-mode-presenter.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/interaction-mode-presenter.md"
        - "src/player/interaction-mode-presenter.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0266**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/interaction-mode-presenter.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/interaction-mode-presenter.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/interaction-mode-presenter.md`，找到 **行动点 #0266** 的完整表述与依据引用的 YAML。
      3. Read `src/player/interaction-mode-presenter.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/interaction-mode-presenter.md` 中 **行动点 #0266** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0267"
AP-0267    status: "completed"
    dependencies: []
    action_point_id: "0267"
    audit_report: "working-plan/skill-audit-code-check/src/player/interaction-mode-presenter.md"
    target_src: "src/player/interaction-mode-presenter.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/interaction-mode-presenter.md"
        - "src/player/interaction-mode-presenter.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0267**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/interaction-mode-presenter.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/interaction-mode-presenter.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/interaction-mode-presenter.md`，找到 **行动点 #0267** 的完整表述与依据引用的 YAML。
      3. Read `src/player/interaction-mode-presenter.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/interaction-mode-presenter.md` 中 **行动点 #0267** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0268"
    title: "行动点 #0268"
    status: "completed"
    dependencies: []
    action_point_id: "0268"
    audit_report: "working-plan/skill-audit-code-check/src/player/mock-world-port.md"
    target_src: "src/player/mock-world-port.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/mock-world-port.md"
        - "src/player/mock-world-port.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0268**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/mock-world-port.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/mock-world-port.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/mock-world-port.md`，找到 **行动点 #0268** 的完整表述与依据引用的 YAML。
      3. Read `src/player/mock-world-port.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/mock-world-port.md` 中 **行动点 #0268** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0269"
    title: "行动点 #0269"
    status: "completed"
    dependencies: []
    action_point_id: "0269"
    audit_report: "working-plan/skill-audit-code-check/src/player/mock-world-port.md"
    target_src: "src/player/mock-world-port.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/mock-world-port.md"
        - "src/player/mock-world-port.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0269**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/mock-world-port.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/mock-world-port.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/mock-world-port.md`，找到 **行动点 #0269** 的完整表述与依据引用的 YAML。
      3. Read `src/player/mock-world-port.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/mock-world-port.md` 中 **行动点 #0269** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0270"
AP-0270    status: "completed"
    dependencies: []
    action_point_id: "0270"
    audit_report: "working-plan/skill-audit-code-check/src/player/mock-world-port.md"
    target_src: "src/player/mock-world-port.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/mock-world-port.md"
        - "src/player/mock-world-port.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0270**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/mock-world-port.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/mock-world-port.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/mock-world-port.md`，找到 **行动点 #0270** 的完整表述与依据引用的 YAML。
      3. Read `src/player/mock-world-port.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/mock-world-port.md` 中 **行动点 #0270** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0271"
AP-0271    status: "completed"
    dependencies: []
    action_point_id: "0271"
    audit_report: "working-plan/skill-audit-code-check/src/player/mock-world-port.md"
    target_src: "src/player/mock-world-port.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/mock-world-port.md"
        - "src/player/mock-world-port.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0271**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/mock-world-port.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/mock-world-port.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/mock-world-port.md`，找到 **行动点 #0271** 的完整表述与依据引用的 YAML。
      3. Read `src/player/mock-world-port.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/mock-world-port.md` 中 **行动点 #0271** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0272"
    title: "行动点 #0272"
    status: "completed"
    dependencies: []
    action_point_id: "0272"
    audit_report: "working-plan/skill-audit-code-check/src/player/need-signals.md"
    target_src: "src/player/need-signals.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/need-signals.md"
        - "src/player/need-signals.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0272**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/need-signals.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/need-signals.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/need-signals.md`，找到 **行动点 #0272** 的完整表述与依据引用的 YAML。
      3. Read `src/player/need-signals.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/need-signals.md` 中 **行动点 #0272** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0273"
    title: "行动点 #0273"
    status: "completed"
    dependencies: []
    action_point_id: "0273"
    audit_report: "working-plan/skill-audit-code-check/src/player/need-signals.md"
    target_src: "src/player/need-signals.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/need-signals.md"
        - "src/player/need-signals.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0273**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/need-signals.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/need-signals.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/need-signals.md`，找到 **行动点 #0273** 的完整表述与依据引用的 YAML。
      3. Read `src/player/need-signals.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/need-signals.md` 中 **行动点 #0273** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0274"
AP-0274    status: "completed"
    dependencies: []
    action_point_id: "0274"
    audit_report: "working-plan/skill-audit-code-check/src/player/need-signals.md"
    target_src: "src/player/need-signals.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/need-signals.md"
        - "src/player/need-signals.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0274**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/need-signals.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/need-signals.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/need-signals.md`，找到 **行动点 #0274** 的完整表述与依据引用的 YAML。
      3. Read `src/player/need-signals.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/need-signals.md` 中 **行动点 #0274** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0275"
    title: "行动点 #0275"
    status: "completed"
    dependencies: []
    action_point_id: "0275"
    audit_report: "working-plan/skill-audit-code-check/src/player/s0-contract.md"
    target_src: "src/player/s0-contract.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/s0-contract.md"
        - "src/player/s0-contract.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0275**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/s0-contract.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/s0-contract.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/s0-contract.md`，找到 **行动点 #0275** 的完整表述与依据引用的 YAML。
      3. Read `src/player/s0-contract.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/s0-contract.md` 中 **行动点 #0275** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0276"
    title: "行动点 #0276"
    status: "completed"
    dependencies: []
    action_point_id: "0276"
    audit_report: "working-plan/skill-audit-code-check/src/player/s0-contract.md"
    target_src: "src/player/s0-contract.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/s0-contract.md"
        - "src/player/s0-contract.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0276**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/s0-contract.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/s0-contract.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/s0-contract.md`，找到 **行动点 #0276** 的完整表述与依据引用的 YAML。
      3. Read `src/player/s0-contract.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/s0-contract.md` 中 **行动点 #0276** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0277"
AP-0277    status: "completed"
    dependencies: []
    action_point_id: "0277"
    audit_report: "working-plan/skill-audit-code-check/src/player/s0-contract.md"
    target_src: "src/player/s0-contract.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/s0-contract.md"
        - "src/player/s0-contract.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0277**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/s0-contract.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/s0-contract.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/s0-contract.md`，找到 **行动点 #0277** 的完整表述与依据引用的 YAML。
      3. Read `src/player/s0-contract.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/s0-contract.md` 中 **行动点 #0277** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0278"
    title: "行动点 #0278"
    status: "completed"
    dependencies: []
    action_point_id: "0278"
    audit_report: "working-plan/skill-audit-code-check/src/player/scenario-loader.md"
    target_src: "src/player/scenario-loader.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/scenario-loader.md"
        - "src/player/scenario-loader.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0278**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/scenario-loader.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/scenario-loader.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/scenario-loader.md`，找到 **行动点 #0278** 的完整表述与依据引用的 YAML。
      3. Read `src/player/scenario-loader.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/scenario-loader.md` 中 **行动点 #0278** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0279"
    title: "行动点 #0279"
    status: "completed"
    dependencies: []
    action_point_id: "0279"
    audit_report: "working-plan/skill-audit-code-check/src/player/scenario-loader.md"
    target_src: "src/player/scenario-loader.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/scenario-loader.md"
        - "src/player/scenario-loader.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0279**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/scenario-loader.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/scenario-loader.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/scenario-loader.md`，找到 **行动点 #0279** 的完整表述与依据引用的 YAML。
      3. Read `src/player/scenario-loader.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/scenario-loader.md` 中 **行动点 #0279** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0280"
AP-0280    status: "completed"
    dependencies: []
    action_point_id: "0280"
    audit_report: "working-plan/skill-audit-code-check/src/player/scenario-loader.md"
    target_src: "src/player/scenario-loader.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/scenario-loader.md"
        - "src/player/scenario-loader.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0280**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/scenario-loader.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/scenario-loader.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/scenario-loader.md`，找到 **行动点 #0280** 的完整表述与依据引用的 YAML。
      3. Read `src/player/scenario-loader.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/scenario-loader.md` 中 **行动点 #0280** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0281"
AP-0281    status: "completed"
    dependencies: []
    action_point_id: "0281"
    audit_report: "working-plan/skill-audit-code-check/src/player/scenario-loader.md"
    target_src: "src/player/scenario-loader.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/scenario-loader.md"
        - "src/player/scenario-loader.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0281**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/scenario-loader.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/scenario-loader.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/scenario-loader.md`，找到 **行动点 #0281** 的完整表述与依据引用的 YAML。
      3. Read `src/player/scenario-loader.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/scenario-loader.md` 中 **行动点 #0281** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0282"
    title: "行动点 #0282"
    status: "completed"
    dependencies: []
    action_point_id: "0282"
    audit_report: "working-plan/skill-audit-code-check/src/player/task-marker-target-cells.md"
    target_src: "src/player/task-marker-target-cells.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/task-marker-target-cells.md"
        - "src/player/task-marker-target-cells.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0282**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/task-marker-target-cells.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/task-marker-target-cells.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/task-marker-target-cells.md`，找到 **行动点 #0282** 的完整表述与依据引用的 YAML。
      3. Read `src/player/task-marker-target-cells.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/task-marker-target-cells.md` 中 **行动点 #0282** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0283"
    title: "行动点 #0283"
    status: "completed"
    dependencies: []
    action_point_id: "0283"
    audit_report: "working-plan/skill-audit-code-check/src/player/task-marker-target-cells.md"
    target_src: "src/player/task-marker-target-cells.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/task-marker-target-cells.md"
        - "src/player/task-marker-target-cells.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0283**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/task-marker-target-cells.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/task-marker-target-cells.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/task-marker-target-cells.md`，找到 **行动点 #0283** 的完整表述与依据引用的 YAML。
      3. Read `src/player/task-marker-target-cells.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/task-marker-target-cells.md` 中 **行动点 #0283** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0284"
AP-0284    status: "completed"
    dependencies: []
    action_point_id: "0284"
    audit_report: "working-plan/skill-audit-code-check/src/player/task-marker-target-cells.md"
    target_src: "src/player/task-marker-target-cells.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/task-marker-target-cells.md"
        - "src/player/task-marker-target-cells.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0284**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/task-marker-target-cells.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/task-marker-target-cells.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/task-marker-target-cells.md`，找到 **行动点 #0284** 的完整表述与依据引用的 YAML。
      3. Read `src/player/task-marker-target-cells.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/task-marker-target-cells.md` 中 **行动点 #0284** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0285"
    title: "行动点 #0285"
    status: "completed"
    dependencies: []
    action_point_id: "0285"
    audit_report: "working-plan/skill-audit-code-check/src/player/tool-input-policy.md"
    target_src: "src/player/tool-input-policy.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/tool-input-policy.md"
        - "src/player/tool-input-policy.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0285**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/tool-input-policy.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/tool-input-policy.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/tool-input-policy.md`，找到 **行动点 #0285** 的完整表述与依据引用的 YAML。
      3. Read `src/player/tool-input-policy.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/tool-input-policy.md` 中 **行动点 #0285** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0286"
    title: "行动点 #0286"
    status: "completed"
    dependencies: []
    action_point_id: "0286"
    audit_report: "working-plan/skill-audit-code-check/src/player/tool-input-policy.md"
    target_src: "src/player/tool-input-policy.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/tool-input-policy.md"
        - "src/player/tool-input-policy.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0286**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/tool-input-policy.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/tool-input-policy.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/tool-input-policy.md`，找到 **行动点 #0286** 的完整表述与依据引用的 YAML。
      3. Read `src/player/tool-input-policy.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/tool-input-policy.md` 中 **行动点 #0286** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0287"
AP-0287    status: "completed"
    dependencies: []
    action_point_id: "0287"
    audit_report: "working-plan/skill-audit-code-check/src/player/tool-input-policy.md"
    target_src: "src/player/tool-input-policy.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/tool-input-policy.md"
        - "src/player/tool-input-policy.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0287**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/tool-input-policy.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/tool-input-policy.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/tool-input-policy.md`，找到 **行动点 #0287** 的完整表述与依据引用的 YAML。
      3. Read `src/player/tool-input-policy.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/tool-input-policy.md` 中 **行动点 #0287** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0288"
    title: "行动点 #0288"
    status: "completed"
    dependencies: []
    action_point_id: "0288"
    audit_report: "working-plan/skill-audit-code-check/src/player/world-core-world-port.md"
    target_src: "src/player/world-core-world-port.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/world-core-world-port.md"
        - "src/player/world-core-world-port.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0288**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/world-core-world-port.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/world-core-world-port.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/world-core-world-port.md`，找到 **行动点 #0288** 的完整表述与依据引用的 YAML。
      3. Read `src/player/world-core-world-port.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/world-core-world-port.md` 中 **行动点 #0288** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0289"
AP-0289    status: "completed"
    dependencies: []
    action_point_id: "0289"
    audit_report: "working-plan/skill-audit-code-check/src/player/world-core-world-port.md"
    target_src: "src/player/world-core-world-port.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/world-core-world-port.md"
        - "src/player/world-core-world-port.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0289**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/world-core-world-port.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/world-core-world-port.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/world-core-world-port.md`，找到 **行动点 #0289** 的完整表述与依据引用的 YAML。
      3. Read `src/player/world-core-world-port.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/world-core-world-port.md` 中 **行动点 #0289** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0290"
    title: "行动点 #0290"
    status: "completed"
    dependencies: []
    action_point_id: "0290"
    audit_report: "working-plan/skill-audit-code-check/src/player/world-port-types.md"
    target_src: "src/player/world-port-types.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/world-port-types.md"
        - "src/player/world-port-types.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0290**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/world-port-types.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/world-port-types.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/world-port-types.md`，找到 **行动点 #0290** 的完整表述与依据引用的 YAML。
      3. Read `src/player/world-port-types.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/world-port-types.md` 中 **行动点 #0290** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0291"
AP-0291    status: "completed"
    dependencies: []
    action_point_id: "0291"
    audit_report: "working-plan/skill-audit-code-check/src/player/world-port-types.md"
    target_src: "src/player/world-port-types.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/world-port-types.md"
        - "src/player/world-port-types.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0291**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/world-port-types.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/world-port-types.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/world-port-types.md`，找到 **行动点 #0291** 的完整表述与依据引用的 YAML。
      3. Read `src/player/world-port-types.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/world-port-types.md` 中 **行动点 #0291** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0292"
AP-0292    status: "completed"
    dependencies: []
    action_point_id: "0292"
    audit_report: "working-plan/skill-audit-code-check/src/player/world-port-types.md"
    target_src: "src/player/world-port-types.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/player/world-port-types.md"
        - "src/player/world-port-types.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0292**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/player/world-port-types.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/player/world-port-types.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/player/world-port-types.md`，找到 **行动点 #0292** 的完整表述与依据引用的 YAML。
      3. Read `src/player/world-port-types.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/player/world-port-types.md` 中 **行动点 #0292** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0293"
    title: "行动点 #0293"
    status: "completed"
    dependencies: []
    action_point_id: "0293"
    audit_report: "working-plan/skill-audit-code-check/src/runtime-log/runtime-log-dev-client.md"
    target_src: "src/runtime-log/runtime-log-dev-client.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/runtime-log/runtime-log-dev-client.md"
        - "src/runtime-log/runtime-log-dev-client.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0293**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/runtime-log/runtime-log-dev-client.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/runtime-log/runtime-log-dev-client.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/runtime-log/runtime-log-dev-client.md`，找到 **行动点 #0293** 的完整表述与依据引用的 YAML。
      3. Read `src/runtime-log/runtime-log-dev-client.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/runtime-log/runtime-log-dev-client.md` 中 **行动点 #0293** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0294"
AP-0294    status: "completed"
    dependencies: []
    action_point_id: "0294"
    audit_report: "working-plan/skill-audit-code-check/src/runtime-log/runtime-log-dev-client.md"
    target_src: "src/runtime-log/runtime-log-dev-client.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/runtime-log/runtime-log-dev-client.md"
        - "src/runtime-log/runtime-log-dev-client.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0294**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/runtime-log/runtime-log-dev-client.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/runtime-log/runtime-log-dev-client.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/runtime-log/runtime-log-dev-client.md`，找到 **行动点 #0294** 的完整表述与依据引用的 YAML。
      3. Read `src/runtime-log/runtime-log-dev-client.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/runtime-log/runtime-log-dev-client.md` 中 **行动点 #0294** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0295"
AP-0295    status: "completed"
    dependencies: []
    action_point_id: "0295"
    audit_report: "working-plan/skill-audit-code-check/src/runtime-log/runtime-log-dev-client.md"
    target_src: "src/runtime-log/runtime-log-dev-client.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/runtime-log/runtime-log-dev-client.md"
        - "src/runtime-log/runtime-log-dev-client.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0295**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/runtime-log/runtime-log-dev-client.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/runtime-log/runtime-log-dev-client.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/runtime-log/runtime-log-dev-client.md`，找到 **行动点 #0295** 的完整表述与依据引用的 YAML。
      3. Read `src/runtime-log/runtime-log-dev-client.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/runtime-log/runtime-log-dev-client.md` 中 **行动点 #0295** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0296"
    title: "行动点 #0296"
    status: "completed"
    dependencies: []
    action_point_id: "0296"
    audit_report: "working-plan/skill-audit-code-check/src/runtime-log/runtime-log-dev-server.md"
    target_src: "src/runtime-log/runtime-log-dev-server.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/runtime-log/runtime-log-dev-server.md"
        - "src/runtime-log/runtime-log-dev-server.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0296**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/runtime-log/runtime-log-dev-server.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/runtime-log/runtime-log-dev-server.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/runtime-log/runtime-log-dev-server.md`，找到 **行动点 #0296** 的完整表述与依据引用的 YAML。
      3. Read `src/runtime-log/runtime-log-dev-server.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/runtime-log/runtime-log-dev-server.md` 中 **行动点 #0296** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0297"
AP-0297    status: "completed"
    dependencies: []
    action_point_id: "0297"
    audit_report: "working-plan/skill-audit-code-check/src/runtime-log/runtime-log-dev-server.md"
    target_src: "src/runtime-log/runtime-log-dev-server.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/runtime-log/runtime-log-dev-server.md"
        - "src/runtime-log/runtime-log-dev-server.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0297**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/runtime-log/runtime-log-dev-server.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/runtime-log/runtime-log-dev-server.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/runtime-log/runtime-log-dev-server.md`，找到 **行动点 #0297** 的完整表述与依据引用的 YAML。
      3. Read `src/runtime-log/runtime-log-dev-server.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/runtime-log/runtime-log-dev-server.md` 中 **行动点 #0297** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0298"
AP-0298    status: "completed"
    dependencies: []
    action_point_id: "0298"
    audit_report: "working-plan/skill-audit-code-check/src/runtime-log/runtime-log-dev-server.md"
    target_src: "src/runtime-log/runtime-log-dev-server.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/runtime-log/runtime-log-dev-server.md"
        - "src/runtime-log/runtime-log-dev-server.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0298**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/runtime-log/runtime-log-dev-server.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/runtime-log/runtime-log-dev-server.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/runtime-log/runtime-log-dev-server.md`，找到 **行动点 #0298** 的完整表述与依据引用的 YAML。
      3. Read `src/runtime-log/runtime-log-dev-server.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/runtime-log/runtime-log-dev-server.md` 中 **行动点 #0298** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0299"
    title: "行动点 #0299"
    status: "completed"
    dependencies: []
    action_point_id: "0299"
    audit_report: "working-plan/skill-audit-code-check/src/runtime-log/runtime-log-session-logger.md"
    target_src: "src/runtime-log/runtime-log-session-logger.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/runtime-log/runtime-log-session-logger.md"
        - "src/runtime-log/runtime-log-session-logger.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0299**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/runtime-log/runtime-log-session-logger.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/runtime-log/runtime-log-session-logger.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/runtime-log/runtime-log-session-logger.md`，找到 **行动点 #0299** 的完整表述与依据引用的 YAML。
      3. Read `src/runtime-log/runtime-log-session-logger.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/runtime-log/runtime-log-session-logger.md` 中 **行动点 #0299** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0300"
AP-0300    status: "completed"
    dependencies: []
    action_point_id: "0300"
    audit_report: "working-plan/skill-audit-code-check/src/runtime-log/runtime-log-session-logger.md"
    target_src: "src/runtime-log/runtime-log-session-logger.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/runtime-log/runtime-log-session-logger.md"
        - "src/runtime-log/runtime-log-session-logger.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0300**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/runtime-log/runtime-log-session-logger.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/runtime-log/runtime-log-session-logger.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/runtime-log/runtime-log-session-logger.md`，找到 **行动点 #0300** 的完整表述与依据引用的 YAML。
      3. Read `src/runtime-log/runtime-log-session-logger.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/runtime-log/runtime-log-session-logger.md` 中 **行动点 #0300** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0301"
AP-0301    status: "completed"
    dependencies: []
    action_point_id: "0301"
    audit_report: "working-plan/skill-audit-code-check/src/runtime-log/runtime-log-session-logger.md"
    target_src: "src/runtime-log/runtime-log-session-logger.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/runtime-log/runtime-log-session-logger.md"
        - "src/runtime-log/runtime-log-session-logger.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0301**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/runtime-log/runtime-log-session-logger.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/runtime-log/runtime-log-session-logger.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/runtime-log/runtime-log-session-logger.md`，找到 **行动点 #0301** 的完整表述与依据引用的 YAML。
      3. Read `src/runtime-log/runtime-log-session-logger.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/runtime-log/runtime-log-session-logger.md` 中 **行动点 #0301** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0302"
    title: "行动点 #0302"
    status: "completed"
    dependencies: []
    action_point_id: "0302"
    audit_report: "working-plan/skill-audit-code-check/src/runtime-log/runtime-log-session.md"
    target_src: "src/runtime-log/runtime-log-session.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/runtime-log/runtime-log-session.md"
        - "src/runtime-log/runtime-log-session.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0302**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/runtime-log/runtime-log-session.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/runtime-log/runtime-log-session.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/runtime-log/runtime-log-session.md`，找到 **行动点 #0302** 的完整表述与依据引用的 YAML。
      3. Read `src/runtime-log/runtime-log-session.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/runtime-log/runtime-log-session.md` 中 **行动点 #0302** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0303"
AP-0303    status: "completed"
    dependencies: []
    action_point_id: "0303"
    audit_report: "working-plan/skill-audit-code-check/src/runtime-log/runtime-log-session.md"
    target_src: "src/runtime-log/runtime-log-session.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/runtime-log/runtime-log-session.md"
        - "src/runtime-log/runtime-log-session.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0303**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/runtime-log/runtime-log-session.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/runtime-log/runtime-log-session.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/runtime-log/runtime-log-session.md`，找到 **行动点 #0303** 的完整表述与依据引用的 YAML。
      3. Read `src/runtime-log/runtime-log-session.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/runtime-log/runtime-log-session.md` 中 **行动点 #0303** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0304"
    title: "行动点 #0304"
    status: "completed"
    dependencies: []
    action_point_id: "0304"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/GameScene.md"
    target_src: "src/scenes/GameScene.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/GameScene.md"
        - "src/scenes/GameScene.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0304**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/GameScene.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/GameScene.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/GameScene.md`，找到 **行动点 #0304** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/GameScene.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/GameScene.md` 中 **行动点 #0304** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0305"
AP-0305    status: "completed"
    dependencies: []
    action_point_id: "0305"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/GameScene.md"
    target_src: "src/scenes/GameScene.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/GameScene.md"
        - "src/scenes/GameScene.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0305**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/GameScene.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/GameScene.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/GameScene.md`，找到 **行动点 #0305** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/GameScene.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/GameScene.md` 中 **行动点 #0305** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0306"
AP-0306    status: "completed"
    dependencies: []
    action_point_id: "0306"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/GameScene.md"
    target_src: "src/scenes/GameScene.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/GameScene.md"
        - "src/scenes/GameScene.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0306**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/GameScene.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/GameScene.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/GameScene.md`，找到 **行动点 #0306** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/GameScene.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/GameScene.md` 中 **行动点 #0306** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0307"
AP-0307    status: "completed"
    dependencies: []
    action_point_id: "0307"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/GameScene.md"
    target_src: "src/scenes/GameScene.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/GameScene.md"
        - "src/scenes/GameScene.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0307**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/GameScene.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/GameScene.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/GameScene.md`，找到 **行动点 #0307** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/GameScene.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/GameScene.md` 中 **行动点 #0307** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0308"
AP-0308    status: "completed"
    dependencies: []
    action_point_id: "0308"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/GameScene.md"
    target_src: "src/scenes/GameScene.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/GameScene.md"
        - "src/scenes/GameScene.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0308**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/GameScene.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/GameScene.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/GameScene.md`，找到 **行动点 #0308** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/GameScene.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/GameScene.md` 中 **行动点 #0308** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0309"
    title: "行动点 #0309"
    status: "completed"
    dependencies: []
    action_point_id: "0309"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/game-scene-camera-controls.md"
    target_src: "src/scenes/game-scene-camera-controls.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/game-scene-camera-controls.md"
        - "src/scenes/game-scene-camera-controls.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0309**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/game-scene-camera-controls.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/game-scene-camera-controls.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/game-scene-camera-controls.md`，找到 **行动点 #0309** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/game-scene-camera-controls.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/game-scene-camera-controls.md` 中 **行动点 #0309** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0310"
AP-0310    status: "completed"
    dependencies: []
    action_point_id: "0310"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/game-scene-camera-controls.md"
    target_src: "src/scenes/game-scene-camera-controls.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/game-scene-camera-controls.md"
        - "src/scenes/game-scene-camera-controls.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0310**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/game-scene-camera-controls.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/game-scene-camera-controls.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/game-scene-camera-controls.md`，找到 **行动点 #0310** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/game-scene-camera-controls.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/game-scene-camera-controls.md` 中 **行动点 #0310** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0311"
AP-0311    status: "completed"
    dependencies: []
    action_point_id: "0311"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/game-scene-camera-controls.md"
    target_src: "src/scenes/game-scene-camera-controls.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/game-scene-camera-controls.md"
        - "src/scenes/game-scene-camera-controls.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0311**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/game-scene-camera-controls.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/game-scene-camera-controls.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/game-scene-camera-controls.md`，找到 **行动点 #0311** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/game-scene-camera-controls.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/game-scene-camera-controls.md` 中 **行动点 #0311** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0312"
    title: "行动点 #0312"
    status: "completed"
    dependencies: []
    action_point_id: "0312"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/game-scene-floor-interaction.md"
    target_src: "src/scenes/game-scene-floor-interaction.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/game-scene-floor-interaction.md"
        - "src/scenes/game-scene-floor-interaction.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0312**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/game-scene-floor-interaction.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/game-scene-floor-interaction.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/game-scene-floor-interaction.md`，找到 **行动点 #0312** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/game-scene-floor-interaction.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/game-scene-floor-interaction.md` 中 **行动点 #0312** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0313"
AP-0313    status: "completed"
    dependencies: []
    action_point_id: "0313"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/game-scene-floor-interaction.md"
    target_src: "src/scenes/game-scene-floor-interaction.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/game-scene-floor-interaction.md"
        - "src/scenes/game-scene-floor-interaction.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0313**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/game-scene-floor-interaction.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/game-scene-floor-interaction.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/game-scene-floor-interaction.md`，找到 **行动点 #0313** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/game-scene-floor-interaction.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/game-scene-floor-interaction.md` 中 **行动点 #0313** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0314"
AP-0314    status: "completed"
    dependencies: []
    action_point_id: "0314"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/game-scene-floor-interaction.md"
    target_src: "src/scenes/game-scene-floor-interaction.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/game-scene-floor-interaction.md"
        - "src/scenes/game-scene-floor-interaction.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0314**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/game-scene-floor-interaction.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/game-scene-floor-interaction.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/game-scene-floor-interaction.md`，找到 **行动点 #0314** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/game-scene-floor-interaction.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/game-scene-floor-interaction.md` 中 **行动点 #0314** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0315"
    title: "行动点 #0315"
    status: "completed"
    dependencies: []
    action_point_id: "0315"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/game-scene-hud-sync.md"
    target_src: "src/scenes/game-scene-hud-sync.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/game-scene-hud-sync.md"
        - "src/scenes/game-scene-hud-sync.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0315**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/game-scene-hud-sync.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/game-scene-hud-sync.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/game-scene-hud-sync.md`，找到 **行动点 #0315** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/game-scene-hud-sync.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/game-scene-hud-sync.md` 中 **行动点 #0315** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0316"
AP-0316    status: "completed"
    dependencies: []
    action_point_id: "0316"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/game-scene-hud-sync.md"
    target_src: "src/scenes/game-scene-hud-sync.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/game-scene-hud-sync.md"
        - "src/scenes/game-scene-hud-sync.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0316**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/game-scene-hud-sync.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/game-scene-hud-sync.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/game-scene-hud-sync.md`，找到 **行动点 #0316** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/game-scene-hud-sync.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/game-scene-hud-sync.md` 中 **行动点 #0316** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0317"
    title: "行动点 #0317"
    status: "completed"
    dependencies: []
    action_point_id: "0317"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/game-scene-keyboard-bindings.md"
    target_src: "src/scenes/game-scene-keyboard-bindings.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/game-scene-keyboard-bindings.md"
        - "src/scenes/game-scene-keyboard-bindings.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0317**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/game-scene-keyboard-bindings.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/game-scene-keyboard-bindings.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/game-scene-keyboard-bindings.md`，找到 **行动点 #0317** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/game-scene-keyboard-bindings.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/game-scene-keyboard-bindings.md` 中 **行动点 #0317** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0318"
AP-0318    status: "completed"
    dependencies: []
    action_point_id: "0318"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/game-scene-keyboard-bindings.md"
    target_src: "src/scenes/game-scene-keyboard-bindings.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/game-scene-keyboard-bindings.md"
        - "src/scenes/game-scene-keyboard-bindings.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0318**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/game-scene-keyboard-bindings.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/game-scene-keyboard-bindings.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/game-scene-keyboard-bindings.md`，找到 **行动点 #0318** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/game-scene-keyboard-bindings.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/game-scene-keyboard-bindings.md` 中 **行动点 #0318** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0319"
AP-0319    status: "completed"
    dependencies: []
    action_point_id: "0319"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/game-scene-keyboard-bindings.md"
    target_src: "src/scenes/game-scene-keyboard-bindings.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/game-scene-keyboard-bindings.md"
        - "src/scenes/game-scene-keyboard-bindings.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0319**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/game-scene-keyboard-bindings.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/game-scene-keyboard-bindings.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/game-scene-keyboard-bindings.md`，找到 **行动点 #0319** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/game-scene-keyboard-bindings.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/game-scene-keyboard-bindings.md` 中 **行动点 #0319** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0320"
    title: "行动点 #0320"
    status: "completed"
    dependencies: []
    action_point_id: "0320"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/game-scene-presentation.md"
    target_src: "src/scenes/game-scene-presentation.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/game-scene-presentation.md"
        - "src/scenes/game-scene-presentation.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0320**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/game-scene-presentation.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/game-scene-presentation.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/game-scene-presentation.md`，找到 **行动点 #0320** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/game-scene-presentation.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/game-scene-presentation.md` 中 **行动点 #0320** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0321"
AP-0321    status: "completed"
    dependencies: []
    action_point_id: "0321"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/game-scene-presentation.md"
    target_src: "src/scenes/game-scene-presentation.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/game-scene-presentation.md"
        - "src/scenes/game-scene-presentation.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0321**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/game-scene-presentation.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/game-scene-presentation.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/game-scene-presentation.md`，找到 **行动点 #0321** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/game-scene-presentation.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/game-scene-presentation.md` 中 **行动点 #0321** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0322"
    title: "行动点 #0322"
    status: "completed"
    dependencies: []
    action_point_id: "0322"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/mock-grid-cell-info.md"
    target_src: "src/scenes/mock-grid-cell-info.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/mock-grid-cell-info.md"
        - "src/scenes/mock-grid-cell-info.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0322**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/mock-grid-cell-info.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/mock-grid-cell-info.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/mock-grid-cell-info.md`，找到 **行动点 #0322** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/mock-grid-cell-info.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/mock-grid-cell-info.md` 中 **行动点 #0322** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0323"
AP-0323    status: "completed"
    dependencies: []
    action_point_id: "0323"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/mock-grid-cell-info.md"
    target_src: "src/scenes/mock-grid-cell-info.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/mock-grid-cell-info.md"
        - "src/scenes/mock-grid-cell-info.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0323**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/mock-grid-cell-info.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/mock-grid-cell-info.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/mock-grid-cell-info.md`，找到 **行动点 #0323** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/mock-grid-cell-info.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/mock-grid-cell-info.md` 中 **行动点 #0323** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0324"
    title: "行动点 #0324"
    status: "completed"
    dependencies: []
    action_point_id: "0324"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/mock-ground-items.md"
    target_src: "src/scenes/mock-ground-items.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/mock-ground-items.md"
        - "src/scenes/mock-ground-items.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0324**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/mock-ground-items.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/mock-ground-items.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/mock-ground-items.md`，找到 **行动点 #0324** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/mock-ground-items.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/mock-ground-items.md` 中 **行动点 #0324** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0325"
AP-0325    status: "completed"
    dependencies: []
    action_point_id: "0325"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/mock-ground-items.md"
    target_src: "src/scenes/mock-ground-items.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/mock-ground-items.md"
        - "src/scenes/mock-ground-items.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0325**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/mock-ground-items.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/mock-ground-items.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/mock-ground-items.md`，找到 **行动点 #0325** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/mock-ground-items.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/mock-ground-items.md` 中 **行动点 #0325** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0326"
AP-0326    status: "completed"
    dependencies: []
    action_point_id: "0326"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/mock-ground-items.md"
    target_src: "src/scenes/mock-ground-items.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/mock-ground-items.md"
        - "src/scenes/mock-ground-items.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0326**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/mock-ground-items.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/mock-ground-items.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/mock-ground-items.md`，找到 **行动点 #0326** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/mock-ground-items.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/mock-ground-items.md` 中 **行动点 #0326** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0327"
    title: "行动点 #0327"
    status: "completed"
    dependencies: []
    action_point_id: "0327"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/mock-pawn-profile-data.md"
    target_src: "src/scenes/mock-pawn-profile-data.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/mock-pawn-profile-data.md"
        - "src/scenes/mock-pawn-profile-data.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0327**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/mock-pawn-profile-data.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/mock-pawn-profile-data.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/mock-pawn-profile-data.md`，找到 **行动点 #0327** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/mock-pawn-profile-data.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/mock-pawn-profile-data.md` 中 **行动点 #0327** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0328"
AP-0328    status: "completed"
    dependencies: []
    action_point_id: "0328"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/mock-pawn-profile-data.md"
    target_src: "src/scenes/mock-pawn-profile-data.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/mock-pawn-profile-data.md"
        - "src/scenes/mock-pawn-profile-data.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0328**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/mock-pawn-profile-data.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/mock-pawn-profile-data.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/mock-pawn-profile-data.md`，找到 **行动点 #0328** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/mock-pawn-profile-data.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/mock-pawn-profile-data.md` 中 **行动点 #0328** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0329"
    title: "行动点 #0329"
    status: "completed"
    dependencies: []
    action_point_id: "0329"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/mock-task-marker-commands.md"
    target_src: "src/scenes/mock-task-marker-commands.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/mock-task-marker-commands.md"
        - "src/scenes/mock-task-marker-commands.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0329**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/mock-task-marker-commands.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/mock-task-marker-commands.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/mock-task-marker-commands.md`，找到 **行动点 #0329** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/mock-task-marker-commands.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/mock-task-marker-commands.md` 中 **行动点 #0329** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0330"
    title: "行动点 #0330"
    status: "completed"
    dependencies: []
    action_point_id: "0330"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/mock-task-marker-selection.md"
    target_src: "src/scenes/mock-task-marker-selection.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/mock-task-marker-selection.md"
        - "src/scenes/mock-task-marker-selection.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0330**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/mock-task-marker-selection.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/mock-task-marker-selection.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/mock-task-marker-selection.md`，找到 **行动点 #0330** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/mock-task-marker-selection.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/mock-task-marker-selection.md` 中 **行动点 #0330** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0331"
AP-0331    status: "completed"
    dependencies: []
    action_point_id: "0331"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/mock-task-marker-selection.md"
    target_src: "src/scenes/mock-task-marker-selection.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/mock-task-marker-selection.md"
        - "src/scenes/mock-task-marker-selection.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0331**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/mock-task-marker-selection.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/mock-task-marker-selection.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/mock-task-marker-selection.md`，找到 **行动点 #0331** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/mock-task-marker-selection.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/mock-task-marker-selection.md` 中 **行动点 #0331** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0332"
    title: "行动点 #0332"
    status: "completed"
    dependencies: []
    action_point_id: "0332"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/renderers/building-renderer.md"
    target_src: "src/scenes/renderers/building-renderer.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/renderers/building-renderer.md"
        - "src/scenes/renderers/building-renderer.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0332**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/renderers/building-renderer.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/renderers/building-renderer.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/renderers/building-renderer.md`，找到 **行动点 #0332** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/renderers/building-renderer.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/renderers/building-renderer.md` 中 **行动点 #0332** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0333"
AP-0333    status: "completed"
    dependencies: []
    action_point_id: "0333"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/renderers/building-renderer.md"
    target_src: "src/scenes/renderers/building-renderer.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/renderers/building-renderer.md"
        - "src/scenes/renderers/building-renderer.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0333**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/renderers/building-renderer.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/renderers/building-renderer.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/renderers/building-renderer.md`，找到 **行动点 #0333** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/renderers/building-renderer.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/renderers/building-renderer.md` 中 **行动点 #0333** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0334"
    title: "行动点 #0334"
    status: "completed"
    dependencies: []
    action_point_id: "0334"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/renderers/grid-renderer.md"
    target_src: "src/scenes/renderers/grid-renderer.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/renderers/grid-renderer.md"
        - "src/scenes/renderers/grid-renderer.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0334**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/renderers/grid-renderer.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/renderers/grid-renderer.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/renderers/grid-renderer.md`，找到 **行动点 #0334** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/renderers/grid-renderer.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/renderers/grid-renderer.md` 中 **行动点 #0334** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0335"
AP-0335    status: "completed"
    dependencies: []
    action_point_id: "0335"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/renderers/grid-renderer.md"
    target_src: "src/scenes/renderers/grid-renderer.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/renderers/grid-renderer.md"
        - "src/scenes/renderers/grid-renderer.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0335**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/renderers/grid-renderer.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/renderers/grid-renderer.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/renderers/grid-renderer.md`，找到 **行动点 #0335** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/renderers/grid-renderer.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/renderers/grid-renderer.md` 中 **行动点 #0335** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0336"
AP-0336    status: "completed"
    dependencies: []
    action_point_id: "0336"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/renderers/grid-renderer.md"
    target_src: "src/scenes/renderers/grid-renderer.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/renderers/grid-renderer.md"
        - "src/scenes/renderers/grid-renderer.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0336**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/renderers/grid-renderer.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/renderers/grid-renderer.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/renderers/grid-renderer.md`，找到 **行动点 #0336** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/renderers/grid-renderer.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/renderers/grid-renderer.md` 中 **行动点 #0336** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0337"
    title: "行动点 #0337"
    status: "completed"
    dependencies: []
    action_point_id: "0337"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/renderers/ground-items-renderer.md"
    target_src: "src/scenes/renderers/ground-items-renderer.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/renderers/ground-items-renderer.md"
        - "src/scenes/renderers/ground-items-renderer.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0337**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/renderers/ground-items-renderer.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/renderers/ground-items-renderer.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/renderers/ground-items-renderer.md`，找到 **行动点 #0337** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/renderers/ground-items-renderer.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/renderers/ground-items-renderer.md` 中 **行动点 #0337** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0338"
AP-0338    status: "completed"
    dependencies: []
    action_point_id: "0338"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/renderers/ground-items-renderer.md"
    target_src: "src/scenes/renderers/ground-items-renderer.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/renderers/ground-items-renderer.md"
        - "src/scenes/renderers/ground-items-renderer.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0338**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/renderers/ground-items-renderer.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/renderers/ground-items-renderer.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/renderers/ground-items-renderer.md`，找到 **行动点 #0338** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/renderers/ground-items-renderer.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/renderers/ground-items-renderer.md` 中 **行动点 #0338** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0339"
AP-0339    status: "completed"
    dependencies: []
    action_point_id: "0339"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/renderers/ground-items-renderer.md"
    target_src: "src/scenes/renderers/ground-items-renderer.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/renderers/ground-items-renderer.md"
        - "src/scenes/renderers/ground-items-renderer.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0339**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/renderers/ground-items-renderer.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/renderers/ground-items-renderer.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/renderers/ground-items-renderer.md`，找到 **行动点 #0339** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/renderers/ground-items-renderer.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/renderers/ground-items-renderer.md` 中 **行动点 #0339** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0340"
    title: "行动点 #0340"
    status: "completed"
    dependencies: []
    action_point_id: "0340"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/renderers/pawn-renderer.md"
    target_src: "src/scenes/renderers/pawn-renderer.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/renderers/pawn-renderer.md"
        - "src/scenes/renderers/pawn-renderer.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0340**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/renderers/pawn-renderer.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/renderers/pawn-renderer.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/renderers/pawn-renderer.md`，找到 **行动点 #0340** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/renderers/pawn-renderer.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/renderers/pawn-renderer.md` 中 **行动点 #0340** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0341"
AP-0341    status: "completed"
    dependencies: []
    action_point_id: "0341"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/renderers/pawn-renderer.md"
    target_src: "src/scenes/renderers/pawn-renderer.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/renderers/pawn-renderer.md"
        - "src/scenes/renderers/pawn-renderer.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0341**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/renderers/pawn-renderer.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/renderers/pawn-renderer.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/renderers/pawn-renderer.md`，找到 **行动点 #0341** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/renderers/pawn-renderer.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/renderers/pawn-renderer.md` 中 **行动点 #0341** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0342"
AP-0342    status: "completed"
    dependencies: []
    action_point_id: "0342"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/renderers/pawn-renderer.md"
    target_src: "src/scenes/renderers/pawn-renderer.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/renderers/pawn-renderer.md"
        - "src/scenes/renderers/pawn-renderer.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0342**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/renderers/pawn-renderer.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/renderers/pawn-renderer.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/renderers/pawn-renderer.md`，找到 **行动点 #0342** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/renderers/pawn-renderer.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/renderers/pawn-renderer.md` 中 **行动点 #0342** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0343"
    title: "行动点 #0343"
    status: "completed"
    dependencies: []
    action_point_id: "0343"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/renderers/selection-renderer.md"
    target_src: "src/scenes/renderers/selection-renderer.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/renderers/selection-renderer.md"
        - "src/scenes/renderers/selection-renderer.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0343**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/renderers/selection-renderer.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/renderers/selection-renderer.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/renderers/selection-renderer.md`，找到 **行动点 #0343** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/renderers/selection-renderer.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/renderers/selection-renderer.md` 中 **行动点 #0343** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0344"
AP-0344    status: "completed"
    dependencies: []
    action_point_id: "0344"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/renderers/selection-renderer.md"
    target_src: "src/scenes/renderers/selection-renderer.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/renderers/selection-renderer.md"
        - "src/scenes/renderers/selection-renderer.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0344**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/renderers/selection-renderer.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/renderers/selection-renderer.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/renderers/selection-renderer.md`，找到 **行动点 #0344** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/renderers/selection-renderer.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/renderers/selection-renderer.md` 中 **行动点 #0344** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0345"
AP-0345    status: "completed"
    dependencies: []
    action_point_id: "0345"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/renderers/selection-renderer.md"
    target_src: "src/scenes/renderers/selection-renderer.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/renderers/selection-renderer.md"
        - "src/scenes/renderers/selection-renderer.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0345**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/renderers/selection-renderer.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/renderers/selection-renderer.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/renderers/selection-renderer.md`，找到 **行动点 #0345** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/renderers/selection-renderer.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/renderers/selection-renderer.md` 中 **行动点 #0345** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0346"
    title: "行动点 #0346"
    status: "completed"
    dependencies: []
    action_point_id: "0346"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/renderers/tree-renderer.md"
    target_src: "src/scenes/renderers/tree-renderer.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/renderers/tree-renderer.md"
        - "src/scenes/renderers/tree-renderer.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0346**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/renderers/tree-renderer.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/renderers/tree-renderer.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/renderers/tree-renderer.md`，找到 **行动点 #0346** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/renderers/tree-renderer.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/renderers/tree-renderer.md` 中 **行动点 #0346** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0347"
AP-0347    status: "completed"
    dependencies: []
    action_point_id: "0347"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/renderers/tree-renderer.md"
    target_src: "src/scenes/renderers/tree-renderer.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/renderers/tree-renderer.md"
        - "src/scenes/renderers/tree-renderer.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0347**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/renderers/tree-renderer.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/renderers/tree-renderer.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/renderers/tree-renderer.md`，找到 **行动点 #0347** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/renderers/tree-renderer.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/renderers/tree-renderer.md` 中 **行动点 #0347** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0348"
AP-0348    status: "completed"
    dependencies: []
    action_point_id: "0348"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/renderers/tree-renderer.md"
    target_src: "src/scenes/renderers/tree-renderer.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/renderers/tree-renderer.md"
        - "src/scenes/renderers/tree-renderer.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0348**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/renderers/tree-renderer.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/renderers/tree-renderer.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/renderers/tree-renderer.md`，找到 **行动点 #0348** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/renderers/tree-renderer.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/renderers/tree-renderer.md` 中 **行动点 #0348** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0349"
    title: "行动点 #0349"
    status: "completed"
    dependencies: []
    action_point_id: "0349"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/renderers/zone-overlay-renderer.md"
    target_src: "src/scenes/renderers/zone-overlay-renderer.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/renderers/zone-overlay-renderer.md"
        - "src/scenes/renderers/zone-overlay-renderer.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0349**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/renderers/zone-overlay-renderer.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/renderers/zone-overlay-renderer.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/renderers/zone-overlay-renderer.md`，找到 **行动点 #0349** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/renderers/zone-overlay-renderer.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/renderers/zone-overlay-renderer.md` 中 **行动点 #0349** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0350"
AP-0350    status: "completed"
    dependencies: []
    action_point_id: "0350"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/renderers/zone-overlay-renderer.md"
    target_src: "src/scenes/renderers/zone-overlay-renderer.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/renderers/zone-overlay-renderer.md"
        - "src/scenes/renderers/zone-overlay-renderer.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0350**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/renderers/zone-overlay-renderer.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/renderers/zone-overlay-renderer.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/renderers/zone-overlay-renderer.md`，找到 **行动点 #0350** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/renderers/zone-overlay-renderer.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/renderers/zone-overlay-renderer.md` 中 **行动点 #0350** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0351"
AP-0351    status: "completed"
    dependencies: []
    action_point_id: "0351"
    audit_report: "working-plan/skill-audit-code-check/src/scenes/renderers/zone-overlay-renderer.md"
    target_src: "src/scenes/renderers/zone-overlay-renderer.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/scenes/renderers/zone-overlay-renderer.md"
        - "src/scenes/renderers/zone-overlay-renderer.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0351**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/scenes/renderers/zone-overlay-renderer.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/scenes/renderers/zone-overlay-renderer.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/scenes/renderers/zone-overlay-renderer.md`，找到 **行动点 #0351** 的完整表述与依据引用的 YAML。
      3. Read `src/scenes/renderers/zone-overlay-renderer.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/scenes/renderers/zone-overlay-renderer.md` 中 **行动点 #0351** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0352"
    title: "行动点 #0352"
    status: "completed"
    dependencies: []
    action_point_id: "0352"
    audit_report: "working-plan/skill-audit-code-check/src/ui/hud-manager.md"
    target_src: "src/ui/hud-manager.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/ui/hud-manager.md"
        - "src/ui/hud-manager.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0352**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/ui/hud-manager.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/ui/hud-manager.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/ui/hud-manager.md`，找到 **行动点 #0352** 的完整表述与依据引用的 YAML。
      3. Read `src/ui/hud-manager.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/ui/hud-manager.md` 中 **行动点 #0352** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0353"
AP-0353    status: "completed"
    dependencies: []
    action_point_id: "0353"
    audit_report: "working-plan/skill-audit-code-check/src/ui/hud-manager.md"
    target_src: "src/ui/hud-manager.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/ui/hud-manager.md"
        - "src/ui/hud-manager.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0353**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/ui/hud-manager.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/ui/hud-manager.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/ui/hud-manager.md`，找到 **行动点 #0353** 的完整表述与依据引用的 YAML。
      3. Read `src/ui/hud-manager.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/ui/hud-manager.md` 中 **行动点 #0353** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0354"
AP-0354    status: "completed"
    dependencies: []
    action_point_id: "0354"
    audit_report: "working-plan/skill-audit-code-check/src/ui/hud-manager.md"
    target_src: "src/ui/hud-manager.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/ui/hud-manager.md"
        - "src/ui/hud-manager.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0354**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/ui/hud-manager.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/ui/hud-manager.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/ui/hud-manager.md`，找到 **行动点 #0354** 的完整表述与依据引用的 YAML。
      3. Read `src/ui/hud-manager.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/ui/hud-manager.md` 中 **行动点 #0354** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0355"
AP-0355    status: "completed"
    dependencies: []
    action_point_id: "0355"
    audit_report: "working-plan/skill-audit-code-check/src/ui/hud-manager.md"
    target_src: "src/ui/hud-manager.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/ui/hud-manager.md"
        - "src/ui/hud-manager.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0355**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/ui/hud-manager.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/ui/hud-manager.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/ui/hud-manager.md`，找到 **行动点 #0355** 的完整表述与依据引用的 YAML。
      3. Read `src/ui/hud-manager.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/ui/hud-manager.md` 中 **行动点 #0355** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0356"
    title: "行动点 #0356"
    status: "completed"
    dependencies: []
    action_point_id: "0356"
    audit_report: "working-plan/skill-audit-code-check/src/ui/index.md"
    target_src: "src/ui/index.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/ui/index.md"
        - "src/ui/index.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0356**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/ui/index.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/ui/index.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/ui/index.md`，找到 **行动点 #0356** 的完整表述与依据引用的 YAML。
      3. Read `src/ui/index.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/ui/index.md` 中 **行动点 #0356** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0357"
    title: "行动点 #0357"
    status: "completed"
    dependencies: []
    action_point_id: "0357"
    audit_report: "working-plan/skill-audit-code-check/src/ui/menu-model.md"
    target_src: "src/ui/menu-model.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/ui/menu-model.md"
        - "src/ui/menu-model.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0357**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/ui/menu-model.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/ui/menu-model.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/ui/menu-model.md`，找到 **行动点 #0357** 的完整表述与依据引用的 YAML。
      3. Read `src/ui/menu-model.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/ui/menu-model.md` 中 **行动点 #0357** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0358"
AP-0358    status: "completed"
    dependencies: []
    action_point_id: "0358"
    audit_report: "working-plan/skill-audit-code-check/src/ui/menu-model.md"
    target_src: "src/ui/menu-model.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/ui/menu-model.md"
        - "src/ui/menu-model.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0358**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/ui/menu-model.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/ui/menu-model.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/ui/menu-model.md`，找到 **行动点 #0358** 的完整表述与依据引用的 YAML。
      3. Read `src/ui/menu-model.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/ui/menu-model.md` 中 **行动点 #0358** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0359"
AP-0359    status: "completed"
    dependencies: []
    action_point_id: "0359"
    audit_report: "working-plan/skill-audit-code-check/src/ui/menu-model.md"
    target_src: "src/ui/menu-model.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/ui/menu-model.md"
        - "src/ui/menu-model.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0359**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/ui/menu-model.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/ui/menu-model.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/ui/menu-model.md`，找到 **行动点 #0359** 的完整表述与依据引用的 YAML。
      3. Read `src/ui/menu-model.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/ui/menu-model.md` 中 **行动点 #0359** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0360"
    title: "行动点 #0360"
    status: "completed"
    dependencies: []
    action_point_id: "0360"
    audit_report: "working-plan/skill-audit-code-check/src/ui/runtime-debug-log-store.md"
    target_src: "src/ui/runtime-debug-log-store.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/ui/runtime-debug-log-store.md"
        - "src/ui/runtime-debug-log-store.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0360**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/ui/runtime-debug-log-store.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/ui/runtime-debug-log-store.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/ui/runtime-debug-log-store.md`，找到 **行动点 #0360** 的完整表述与依据引用的 YAML。
      3. Read `src/ui/runtime-debug-log-store.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/ui/runtime-debug-log-store.md` 中 **行动点 #0360** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0361"
    title: "行动点 #0361"
    status: "completed"
    dependencies: []
    action_point_id: "0361"
    audit_report: "working-plan/skill-audit-code-check/src/ui/status-display-model.md"
    target_src: "src/ui/status-display-model.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/ui/status-display-model.md"
        - "src/ui/status-display-model.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0361**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/ui/status-display-model.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/ui/status-display-model.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/ui/status-display-model.md`，找到 **行动点 #0361** 的完整表述与依据引用的 YAML。
      3. Read `src/ui/status-display-model.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/ui/status-display-model.md` 中 **行动点 #0361** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0362"
AP-0362    status: "completed"
    dependencies: []
    action_point_id: "0362"
    audit_report: "working-plan/skill-audit-code-check/src/ui/status-display-model.md"
    target_src: "src/ui/status-display-model.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/ui/status-display-model.md"
        - "src/ui/status-display-model.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0362**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/ui/status-display-model.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/ui/status-display-model.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/ui/status-display-model.md`，找到 **行动点 #0362** 的完整表述与依据引用的 YAML。
      3. Read `src/ui/status-display-model.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/ui/status-display-model.md` 中 **行动点 #0362** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0363"
    title: "行动点 #0363"
    status: "completed"
    dependencies: []
    action_point_id: "0363"
    audit_report: "working-plan/skill-audit-code-check/src/ui/ui-types.md"
    target_src: "src/ui/ui-types.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/ui/ui-types.md"
        - "src/ui/ui-types.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0363**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/ui/ui-types.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/ui/ui-types.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/ui/ui-types.md`，找到 **行动点 #0363** 的完整表述与依据引用的 YAML。
      3. Read `src/ui/ui-types.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/ui/ui-types.md` 中 **行动点 #0363** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0364"
AP-0364    status: "completed"
    dependencies: []
    action_point_id: "0364"
    audit_report: "working-plan/skill-audit-code-check/src/ui/ui-types.md"
    target_src: "src/ui/ui-types.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/ui/ui-types.md"
        - "src/ui/ui-types.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0364**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/ui/ui-types.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/ui/ui-types.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/ui/ui-types.md`，找到 **行动点 #0364** 的完整表述与依据引用的 YAML。
      3. Read `src/ui/ui-types.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/ui/ui-types.md` 中 **行动点 #0364** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0365"
    title: "行动点 #0365"
    status: "completed"
    dependencies: []
    action_point_id: "0365"
    audit_report: "working-plan/skill-audit-code-check/src/vite-env.d.md"
    target_src: "src/vite-env.d.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/vite-env.d.md"
        - "src/vite-env.d.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0365**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/vite-env.d.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/vite-env.d.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/vite-env.d.md`，找到 **行动点 #0365** 的完整表述与依据引用的 YAML。
      3. Read `src/vite-env.d.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/vite-env.d.md` 中 **行动点 #0365** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。

  - id: "AP-0366"
AP-0366    status: "completed"
    dependencies: []
    action_point_id: "0366"
    audit_report: "working-plan/skill-audit-code-check/src/vite-env.d.md"
    target_src: "src/vite-env.d.ts"
    context:
      read_files:
        - "working-plan/skill-audit-code-check/src/vite-env.d.md"
        - "src/vite-env.d.ts"
      reference_docs:
        - ".cursor/skills/code-audit-4.6/SKILL.md"
        # 具体 oh-code-design / oh-gen-doc 条目以审计报告「依据」为准，子 Agent 自行从报告中扩展读取
    subagent_context_injection: |
      这是 **code-audit-4.6** 审计结论的**落地修复**任务（非再次只读审计）。前因后果、文档层级与审计禁忌/工作流请先 **Read**：
      - `.cursor/skills/code-audit-4.6/SKILL.md`
      
      你被分配处理 **行动点 #0366**（全局编号）。在改 `src/` 之前，你必须：
      1. **Read** 审计报告全文：`working-plan/skill-audit-code-check/src/vite-env.d.md`，定位并理解该行动点的指控与建议；
      2. **Read** 对应实现文件：`src/vite-env.d.ts`（及报告中引用的关联文件），核对当前代码是否**仍符合**报告描述的问题状态；
      3. 若该问题在主线代码中**已被修复或已不再成立**，则**不要做无意义改动**，在交付说明中写明核对依据即可；
      4. 若仍成立，则按报告建议做**最小必要**修改，并遵守仓库现有风格与分层。
      
      禁止：未读审计条目即批量删改；扩大范围重构与本次行动点无关的模块。
    action: |
      1. Read `.cursor/skills/code-audit-4.6/SKILL.md`（理解本任务集背景）。
      2. Read 审计报告 `working-plan/skill-audit-code-check/src/vite-env.d.md`，找到 **行动点 #0366** 的完整表述与依据引用的 YAML。
      3. Read `src/vite-env.d.ts` 及本行动点实施所需的关联模块（仅从审计报告与代码引用中扩展，避免全仓漫游）。
      4. 核对现状：该行动点描述的问题是否仍存在；若不存在，记录证据并结束。
      5. 若仍存在：修改代码/配置/测试（按需），保持改动与单条行动点强相关。
      6. 运行 `npx tsc --noEmit`（仓库根目录）确认无新增类型错误。
    acceptance_criteria: |
      - [ ] Shell（仓库根）: `npx tsc --noEmit` 退出码为 0。
      - [ ] 已 Read `working-plan/skill-audit-code-check/src/vite-env.d.md` 中 **行动点 #0366** 原文，且交付说明中含「已核对」结论：已修复 / 无需修改（附证据）/ 已部分修复（附剩余项）。
      - [ ] 若修改了 `src/`：改动与审计建议一致，且 `rg`/Read 可定位到与行动点相关的关键符号或行为变化。
